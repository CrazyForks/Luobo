# Luobo 视觉与交互对标飞牛音乐（fnos）技术方案

> 状态：**方案待拍板**（2026-09-23），未开工
> 范围：以飞牛音乐 App 为标杆，对齐 Luobo 的**视觉语言**（悬浮毛玻璃 + 大圆角 + 玫红强调色）与**交互骨架**（页面骨架 / 列表呈现 / 浮层范式 / 表单范式 / 全局常驻元素）
> 设计约束：不改业务逻辑、不改存储结构、不改服务端协议；**不做「处处毛玻璃」**（§5 性能红线）；导航骨架与公共组件新建文件，旧实现保留不删（可回退）；**文档先行，用户确认后开工**
> 对标素材：`/Users/v_chengsiyuan03/Downloads/fnosmusic/`（**40 张**，2026-09-23 整理归档；已按页面族分 9 个子目录 `01-首页` … `09-箭头音乐`，**逐张说明见该目录下 `_索引.md`**）
> 其中 `09-箭头音乐` 为**第三方 Subsonic 客户端**，仅作**服务器管理流程**参考（详见 `docs/设置页重构技术方案.md` §9）

---

## 1. 背景与目标

### 1.1 用户诉求

用户认为飞牛音乐 App「审美非常好」，希望 Luobo 改成「和它一样的效果」，并强调——**核心不只是视觉，还有「一套整理好的交互逻辑」**。

### 1.2 对标物定位

飞牛音乐（fnos）是飞牛 NAS 的自研移动端音乐 App，**连自家服务端、可写回元数据**，因此其「歌曲信息」是完整可编辑表单。Luobo 是 Subsonic/OpenSubsonic 客户端（Navidrome / 道理鱼 / Jellyfin），**协议层不支持歌曲元数据写入**。

> **已拍板（2026-09-23）**：歌曲信息页**只展示、不修改**。原唯一硬卡点消除，「改歌手/改风格」选择器页**不做**。

### 1.3 目标

- **视觉**：把已有但零散的玻璃基建收敛为统一设计语言（token + 公共组件）
- **交互**：对齐 fnos 的页面骨架与各类浮层/列表/表单范式，补齐缺失页面
- **工程**：不推翻现有可用实现（悬浮底栏/播放条已达标），只做「收口 + 补页 + 统一浮层」

---

## 2. fnos 设计语言拆解

### 2.1 视觉配方

| # | 配方 | 取值（截图实测） |
|---|---|---|
| 1 | **悬浮** | 元素脱离屏幕边缘，四周留 12px 边距 |
| 2 | **大圆角** | 胶囊 `radius 30`；卡片 `16~20`；封面 `16~24`；按钮全圆 |
| 3 | **半透明白 + 模糊** | 白 `alpha 0.6~0.75` + `blur 12~24` + 1px 高光描边（白 `alpha 0.8`） |
| 4 | **柔和外阴影** | 黑 `alpha 0.12~0.18`、`blur 24~28`、`offset (0, 6)` |
| 5 | **强调色做实心色块** | 选中态是**玫红实心圆/胶囊**，不是图标变色 |

- 底色：浅色 = 浅灰 `#F2F2F7` 系；**深色 = 近黑 `#121212` 系**
- 卡片：浅色 = 纯白；**深色 = 深灰 `#1C1C1E ~ #2C2C2E`**
- 强调色：玫红，约 `#F0305C`（**深色下不变**）
- **渐变头图**：程序生成的大面积柔和渐变（绿 / 橙红黄 / 紫），中心一枚半透明图标（**深色下不变**）
- **深色模式已实测存在**（2026-09-23 补充截图），规范见 §2.9

### 2.2 页面骨架（3 种）

| 骨架 | 结构 | fnos 实例 |
|---|---|---|
| **大标题页** | 左对齐大字标题 + 右上悬浮圆钮 | 首页、音乐库、设置 |
| **列表页** | **居中标题** + 左上圆返回 + 右上圆（搜索 / `···`）；下方「共 N 首」+ 视图/排序图标 | 全部歌曲、专辑、歌手、音乐风格、最近播放、收藏 |
| **模态表单页** | 左上圆（`×` 或 `←`）+ 居中标题 + 右上**实心圆 ✓** | 新建歌单、歌曲信息、修改歌手、修改风格 |
| **全屏播放页** | 无 AppBar；**整屏封面模糊放大为背景** + 左上 `⌄` 下箭头收起；全白文字 | 正在播放 |
| **折叠头详情页** | 头图随滚动收起；**分段控件（歌曲｜专辑）从内容区「迁移」到顶栏右侧**，同时返回圆钮变成**带标题的胶囊** | 歌手详情页 |

> **列表页头部再分两型**：
> - **普通头**：标题 + 「共 N 首」+ 视图/排序图标（全部歌曲、专辑、歌手、音乐风格）
> - **头图式**：超大渐变封面 + 居中标题 + **双黑胶囊按钮**（`▶ 播放` / `⤨ 随机`）（最近播放、收藏、歌单详情）

### 2.3 列表与卡片范式（4 种呈现）

| # | 呈现 | 特征 | fnos 实例 |
|---|---|---|---|
| 1 | **分组白卡** | 一张白卡装一组同类项，组内分隔线**缩进到文字起始处** | 设置页、音乐库页 |
| 2 | **朴素列表** | 无卡片，行间**全宽细分隔线**；行 = 大字标题 + 灰字副标题 + chevron | 音乐风格、歌手 |
| 3 | **双列网格** | 封面 + 标题 + 副标题 | 专辑 |
| 4 | **歌曲行** | 封面（带播放三角覆盖）+ 歌名 + 歌手 + 右侧 `⋮` | 全部歌曲、最近播放、收藏 |

**行结构统一三件套**：`图标/封面 + 文字 + 右侧位`，右侧位为 状态值 / chevron / `⋮` / 开关 之一。
状态值实例：`1009.48 MB`（自动缓存）、`V1.0.1`（关于）、`460 首歌`（风格行副标题）。

> ⚠️ **「更多」有两种形态，别混用（2026-09-23 用户指出）**
>
> | 位置 | 形态 | fnos 实例 |
> |---|---|---|
> | **顶部悬浮圆钮内** | **横向 `···`** | 收藏页 / 歌单详情的右上圆钮 |
> | **行内右侧** | **纵向 `⋮`** | 歌曲行、服务器列表行 |
>
> **Luobo 现状**：`song_tile.dart:430` 用的是 `Icons.more_horiz`（**横向**），按 fnos 行内应为**纵向** → 需改。
> **`AppIcons` 映射层**因此拆成两个键：`AppIcons.moreH`（圆钮·横向）/ `AppIcons.moreV`（行内·纵向）。

> ⚠️ **右侧位必须贴右边缘（2026-09-23 用户指出，我首版设计稿写错）**
> - **chevron 永远贴右边缘**，**不紧跟文字** —— 无论该行有没有状态值
> - 有状态值时：`文字 … 状态值 ›`（状态值右对齐，chevron 紧随其后）
> - 无状态值时：`文字 … ›`（chevron 自己撑到最右）
> - 实现要点：chevron 用 `margin-left: auto`（Flutter 里即 `Spacer()` 或 `Expanded` 包裹文字），**有状态值时再让状态值接管 `auto`、chevron 改回固定间距**
>
> 首版 HTML 稿把 chevron 写成固定 `margin-left: 7px`，导致无状态值的行 chevron 紧跟文字 —— 已修正。**这条在 C1 的 `SettingsTile` 组件里要一次做对，否则会污染所有行。**

### 2.4 浮层与弹层范式（5 种）

| # | 范式 | 特征 | fnos 实例 |
|---|---|---|---|
| 1 | **锚定毛玻璃菜单** | 从触发按钮**锚定展开**，毛玻璃圆角浮层；选中项带**玫红勾 + 排序方向箭头** | 排序菜单（全部歌曲 2 项 / 专辑 5 项 / 歌手 3 项 / 风格 2 项） |
| 2 | **底部胶囊叠面板** | 底部升起白圆角面板；标题居中大字 + **一叠全宽胶囊按钮**（普通灰底 / **危险项淡红底红字** / 末尾「取消」）；背景变暗 | 歌单设置（编辑 / 清除失效歌曲 / 删除 / 取消） |
| 3 | **多选 Sheet** | 底部升起**全屏圆角 sheet**（带拖拽把手）；顶部 = 玫红圆 `−` + 「已选 N 首」+ 「完成」；行左侧**圆形选择框**（灰圈 → 玫红实心+白勾）；底部 **4 等分操作栏** | 歌曲列表多选（播放 / 加入播放列表 / 下载 / 添加到歌单） |
| 4 | **模态表单 Sheet** | 底部升起全屏圆角 sheet + §2.2 模态骨架 | 新建歌单 |
| 5 | **上下文操作面板** | 行内 `⋮` 触发，底部升起；**顶部 = 当前对象上下文**（封面 + 主标题 + 副标题）；**主操作横排 3 个方形按钮**（图标在上文字在下）；**次操作竖列圆角行**（左文字 + 右图标） | 歌曲行 `⋮`（播放 / 下一首播放 / 加入播放列表 ＋ 专辑 / 歌手 / 下载 / 收藏 / 添加到歌单 / 歌曲信息） |

> **范式 5 是对 §3.2 #7 的修正**：fnos 的歌曲 `⋮` 菜单**不是**简单弹窗列表，而是「上下文头 + 主操作横排 + 次操作竖列」的三段式结构。这**修正了 2026-09-23 早先"保留现有弹窗列表"的判断**——结论仍是"不做胶囊叠"，但要做的是**结构升级**而非原样保留（见 §7 ⑥）。
>
> ⚠️ **该面板是「按来源自适应」的 —— 共 3 个变体（2026-09-23 补充截图确认，用户特别指出这点）**：
>
> | 来源 | 上下文头 | 主操作横排 3 方形按钮 | 「收藏」 | 「从当前歌单移除」 |
> |---|---|---|---|---|
> | **列表页 / 首页** | ✅ | ✅ | ✅ | ❌ |
> | **播放页** | ✅ | ❌ | ❌（已有 `♡`） | ❌ |
> | **歌单详情页内** | ✅ | ✅ | ✅ | ✅（位于「添加到歌单」之后、「歌曲信息」之前） |
>
> 其余条目恒有：专辑 / 歌手 / 下载 / 添加到歌单 / 歌曲信息。**实现时须按调用来源裁剪条目，不能一套写死。**

### 2.5 表单范式

- **可编辑字段**：**标签在卡片外**（小字），输入框是独立白卡
- **只读信息**：`label + value` 竖排**排在卡内**（文件位置 / 文件大小 / 添加日期）
- 二者用不同呈现，一眼区分可否编辑
- **灰字说明**：单选/开关项**下方跟灰字解释**；小节标题也在卡外
- **已选 chip**：玫红淡底胶囊 + `×`（搜索框下方流式排列）
- **封面预览**：大方形封面 + 两个**圆形悬浮小按钮**（`↻` 换一张 / `✏️` 编辑）+ 灰字格式说明（「支持JPG、JPEG、PNG、WEBP，大小<5MB」）

### 2.6 全局常驻元素

| 元素 | 行为 | 出现范围 |
|---|---|---|
| **悬浮胶囊播放条** | 封面 + 歌名/歌手 + `▶` + `☰`（队列）；**悬浮覆盖在内容之上**（列表可滚到它下面） | **所有有内容的页面**（含无底栏的二级页） |
| **悬浮胶囊底栏** | 3 图标 + 分隔线 + 当前专辑圆封面 | **仅一级页**（首页 / 音乐库 / 设置） |

> **关键**：播放条是**覆盖式**而非占位式——内容从玻璃下方流过，这是「液态玻璃」观感的主要来源之一。

### 2.7 搜索页范式

顶部白圆角搜索框（放大镜 + placeholder）+ 右侧「取消」文字 → 「搜索历史」标题 + 右侧垃圾桶（清空）→ **历史 chips 流式换行**（白色胶囊）。覆盖层，无底栏。

### 2.8 交互设计原则（「交互也很好」的底层原因）

> 用户明确强调「不只是审美，交互也很好」。**视觉是表层，以下原则才是这套设计的价值所在**——实现时只抄视觉而丢掉这些，会得到「像 fnos 但更难用」的结果。

**核心命题：用「空间位置」表达作用域，而不是用文字标签。**

| # | 原则 | 具体表现 | 反例（要避免的） |
|---|---|---|---|
| 1 | **操作密度恒定** | 任何页面的全局可点元素固定为：左上 1 + 右上 1 + 底部 1。页面再复杂，「在哪点」都不变 | 每个页面按钮位置/数量都不一样，用户需重新找 |
| 2 | **位置编码作用域** | 左上=离开本页；右上=对本页的动作（搜索/更多/保存）；底部=跨页面（导航/当前播放）；行右=对这一个对象 | 把全局操作和行内操作混在同一区域 |
| 3 | **状态外显，不靠进入** | 行右侧直显状态值（`1009.48 MB` / `V1.0.1` / `460 首歌`）；排序菜单直显当前选中项 + 方向；已选 chip 直显已选项 | 要「点进去看一眼再退出来」才知道当前状态 |
| 4 | **决策降维** | 排序 = **字段 × 方向**两个维度：菜单列字段，勾选 + 箭头表达方向，再点同项切方向 | 把方向烤进标签、平铺成 N 个选项（见 §3.2 #23） |
| 5 | **危险靠视觉重量劝退，不靠弹窗打断** | 淡红底 + 红字 + 单独一行 + 置于末尾 | 一律弹 AlertDialog 二次确认，打断流程 |
| 6 | **模式切换显式化** | 多选 = 从底部升起全新全屏 sheet，且**底部操作栏先告知「选完能干什么」** | 在原列表上叠勾选框，模式边界模糊 |
| 7 | **唯一退出路径** | 进任何一层，都从同一个位置（左上）退出 | 有的页面可滑退、有的要按钮、有的要返回键 |
| 8 | **内容与操作空间分离** | 列表行只负责「选择」，行内不放操作按钮（仅最右 `⋮`），保证列表可扫读 | 行内塞满图标按钮，破坏扫读 |
| 9 | **浮层锚定而非居中** | 排序菜单从触发按钮旁展开，视线不需重新定位 | 弹到屏幕中央，来源与结果位置分离 |
| 10 | **一致性到肌肉记忆** | 「左上圆返回」出现在每张二级页；「右上实心圆 ✓」出现在每处保存 | 同类操作在不同页面形态不同 |

**一句话**：你不需要读任何标签，**看位置就知道这个操作作用在什么范围**。

### 2.9 深色模式规范（2026-09-23 实测补充）

深色模式**已实测存在**（首页 / 歌手页 / 设置页 / 排序浮层 / 歌曲 `⋮` 面板 共 5 张深色截图）。核心结论：

| 元素 | 浅色 | 深色 | 变化 |
|---|---|---|---|
| 页面底色 | `#F2F2F7` | **近黑 `#121212` 系** | 换色 |
| 内容卡片 | 纯白 | **深灰 `#1C1C1E ~ #2C2C2E`** | 换色 |
| 卡片文字 | 黑 | 白 | 换色 |
| 副标题文字 | 灰 `#8E8E93` | 灰（略亮） | 换色 |
| 分隔线 | 浅灰 | 深灰细线 | 换色 |
| **强调色（玫红）** | `#F0305C` | **不变** | ✅ 不换 |
| **图标（玫红线性）** | 玫红 | **不变** | ✅ 不换 |
| **悬浮玻璃** | 白 `alpha 0.6~0.75` + 模糊 | **黑 `alpha ~0.5` + 模糊 + 白 `alpha 0.1` 描边** | 换 tint，保留模糊 |
| **渐变头图 / 渐变卡** | 绿 / 橙红黄 / 紫 | **完全不变** | ✅ 不换 |
| **结构 / 圆角 / 间距 / 字阶** | — | **完全一致** | ✅ 不变 |

**关键推论**：深色模式**只换颜色 token，不换任何结构**。这意味着 §4.1 的 `design_tokens.dart` 必须**从一开始就双色定义**（`LuoboColors.light` / `.dark`），否则后期补深色会返工。

**一处待确认**：深色设置页截图里，大标题「设置」被账号卡片**部分覆盖**，无法判断大标题是「固定 AppBar」还是「随内容滚动的页内标题」。浅色截图（未滚动）看不出差别 → 列入 §7 待确认。

### 2.10 全屏播放页规范

**布局（自上而下）**

1. **背景**：**彩色渐变流体动态背景**——从当前封面**取色**，整幅被噪声场平流、**持续流动**（Apple Music 式「流沙」），上覆线性暗渐变保证白字可读。设计语言见 §2.11，实现见 `docs/流沙流体背景技术文档.md`
2. **左上**：`⌄` 下箭头（收起播放页，非返回）
3. **封面**：居中大图
4. **信息行**：左 = 歌名（大）+ 歌手（灰）；右 = `♡` 收藏 + `⋮` 更多
5. **进度条**：细线 + 圆点手柄
6. **时间行**：左 = 已播时长；**中 = 转码状态 · 格式 · 码率**（如 `转码 · OPUS · 192 kbps`）；右 = 总时长
7. **控制行（5 按钮）**：`⟳` 循环 / `⏮` 上一首 / `▶⏸` 播放暂停（最大）/ `⏭` 下一首 / `☰` 队列
8. 全白文字（因背景是封面）；**无底栏、无播放条**

> 注意第 6 项——**把「转码 · 格式 · 码率」放在播放页时间行中间**，是 fnos 的独特做法：把音频技术状态放在最高频的界面上。**用户 2026-09-23 明确称赞这个位置**（「播放界面的展示转码信息位置非常好」）。Luobo 已有 `_QualityInfo`（在 `_SongOptionsSheet` 内），应上移到播放页时间行。
>
> **「更多」收纳 + 保留车载入口（用户 2026-09-23 明确要求）**：fnos 把播放页的多数功能收进 `⋮`，**用户认可这种收纳**，但**要求保留车载入口为可见按钮**。Luobo 现状：`now_playing_screen.dart:2695` 的 `_SongInfo` 里有 `Icons.directions_car` 可见按钮（`onCarPressed`），另有「播放页停留 30s 自动进车载」（`:93-239`）。**改造时车载入口不得埋进 `⋮`。**
>
> **「添加到歌单」选择器**（`添加到歌单-选择器.jpg`）：底部面板 = **新建歌单**（玫红圆图标）+ 已有歌单列表（封面缩略图 + 「N 首」）。

### 2.13 头图式详情页与空态（2026-09-23 补充）

**头图式详情页**（专辑 / 歌单 / 歌手，同一族）：

| 元素 | 专辑详情页 | 歌单详情页 |
|---|---|---|
| **背景** | **取自专辑封面色**（如暗橄榄） | 深灰（取色结果很淡 / 未取色，待确认） |
| 顶栏 | 左圆返回 + 右上 视图/排序 图标 | 左圆返回 + 右上 `···` |
| 头图 | 大封面（向下淡出） | 大方形封面 |
| 标题 | 居中大字 + 「歌手 · 年份」 | 居中大字 + 「N 首」 |
| 双胶囊 | `▶ 播放` + `⤨ 随机`（**浅背景=黑胶囊；暗背景=白胶囊，自适应对比色**） | 同 |
| 列表头 | 「1. CD」/「共 N 首」+ 视图切换 + 排序 | 「共 N 首」+ 视图切换 + 排序 |
| **歌曲行** | **序号 + 歌名 + `⋮`（无封面** —— 整张专辑同封面，不必重复） | **封面 + 歌名 + 歌手 + `⋮`** |

> **两种歌曲行形态是最容易实现错的点之一**，见 `~/Downloads/fnosmusic/_索引.md` 末尾「三处最容易实现错的点」。

**空态范式**（`歌单详情-空态-深色.jpg`）：

- **灰色插画**（居中，约 120×120，低饱和灰）
- 下方**一句口语化文案**，灰字、字号偏小、**带波浪号**（「空空如也~」）
- 不用大图标 + 长说明文字（那太重）

> Luobo 现状：`favorites_screen.dart:82-88` 等空态是 `Icon(size: 64, color: grey)` + 一句说明，**缺插画**；文案偏正式。C7 统一。

### 2.14 新增的弹层范式（2026-09-23 补充）

| 范式 | 特征 | 来源 |
|---|---|---|
| **确认弹窗** | 底部升起；**居中标题 + 居中说明文字 + 左右双胶囊**（取消＝灰底 / 危险＝玫红实心） | `确认弹窗-从歌单移除-深色.jpg` |
| **选择器面板** | 底部面板；**新建项**（玫红圆图标 + 文字）+ 已有项列表（封面缩略图 + 名称 + 「N 首」） | `选择器-添加到歌单-浅色.jpg` |

### 2.12 折叠头：分段控件迁入顶栏

**用户 2026-09-23 特别指出这个交互**（歌手详情页）。以歌手详情页为例：

| 滚动状态 | 顶栏 | 分段控件位置 |
|---|---|---|
| **未上滑** | 左上**独立圆返回** | 在**内容区**：歌手名 + 统计下方，与 `▶ 播放全部` 黑胶囊同一行 |
| **上滑后** | 返回圆钮变成 **`← 歌手名` 胶囊** | **迁入顶栏右侧**，与返回胶囊同一行；`▶ 播放全部` 消失 |

**本质**：**同一个分段控件，随滚动「迁移」到顶栏**（横向位置也变了，不只是纵向跟随）。

**实现要点**：单一 widget 无法跨 parent 平滑移动，标准做法是**两处渲染 + 按滚动进度交叉过渡**（或带位移的动画），由同一个 `ScrollController` 驱动；顶栏侧用 `SliverAppBar` / `SliverPersistentHeader` 的 pinned 态承载。

**Luobo 现状差异**：`artist_screen.dart:264` 用 `SliverAppBar(pinned, expandedHeight: 200)` + `FlexibleSpaceBar`，内容是**纵向堆叠**（热门歌曲区块 + 专辑区块），**没有分段控件**，也没有折叠迁移。→ 属**页面级重构**（见 §3.2 #24）。

### 2.11 彩色渐变流体动态背景

> **已拍板理解（2026-09-23 用户指出）**：fnos 里「很多渐变图」用的不是静态图，而是**彩色渐变流体动态背景**——即 Apple Music 那套「**专辑封面色彩提取 + 不断流动**」的背景。

**设计语言要点**（仅记与本方案相关的部分）

- **取色**：从封面提取调色板（vibrant 优先，回落 dominant）
- **连续场**：不是「几个圆在动」，而是整幅被噪声场平流的**连续场**（用户原话「像流沙似的」）
- **持续流动**：静止时也在缓慢流动，**不是**只在换封面时过渡
- **可读性遮罩**：上覆线性暗渐变，保证白字可读
- **降级**：取色失败 / 无封面 → 回落默认深色 mesh

**应用面**（本方案涉及）：全屏播放页背景、头图式列表页头图（收藏 / 最近播放 / 歌单）、首页渐变卡。

> 📄 **实现细节不在此文档**：技术方案、公共 API、着色器原理、三条硬约束、调参旋钮、测试方法、移植清单，见独立文档 **`docs/流沙流体背景技术文档.md`**；首页落地背景见 `docs/首页重构技术方案.md` §5.6-§5.8。**本方案只消费该模块，不复述其实现。**
>
> ⚠️ **早先更正留档**：本方案曾把 Luobo 播放页背景描述为「渐变 blob，不如 fnos 的封面放大模糊」——**这是错的**。Luobo 的实现与 fnos 同源（Apple Music 式取色流体），且已抽为公共模块。

---

## 3. Luobo 现状对照

### 3.1 已对齐（可复用，勿推翻）

| 能力 | 位置 | 与 fnos 差异 |
|---|---|---|
| 浅灰底 `#F2F2F7` | `lib/theme/app_theme.dart:11` | 几乎一致 ✅ |
| 悬浮胶囊底栏（blur 24 + 白 0.65 + 阴影 + radius 30） | `lib/screens/main_screen.dart:564 _buildGlassBottomNav` | 配方一致；**选中态是图标变色**（fnos 是实心圆块）⚠️ |
| 悬浮胶囊播放条（blur 12 + 白 0.62 + 阴影 + radius 22） | `lib/widgets/mini_player.dart:76` | 配方一致；**占位式而非覆盖式** ❌ |
| 玻璃开关 | `lib/services/theme_service.dart:81,113`（`liquidGlass`，**默认 false**） | fnos 是默认态 ⚠️ |
| 设置页分组白卡 | `lib/screens/settings_root_screen.dart:262 _section` / `:342 _navTile` | 已是 fnos 范式 ✅ |
| 区块标题 + 横向滚动区 | `lib/widgets/section_header.dart` | fnos 用 chevron，Luobo 用「查看全部」文字 ⚠️ |
| 歌曲行 `⋮` 菜单 | `lib/widgets/song_tile.dart:430` / `:445 _showOptions` | 有；但弹层范式不同（§3.2 #8） |
| 排序能力 | `lib/screens/all_songs_screen.dart:88 _sortSongs`（7 种选项） | 能力在；**UI 是底部弹窗**（`:150-215`），非锚定浮层 ⚠️ |
| 网格视图 | `favorites_screen.dart:248`、`albums_screen.dart:124`、`liked_albums_screen.dart:87`、`genre_screen.dart:242` | 页面级网格有；**无同页视图切换** ⚠️ |
| 强调色可选 7 色（`#FA243C` 红 / `#FC5C65` 粉） | `lib/services/theme_service.dart:4 AccentColor` | 缺 fnos 那支玫红 `#F0305C` ⚠️ |
| **全屏播放页骨架** | `now_playing_screen.dart`：`_PlayerHeader`（`⌄` 下箭头 `:1549`）、`_AlbumArtworkSection`（`:2099`）、`_PlayerControls`（`:2472`）、`_SongInfo`（`♡` `:2713`） | 骨架齐 ✅；另有 `stop` 按钮（`:1217`）fnos 无 ⚠️ |
| **彩色渐变流体动态背景**（§2.11） | ✅ **已是公共模块**：`lib/widgets/fluid_background/`（`FluidBackground` / `FluidBackgroundStyle` / `FluidBackgroundScope` / `FluidClock`）+ `lib/services/cover_palette_service.dart` + `shaders/fluid_mesh.frag`；已接入首页 `home_v2_screen.dart` / `mix_grid_section.dart` | ✅ **已完成，可直接复用**。细节见 `docs/流沙流体背景技术文档.md`；**播放页 `_DynamicBackground` 仍是旧私有实现，未切过来**（两套并存） |
| **歌曲 `⋮` 面板已有上下文头** | `song_tile.dart:490-536`：拖拽把手 + 封面(60) + 歌名 + 歌手 | 与 fnos 上下文头**一致** ✅；另有 `_QualityInfo`（fnos 无） |

> 结论：**视觉基底已具备约 60~70%**，底栏与播放条配方正确。**缺口集中在「页面与浮层范式」**，而非配色。

### 3.2 缺口清单

**A. 页面级**

| # | 缺口 | 现状证据 |
|---|---|---|
| 1 | **歌曲信息展示页** | 全仓无；`Song` 模型无 `discNumber`/`channelCount` |
| 2 | **头图式列表页**（渐变头图 + 双黑胶囊按钮） | `favorites_screen.dart:58-84` 是标准 `AppBar` + 内嵌 `_TabButton` 行；`history_screen.dart:109` 同 |
| 3 | **搜索历史 chips** | `search_screen.dart` 无 history/chip 相关实现 |
| 4 | **多选模式** | 全仓无 `multiSelect` / `selectionMode` / `_selectedSongs` |
| 24 | **歌手详情页折叠头 + 分段控件**（§2.12） | `artist_screen.dart:264` 是 `SliverAppBar(pinned, expandedHeight:200)` + `FlexibleSpaceBar`，内容**纵向堆叠**（热门歌曲 + 专辑），**无分段控件、无折叠迁移**；另缺 `▶ 播放全部` + 统计行 |

**B. 组件级**

| # | 缺口 | 现状证据 |
|---|---|---|
| 5 | **悬浮圆返回 / 实心圆动作钮** | 全仓无该组件；`settings_sub_page.dart:25` 为 `centerTitle: false` 标准 `AppBar` |
| 6 | **锚定毛玻璃排序菜单** | 现为底部弹窗 + `ListTile`（`all_songs_screen.dart:187-214`） |
| 7 | **歌曲 `⋮` 面板缺「主操作横排」** | `_SongOptionsSheet`（`song_tile.dart:455+`）已有上下文头 ✅，但所有操作都在**一列 `_OptionTile`** 里（收藏/下一首/加入队列/添加到歌单/去专辑/去歌手/评分/下载），**缺 fnos 的「主操作横排 3 方形按钮」**；也**缺「歌曲信息」入口** |
| 8 | **模态表单 Sheet 范式** | 新建歌单现为 `AlertDialog`（`library_screen.dart:1233`） |
| 9 | **表单范式**（标签卡外 + 输入卡 / 只读 `label+value` 卡） | 无统一组件 |
| 10 | **横排技术参数卡**（码率/采样率/声道数） | 无 |
| 11 | **行下灰字说明** | 无 |
| 12 | **朴素列表**（全宽分隔线行） | 无统一组件，多为 `ListTile`/卡片 |
| 13 | **同页视图切换按钮**（列表 ↔ 网格） | 无 |
| 14 | **渐变头图**组件 | 无（听歌报告有统计卡，非同一组件） |
| 15 | **深色模式 token 未双色定义** | `app_theme.dart` 深色是「Spotify 式纯黑 `#000000`」，**与 fnos 的「近黑 `#121212` + 深灰卡 `#1C1C1E`」不同**；且无 token 层，无法一处切换 |
| 16 | ~~流体渐变实现不可复用 + 取色散落~~ | ✅ **已解决（2026-09-23）**：抽出 `lib/widgets/fluid_background/` + `lib/services/cover_palette_service.dart`。**遗留**：`now_playing_screen.dart` 的 `_DynamicBackground` 尚未切到新组件，两套实现并存 |
| 17 | ~~流体渐变缺「持续流动」~~ | ✅ **已解决（2026-09-23）**：`fluid_mesh.frag` + `FluidClock`（`driftPeriodSeconds = 16`，`driftAmplitudeX/Y`、`driftRadiusBreath`）实现 idle 持续漂移 |
| 18 | **播放页时间行缺技术状态** | fnos 在时间行中间显示「转码 · OPUS · 192 kbps」；Luobo 的 `_QualityInfo` 藏在 `⋮` 面板内 |

**C. 架构级**

| # | 缺口 | 现状证据 | 影响 |
|---|---|---|---|
| 19 | **播放条覆盖式布局** | `main_screen.dart:532-540` 中 `MiniPlayer` 位于 `Column` 内**占位** | 需重排所有页面底部留白；是「液态玻璃」观感关键 |
| 20 | **圆角无 token 层** | 全仓 15 种圆角值（`circular(8)`×80、`circular(12)`×71、4/10/16/20…） | 视觉不统一 |
| 21 | **底栏选中态** | `main_screen.dart:659-680` 图标+文字变主色 | 与 fnos 实心圆块不一致 |
| 22 | **强调色缺玫红** | `theme_service.dart:4` 7 色无 `#F0305C` | 观感偏 Coral 而非 Rose |
| 23 | **排序「决策未降维」** | `all_songs_screen.dart:164-183` 把 `字段 × 方向` 烤进标签后**平铺成 7 个选项**（`sortTitleAz`/`sortTitleZa`/`sortArtistAz`/`sortArtistZa`/`sortAlbumAz`/`sortAlbumZa`/`recentlyAdded`） | 违反 §2.8 原则 4；fnos 是「2 个字段 + 方向箭头」。这是 Luobo 交互上**明显劣于 fnos** 的一处 |

---

## 4. 改造方案

### 4.1 视觉层：先立 token，再收口

**新增 `lib/theme/design_tokens.dart`**（新建文件，不改 `app_theme.dart` 既有值）

```
LuoboRadius:  card=16, group=20, cover=16, pill=30, button=full
LuoboGlass:   tintLight=white .65 / tintDark=black .50
              blurNav=24, blurBar=12, blurMenu=20, blurButton=16
              borderLight=white .80 / borderDark=white .10
              shadow=black .14 / blur 28 / offset (0,6)
LuoboGradient: 程序化头图渐变（seed → 柔和双色）
```

**新增公共组件**（`lib/widgets/`）

| 组件 | 用途 | 对应 fnos |
|---|---|---|
| `GlassCircleButton` | 悬浮圆形按钮（返回 / 搜索 / `···` / 实心强调色动作钮） | §2.2 各骨架的圆钮 |
| `GlassPillNav` | 悬浮胶囊底栏（从 `_buildGlassBottomNav` 抽出，选中态改实心圆） | §2.6 底栏 |
| `GlassAnchoredMenu` | **锚定毛玻璃菜单**（玫红勾 + 方向箭头） | §2.4-1 排序 |
| `ActionCapsuleSheet` | **底部胶囊叠面板**（普通 / 危险 / 取消） | §2.4-2 歌单设置 |
| `MultiSelectSheet` | 多选 sheet（拖拽把手 + 选择框 + 4 等分操作栏） | §2.4-3 |
| `ModalFormSheet` | 模态表单 sheet 壳（`×` / 标题 / 实心 ✓） | §2.4-4 新建歌单 |
| `SettingsGroupCard` | 分组白卡（组内缩进分隔线） | §2.3-1 |
| `PlainListTile` | 朴素列表行（全宽分隔线） | §2.3-2 |
| `SettingsTile` | 统一行组件（图标 + 文字 + 右侧状态值/chevron/开关） | §2.3 行结构 |
| `FormFieldCard` | 标签卡外 + 输入白卡 | §2.5 |
| `ReadOnlyInfoCard` | `label + value` 竖排只读卡 | §2.5 |
| `SpecStripCard` | 横排技术参数卡（竖线分隔） | §2.5 |
| `TileHintText` | 行下灰字说明 | §2.5 |
| `GradientHeroHeader` | 渐变头图 + 标题 + 双胶囊按钮 | §2.2 头图式列表页 |
| `ViewToggleButton` | 列表 ↔ 网格视图切换 | §2.3 |
| `FluidBackground`（复用） | 头图式列表页头图 / 其他动态背景场景 | §2.11 + `docs/流沙流体背景技术文档.md` §13 移植清单 |

> **流体背景模块已完成**（`lib/widgets/fluid_background/` + `cover_palette_service.dart`），本方案只需**消费**它（头图式列表页头图等），不再自行实现。**遗留**：`now_playing_screen.dart` 的 `_DynamicBackground` 仍是旧私有实现，是否切到新模块另议（切换须保证播放页外观零变化）。
>
> ⚠️ 抽取时必须保持**现有播放页外观零变化**（纯重构，C1 期验收项）。

> `SettingsTile` 即 `docs/设置页重构技术方案.md` 中遗留的 **P3 视觉统一**——本方案与其合并实施，避免两轮返工。

### 4.2 交互层：骨架对齐

**① 二级页骨架**：改造 `lib/widgets/settings_sub_page.dart`
- 标题 `centerTitle: false` → **居中**
- `AppBar` leading → **`GlassCircleButton` 悬浮圆返回**
- 新增 `actions` 槽位承载右上实心圆动作钮

**② 大标题页**：右上角入口（搜索 / 设置）由标准 `IconButton` 换 `GlassCircleButton`

**③ 首页区块标题**：`SectionHeader` 的「查看全部」文字按钮 → **chevron 箭头**（`onSeeAllTap` 语义不变）

**④ 播放条覆盖式布局**（缺口 15，架构级）
- 一级页：`MiniPlayer` + `GlassPillNav` 从 `Column` 移出，改为 `Stack` 内 `Positioned` 悬浮
- 二级页：新增悬浮播放条（fnos 二级页有播放条、无底栏）
- 各页 `bottom` 留白改为「播放条高度 + 底栏高度」常量，避免内容被永久遮挡

**⑤ 列表页统一**（缺口 2/13）
- 抽 `PlainListTile` + `ViewToggleButton`，按 §2.3 重构 专辑 / 歌手 / 音乐风格 / 全部歌曲
- `favorites_screen` / `history_screen` 换 `GradientHeroHeader`（保留内嵌 Tab 语义 → 改为双胶囊）

**⑥ 排序菜单统一**（缺口 6 / 23）
- 排序能力已存在（`all_songs_screen.dart:88`），但 UI 需两处改：底部弹窗 → `GlassAnchoredMenu`；**7 个平铺选项 → `字段 × 方向` 降维**（§2.8 原则 4）
- 补齐 专辑 / 歌手 / 风格 三页的排序（fnos 有，Luobo 目前仅全部歌曲有）

**⑦ 浮层范式统一**（缺口 7 / 8）
- **歌曲行 `⋮`：结构升级**（非替换）——保留现有 `_SongOptionsSheet` 的全部操作项（收藏/下一首/加入队列/添加到歌单/去专辑/去歌手/评分/下载/`_QualityInfo`），**新增 fnos 的三段式结构**：①上下文头（已有 ✅）②**主操作横排 3 方形按钮**（播放 / 下一首播放 / 加入播放列表）③次操作竖列 + **新增「歌曲信息」入口**（指向 §4.4 新页）
- 新建歌单：`AlertDialog` → `ModalFormSheet`（含渐变封面预览 + `↻`/`✏️` 圆钮）

**⑧ 设置页职责分离**（缺口源自前版）
- 根页 `settings_root_screen.dart` 保留：服务器 / 播放 / 存储 / 显示 / AI / 关于
- **移出**到右上角二级设置：**外观**（拆自 `settings_display_tab.dart`）、**清除缓存**、**App 日志上报**、**退出登录**

### 4.3 搜索页（缺口 3）

`search_screen.dart` 补：白圆角搜索框 + 「取消」+ 「搜索历史」+ 清空垃圾桶 + **历史 chips 流式换行**（本地持久化历史词）。

### 4.4 歌曲信息展示页（只展示）

**新增 `lib/screens/song_info_screen.dart`**，入口挂 `song_tile.dart:445 _showOptions` 的 `_SongOptionsSheet` 内新增「歌曲信息」项。

页面结构（对齐截图）：模态骨架（居中标题「歌曲信息」，**无右上动作钮**）→ 大封面 → `SpecStripCard`（码率/采样率/声道数）→ `ReadOnlyInfoCard`（文件位置/大小/添加日期）→ 只读字段区（名称 / 歌手（可跳 `artist_screen`）/ 专辑 / 年份 / 曲目序号 / 光盘序号 / 风格（可跳 `genre_screen`））。

**字段可行性核对**（`lib/models/song.dart`）

| 展示项 | 字段 | 状态 |
|---|---|---|
| 码率 / 采样率 / 大小 / 位置 / 添加日期 | `bitRate` / `samplingRate` / `size` / `path` / `created` | ✅ 已有 |
| 名称 / 歌手 / 专辑 / 年份 / 曲目 / 风格 | `title` / `artist` / `album` / `year` / `track` / `genre` | ✅ 已有 |
| **声道数** | — | ❌ **模型无 `channelCount`**；Subsonic 标准 `Child` 无此属性，需实测 → **拿不到则隐藏** |
| **光盘序号** | — | ❌ **模型无 `discNumber`**；Subsonic `Child` 有该属性（API 1.8+）→ 补解析即可 |

> 落地原则：**字段拿不到就不渲染该行**，不做占位符，避免破相。

### 4.5 多选模式（缺口 4）

按 §2.4-3 新增 `MultiSelectSheet`；入口挂歌曲列表页（fnos 从列表页视图切换图标旁进入）。4 项操作中「加入播放列表 / 下载 / 添加到歌单」Luobo 已有对应能力，仅需接线。

---

## 5. 性能约束与红线

**红线：不做「处处毛玻璃」。**

项目**曾特意移除大面积毛玻璃**，两处代码考古证据：

| 证据 | 内容 |
|---|---|
| `lib/widgets/glass_surface.dart:3-5` | 注释明说旧毛玻璃包装被换成实色：*"BackdropFilter-free solid bottom sheet. Kept as a drop-in replacement for the previous frosted-glass wrapper"* |
| `lib/widgets/mini_player.dart:93-95` | 注释明说 sigma 从 24 降到 12 是为省 GPU：*"Lower sigma keeps the glass look while costing far less GPU per frame"* |

**执行原则**

- `BackdropFilter` **只用于小面积固定元素**：底栏、播放条、悬浮圆钮、锚定菜单
- **禁止**用于：列表项、滚动中的大面积卡片、整页背景
- 所有玻璃元素必须包 `RepaintBoundary`（`mini_player.dart:77` 已有先例）
- **播放条改覆盖式后**（§4.2-④）模糊面积不变，但滚动内容会从其下方经过 → **必须实测**帧率
- 每期改造后需**低端安卓真机**验证滚动帧率（仅模拟器验证不算通过）

---

## 6. 开发周期划分

> **优先级来源（2026-09-23 用户明确）**：① **设置页改造**（长期不满意，现有 fnos 可参考）→ ② **歌曲 `⋮` 面板（唤起歌曲更多信息的卡片）**（对 fnos 的入口位置与结构很满意）→ ③ 才是美化类。
>
> **进度**：流体渐变背景已由用户在独立窗口完成并抽为公共模块（`docs/流沙流体背景技术文档.md`），**超出原规划**；本方案只需消费。

| 周期 | 内容 | 为什么排在这 | 风险 | 验收 |
|---|---|---|---|---|
| **C1｜地基** | **① Token 全表**：`design_tokens.dart`（双色 `light`/`dark`）+ **颜色 8 / 圆角 4 / 间距梯度 / 字阶 6 档 / 玻璃 / 阴影 / 动效参数**（原稿只画了颜色+圆角，2026-09-23 自检后补齐）<br>**② `GlassCircleButton`**（7 形态：返回 / 搜索 / 更多·圆钮横向 / 更多·行内纵向 / 保存·淡底 / 保存·实心 / 扫码）<br>**③ `GlassPillNav` 底栏**（3 项纯图标 `flex:1` 均匀分布 + 分隔线 + 专辑封面等宽槽；选中＝玫红实心圆）<br>**④ 行家族 7 变体**（单行+chevron / +状态值 / +开关 / +动作按钮 / 双行+副标题 / 单选组 / 危险行）<br>**⑤ 分组白卡 + 小节标题 + 行下灰字说明**<br>**⑥ 胶囊按钮 3 种**（普通灰 / 主色实心 / 危险淡红）<br>**⑦ Sheet 壳**（顶部圆角 20 + 把手 + 遮罩 .42，5 种浮层共用）<br>**⑧ 空态**（灰插画 + 口语化文案）<br>**⑨ `AppIcons` 映射层**（含 `moreH`/`moreV`）+ **每期可勾选验收清单** | **凡是跨 2 个以上周期复用的，都在 C1 定** —— 否则 C2 起每期都要临时定义组件。改一处（`settings_sub_page.dart`），**所有二级页同时受益** | 低 | analyze 干净；**二级页 / 底栏 / 行外观回归零变化**（C1 是纯重构 + 新增组件，不改业务） || **C2｜设置页改造** ⭐用户第 1 优先 | ① **职责分离**：根页留「服务器 / 播放 / 存储 / 显示 / AI / 关于」，把「**外观 / 清除缓存 / App 日志上报 / 退出登录**」迁到右上角二级设置；② `SettingsGroupCard` + `SettingsTile` 统一行组件（**合并 `设置页重构技术方案.md` 遗留的 P3 视觉统一**）；③ `TileHintText` 行下灰字说明；④ 表单范式（标签卡外 + 输入白卡）；⑤ **服务器管理流程重排**（对标箭头音乐：列表 + `⋮` 操作面板 + 服务器详情页 + 音乐库范围并入） | 用户长期痛点；与 fnos 设置页范式（§2.2/§2.3）直接对齐。**详细设计与逐项迁移映射见 `docs/设置页重构技术方案.md` §9** | 中 | 设置全路径回归 + 真机走查（按既有习惯顺带遍历同类隐患） |
| **C3｜歌曲 `⋮` 面板 + 歌曲信息页** ⭐用户第 2 优先 | ① 面板**三段式升级**：上下文头（已有 ✅）+ **主操作横排 3 方形按钮**（播放 / 下一首播放 / 加入播放列表）+ 次操作竖列；**按来源自适应**（列表页含主操作与收藏，播放页不含，§2.4）；② 新增「**歌曲信息**」入口；③ 新建 `song_info_screen.dart`（**只展示**）：模态骨架 + `SpecStripCard`（码率/采样率/声道数）+ `ReadOnlyInfoCard`（位置/大小/添加日期）+ 只读字段区；④ 补 `Song.discNumber` 解析；声道数按实测决定隐藏或换 `bitDepth`；⑤ **播放页**：`_QualityInfo` 上移到时间行（§2.10）；功能收进 `⋮` 但**保留车载入口为可见按钮**；「添加到歌单」改选择器面板 | 用户明确满意 fnos 的入口位置与结构；「歌曲信息」是唯一新增页面 | 中 | Navidrome / 道理鱼**双端**字段渲染与缺失隐藏；播放页真机走查（车载入口仍在） |
| **C4｜播放条覆盖式**（独立、高风险） | 一级页 `MiniPlayer` + `GlassPillNav` 从 `Column` 移入 `Stack`/`Positioned` 悬浮；二级页新增悬浮播放条；各页底部留白改为常量 | **它会动所有页面底部布局**——放在大规模美化（C5/C6）**之前**：①通过则后续页面只改一次；②失败则回退，**不影响已完成的 C2/C3** | **高** | 低端安卓滚动帧率实测；**不通过即回退占位式** |
| **C5｜列表页族**（美化） | 专辑 / 歌手 / 音乐风格 / 全部歌曲：列表页骨架 + 朴素列表（全宽分隔线）+ **锚定毛玻璃排序菜单**（字段 × 方向，**修正 7 项平铺**）+ 同页视图切换；**＋ 歌手详情页折叠头重构**（§2.12：分段控件 + 滚动迁入顶栏，`artist_screen.dart` 由纵向堆叠改分段切换） | 交互劣化最明显的一族（§3.2 #23）；行组件与 C2 复用 | 中 | 真机走查 4 页排序与视图切换 + 歌手详情页滚动迁移 |
| **C6｜头图式族（流体复用）** | 收藏 / 最近播放 / 歌单详情：`FluidBackground` 头图 + 居中标题 + 双黑胶囊（▶ 播放 / ⤨ 随机）；顺带清 `CoverCollage` 遗留 | **流体组件已就绪**，复用成本低；同时验证流体在播放页以外的场景 | 中 | 真机走查 + 帧率（多实例流体） |
| **C7｜其余美化与收尾** | 大标题页圆钮（首页/音乐库/设置）；底栏选中态改**实心圆**；新增玫红强调色 `#F0305C`；`SectionHeader` 换 chevron；`ModalFormSheet` 新建歌单；`ActionCapsuleSheet` 歌单设置；搜索历史 chips；多选 sheet；播放页时间行加「转码 · 格式 · 码率」；**深色模式逐页走查**（结构不变只校色）；存量圆角收口 | 纯美化，无结构性依赖，可拆可并 | 低 | 逐屏走查 |

**每期收尾**：`flutter analyze` 零 error + 全量单测绿 + 真机走查（按既有习惯，**顺带遍历同类隐患一并修**）。

**周期间的依赖关系**

- C1 → C2 / C3（都要圆钮与二级页骨架）
- C2 的行组件 → C5 复用
- C4 必须在 C5 / C6 **之前**（否则底部留白要改两遍）
- C6 依赖已完成的 `FluidBackground` / `CoverPaletteService` ✅（已具备）
- C7 无依赖，可在任一周期的空档插入

---

## 7. 待拍板项

| # | 决策点 | 我的建议 |
|---|---|---|
| ① | **改造范围与起点**：按 §6 的 C1→C7 一路做，还是先做 C1-C3 看效果？ | **建议先做 C1-C3**（地基 + 设置页 + 歌曲 `⋮` 面板）——正好是你的两个优先项，一轮做完就能验收最关心的两块。**C4（播放条覆盖式）单独排期**，且必须在 C5/C6 之前 |
| ② | **播放条覆盖式改造**（C4）是否本轮做？这是「液态玻璃」观感的关键，但会动到所有页面底部布局，且需真机帧率实测 | ✅ **已拍板（2026-09-23）：做，但独立成期**（C4），做完立刻真机验证，**不通过就回退占位式**。位置安排在 C5/C6 之前 |
| ③ | **玻璃开关语义**：保持「开/关」，还是改「玻璃强度档」（关/轻/强）？ | 改**强度档**——既留性能退路，又能表达 fnos 那种更强的玻璃感 |
| ④ | **强调色**：新增玫红 `#F0305C` 后是否设为默认？ | **仅新增，默认不动**。换默认色影响所有存量用户观感，属独立决策 |
| ⑤ | **`liquidGlass` 是否改默认开启**？ | **暂不改**。先过低端机帧率，再谈默认开启 |
| ⑥ | **歌曲行 `⋮` 菜单**：换成 fnos 的「底部胶囊叠面板」，还是保留现有底部弹窗列表？ | ✅ **已拍板（2026-09-23）：不做胶囊叠**。fnos 胶囊叠是「歌单级」操作；歌曲级操作项多，胶囊叠会过长。**仅歌单设置**用 `ActionCapsuleSheet`<br>⚠️ **2026-09-23 补充截图后修正**：fnos 的歌曲 `⋮` **也不是简单弹窗列表**，而是「上下文头 + 主操作横排 3 方形按钮 + 次操作竖列」三段式。**结论不变（不做胶囊叠），但要做结构升级**（见 §2.4 范式 5、§4.2 ⑦） |
| ⑦ | **声道数**拿不到时：隐藏该格，还是用 `bitDepth`（位深）替换？ | **用位深替换**——同为音频技术参数，信息密度不降 |
| ⑧ | **深色模式是否本轮做？** fnos 深色已实测（§2.9），且**只换色不换结构** | **本轮只保证 token 双色定义**（C1 的 `design_tokens.dart` 从第一天就写 `light`/`dark` 两套），**深色模式的逐页走查并入 C7**。理由：结构不变意味着后补成本低，但一页页调色会拖慢主线 |
| ⑨ | ~~播放页背景改成封面模糊？~~ **→ 作废（理解有误）** | ✅ **作废**：Luobo 播放页已是 Apple Music 式取色流体渐变，做法正确。流体模块已抽为公共件（`docs/流沙流体背景技术文档.md`） |
| ⑩ | **播放页时间行**是否加「转码 · 格式 · 码率」？ | **加**。这是 fnos 的亮点（把技术状态放在最高频界面），且 Luobo 数据已有（`_QualityInfo`） |
| ⑪ | **大标题页的标题**是固定 AppBar，还是随内容滚动的页内标题？ | 深色设置页截图里标题被卡片**部分覆盖**，无法判断。**需你确认或再补一张滚动中的截图** |
| ⑫ | **桌面端是否一起改？** | ✅ **已拍板（2026-09-23）：不一起改**。本方案**只覆盖移动端**；桌面端（`desktop_navigation_sidebar` / `desktop_player_bar` / `_isDesktop` 分支）保持现状。→ 所有公共组件需保证**不破坏桌面端现有布局** |
| ⑬ | **自定义主题功能**（`theme_editor_screen` / `theme_manager_screen` / `now_playing_theme_service`） | ✅ **已拍板（2026-09-23）：删掉，做减法**。**保留「强调色」**（`AccentColor` 7 色 → 加玫红成 8 色）作为唯一的用户可调外观项；**删除**自定义背景类型（`solid` / `gradient` / `dynamic`）、自定义播放页主题、自定义配色。→ 连带**简化 `_DynamicBackground` 里的 `ThemeAwareBuilder` 分支** |
| ⑭ | **`liquidGlass` 用户开关** | ✅ **已拍板（2026-09-23）：删掉用户可自定义的点，强制按最新样式展示**。存量 `app_liquid_glass` 键废弃；玻璃样式由设计体系决定，不再暴露给用户 |
| ⑮ | **图标体系** | ✅ **已拍板（2026-09-23）：引入 Lucide（`lucide_icons`）+ `AppIcons` 映射层**。理由：Lucide 是线性图标、统一 2px 线宽、24px 网格，与 fnos 的线性路线吻合；映射层让代码统一写 `AppIcons.back`，将来换图标集只改一个文件。→ **C1 交付 `AppIcons` + 「现有图标 → Lucide」映射表** |
| ⑯ | **底栏去掉文字标签** | ✅ **已拍板（2026-09-23）：接受**（fnos 亦然），牺牲少量可发现性换整齐 |
| ⑰ | **底栏第 3 项 = 设置** | ✅ **已拍板（2026-09-23）：设置与音乐库平级**；三个 tab = 首页 / 音乐库 / 设置；搜索只在首页 + 音乐库的右上角圆钮 |
| ⑱ | **彩蛋页** | ✅ **已拍板（2026-09-23）：先不要了** |
