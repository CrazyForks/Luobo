# Luobo 性能监控与日志系统技术文档

> 状态：部分实施（2026-08-03）
> 范围：统一日志体系、全局异常捕获、性能指标采集、场景化埋点（播放中断无声 / 全屏车载页卡顿 / 网络切换转码）
> 设计约束：**纯本地、不上报**（隐私优先），数据只存设备；对外唯一出口为手动导出日志文件。开发基于本设计文档进行，实施进度回写本表。
> 实施进度：✅ P0/P1/P2 完成；P3 补充场景 1-4/6/7 + 远程链路最小埋点完成，debugPrint 长尾替换 ⬜（zone print 钩子已兜底 debug 模式全量采集）。详见 §8。
> 缺陷修复轮（2026-08-05）：导出日志分析发现双写损坏等 5 个问题，已全部修复：写锁 + 尾部对账（`file_store_io.dart:183-267`、`diagnostics_service.dart:74-104`）、appVersion 写入（`main.dart:34,142`）、as-init 补录改写（`diagnostics_service.dart:117-122,185-194`）、print 节流（`global_error_handler.dart:29,111`）、jank 判定改 build+raster（`metrics_collector.dart:135`）。详见 §9。

## 1. 背景与目标

当前痛点：

- 全项目约 465 处散落 `debugPrint`，靠手写前缀（`[Auth]`、`[Musly]`、`UPnP:`）区分；无级别、无统一格式、release 下零输出，线上排查只能靠猜；
- 无任何全局异常捕获（无 `runZonedGuarded` / `FlutterError.onError`），未捕获异常直接走 Flutter 默认行为；Countly `recordError()`（`analytics_service.dart:248`）定义后从未被调用；
- 已复现但无法定位的问题：电话/蓝牙中断后"点继续播放无声"、全屏页/车载页切换间歇卡顿、WiFi/移动网切换时的转码问题；
- 性能数据是"点状"的（Dio 拦截器计时、缓存用量），无汇聚、无帧率/启动/播放质量指标。

目标：

- 建立**统一本地日志体系**（全模式生效，含滚动文件），替换散落 debugPrint；
- 三路**全局异常捕获**，错误带堆栈落入日志；
- **性能指标采集**（帧率/启动/网络/内存），成本可控（平时无感、卡顿才触发深录）；
- **场景化埋点**以"会话时间线"形式还原问题现场：播放中断无声、页面卡顿、网络切换转码；
- 诊断页查看与导出，帮助排查。

## 2. 现状盘点

| 项 | 现状 | 位置 |
|---|---|---|
| 日志 | ~465 处 `debugPrint`，无统一格式 | 全项目 |
| 请求计时 | Dio 拦截器 Stopwatch 计时 | `subsonic_service.dart:56-84` |
| 全局异常 | 无 | — |
| 崩溃上报 | Countly `enableCrashReporting`（仅配置且开启时） | `analytics_service.dart:69` |
| `recordError()` | 已定义未调用 | `analytics_service.dart:248-260` |
| 音频会话 | 电话打断/蓝牙断开仅 pause，无恢复逻辑；Android 无 `setActive(true)`；无路由切换处理 | `player_provider.dart:1349-1390` |
| 网络切换 | `TranscodingService` smart 模式监听 connectivity；`resolveActiveUrl()` 探测切换 base URL（✅ 2026-08-03 优化：上次会话在远端时冷启跳过探测、后台静默切换，见 §6.3） | `transcoding_service.dart:114-136`、`subsonic_service.dart:86-197`、`auth_provider.dart:40-52` |
| 页面重活 | 全屏页调色板提取（缓存上限 20 条）、双图加载、双常驻动画；车载页 initState 拉歌词+收藏 | `now_playing_screen.dart:1107-1168,2046-2068`、`car_mode_screen.dart:75-101` |
| 现有依赖 | path_provider / shared_preferences / sqflite / connectivity_plus / battery_plus 均已就绪 | `pubspec.yaml` |

## 3. 架构总览（本地优先，三层）

```
采集层 Collect          处理层 Aggregate             消费层 Consume
┌──────────────┐      ┌───────────────────┐      ┌──────────────────┐
│ Logger API   │      │ DiagnosticsService│      │ 诊断页（设置内）   │
│ 全局异常钩子  │ ───► │ 事件环形缓冲(500)  │ ───► │ 日志查看/过滤      │
│ Dio拦截器    │      │ 会话时间线聚合      │      │ 指标卡片(FPS等)    │
│ FrameTimings│      │ 60s 指标快照 JSON  │      │ 按 sessionId 回放  │
│ 场景埋点     │      │ 事件/快照文件      │      │ 导出日志文件        │
│ 派生检测器   │      │ (events/metrics)  │      │                    │
└──────────────┘      │ 轮转与保留(5×1MB)  │      └──────────────────┘
                     └───────────────────┘
```

设计原则：

- **本地优先**：所有事件只写本地（内存环形缓冲 + 滚动日志文件），不发起任何网络上报；
- **零损耗常态**：常开埋点成本每帧几 µs；深录（Timeline trace）只在检测到卡顿后触发 1~2s；
- **时间线可回放**：所有事件带 sessionId 与全局序号，一次问题 = 一条可读时间线。

## 4. 核心机制：事件与会话时间线

### 4.1 事件模型

```
DiagnosticEvent {
  ts:           DateTime（本地毫秒）
  seq:          int（全局递增序号，用于排序）
  sessionId:    String（主会话 ID：播放相关=PlaybackSession，网络相关=NetSession，其余=AppSession）
  appSessionId: String（必带，见 4.2）
  netSessionId: String?（网络相关事件携带）
  type:         String（如 audio.interruption / frame.jank）
  level:        debug|info|warn|error
  payload:      Map<String, dynamic>（结构化为 JSON 落盘）
}
```

示例（单行 JSON）：

```json
{"seq":1234,"ts":"2026-08-03T21:03:45.123","sessionId":"ps-8f3a","appSessionId":"as-01","type":"log.info","level":"info","payload":{"tag":"Player","file":"player_provider.dart","line":1934,"msg":"play()","trigger":"user","sessionActive":false}}
```

事件 `type` 命名采用 `domain.action`（如 `audio.interruption`、`net.switch`），开发时维护**常量表**（如 `lib/services/diagnostics/event_types.dart`），避免字符串拼写漂移。

### 4.2 会话 ID

- **PlaybackSession**：一次播放会话（开始播放 → 切歌/停止/App 退出），播放相关事件共享；
- **NetSession**：网络状态段（每次 `net.switch` 切段）；
- **AppSession**：一次 App 运行（启动 → 退出），全局日志按此归档。

### 4.3 存储与导出策略

**增量追加，跨冷启动保留，不清空。**

- 事件文件 `events.jsonl`：append 模式，冷启动不删旧内容；每次启动靠 `appSessionId` + 「启动里程碑」事件分段，时间线可跨启动回放；
- 轮转：单文件满 1MB 切新文件，最多 5 个，删最旧 → 始终保留"最近约 5MB"，覆盖崩溃前最后几次启动；
- 崩溃不丢数据：事件**实时异步落盘**（批量 flush，如 200ms 一次），进程被杀/崩溃时最后事件已在磁盘；
- 内存环形缓冲（500 条）仅为诊断页实时查看视图，**不影响落盘完整性**（落盘全量，不截断）；
- 指标快照 `metrics.jsonl`：60s 一条，增量追加，独立保留（如最近 24h）；
- `seq` 全局递增需**跨启动持久化**（启动时从文件尾或 SharedPreferences 恢复），保证时间线顺序不重号；
- **Web 平台降级**：无文件系统，事件仅存内存环形缓冲 + console，导出走浏览器下载（Blob/File API），`metrics.jsonl` 同；
- **isolate 边界**：事件仅在 UI isolate 产生与落盘；其他 isolate（如 BPM 分析）如需记录，通过 `SendPort` 转发到 UI isolate 统一落盘。

导出 = `diagnostics/` 目录打包：`events.jsonl`（全部事件+日志+异常堆栈）+ `metrics.jsonl` + `meta.json`（版本/设备/启动次数）+ 自动生成的可读文本 `export.txt`（时间线格式）。移动端走系统分享/存文件，桌面端存用户目录。

### 4.4 容量预算（1h/天 场景）

- 事件格式：紧凑 JSON 单行，平均 ~250B/条；
- 逐条落盘（日志 + 播放/导航/埋点事件）：~1400 条/h ≈ 350KB/h；
- 聚合摘要（帧率/网络耗时，60s 一条）：60 条/h ≈ 18KB/h；
- **合计 ≈ 0.37MB/h → 1h/天 ≈ 2.6~3MB/周**，5MB 轮转窗口覆盖约 1.5~2 周；
- **红线**：帧级/请求级数据只聚合、不逐条落盘（逐帧落盘 ≈54MB/h 不可接受）；release 默认 info 级，debug 级不落盘；轮转上限兜底。

### 4.5 派生检测器（后台自动生成"结论事件"）

| 检测器 | 判定规则 | 产出事件 |
|---|---|---|
| silentPlayback | play 后 1~2s 内 `processingState==playing` 但 position 未前进 | `audio.silentPlayback` |
| stall | buffering 中 position 停滞超阈值（如 3s） | `audio.stall` |
| frame.jank | 单帧 build+raster 耗时 >50ms | `frame.jank`（带路由/交互上下文） |
| cacheConflict | prune 删除了当前正在播放的流文件 | `cache.prune` + conflict 标记 |

## 5. 模块设计

### 5.1 LoggingService（`lib/services/diagnostics/logging_service.dart`，新增）✅

- 级别：debug / info / warn / error；
- 格式：`[时间][级别][Tag][file:line] message`；实际落盘为事件 payload（tag/msg/error），导出时渲染可读文本；
- 双 sink：
  - console：仅 debug 模式（替换 debugPrint）；
  - 事件流：**全模式生效**，日志作为 `log.*` 类型事件**产出并转发 DiagnosticsService**（见 5.4）统一落盘/轮转/导出；LoggingService 本身不直接写文件；
- 敏感信息：日志统一复用 `_sanitizeUrl` 打码逻辑（`subsonic_service.dart:43-54`），不落密码/token；
- 实现：自研静态 `Log` API（未引入 `logging` 包，零新依赖），`Log.d/i/w/e(tag, msg, {error, extra})`；
- 懒格式化：仅 info 及以上默认落盘；debug 级仅 debug 模式开启；
- 初始化时序：**全局静态实例，`main()` 首行即可调用**（`main.dart:131-135`）；未落盘前事件先入内存队列（`DiagnosticsService._pending`），初始化后统一 flush 落盘；
- 异常堆栈中的 URL 同样走 `_sanitizeUrl` 脱敏后落盘。

### 5.2 GlobalErrorHandler（`lib/services/diagnostics/global_error_handler.dart`，新增）✅

- 三路挂钩，全部落入日志（error 级，带堆栈）：
  - `runZonedGuarded` 包裹 `runApp`（`main.dart:277-279`，`GlobalErrorHandler.runGuarded`）；
  - `FlutterError.onError`；
  - `PlatformDispatcher.instance.onError`；
- 记录未捕获异常后**触发一次立即 flush**，避免进程即将终止时最后一条丢失；
- `ErrorWidget.builder` 显示友好错误页 + 错误摘要（不影响崩溃信息落盘）；
- zone 内 `print` 钩子拦截全部 debugPrint（debug 模式）写入 `log.print` 事件，兜底覆盖剩余散落日志；
- 本设计不含崩溃上报（纯本地）；Countly 现有崩溃上报维持原样，不动。

### 5.3 MetricsCollector（`lib/services/diagnostics/metrics_collector.dart`，新增）✅

| 指标 | 机制 | 成本 | 生效模式 |
|---|---|---|---|
| 帧摘要 | `SchedulerBinding.addTimingsCallback` → build/raster 耗时入**帧数据环形缓冲（最近 10s，供派生检测器消费）** | 每帧几 µs | 所有模式 |
| 长帧深录 | jank 触发 → `frame.jank` 事件（带路由上下文） | 仅触发时 | 所有模式 |
| 页面 build 计时 | 车载页首帧耗时（`car_mode_screen.dart:57-62`）→ `screen.buildTime` | 一次性 | 所有模式 |
| 启动里程碑 | `main()` 各阶段打点（`main.dart:133/191/236/278`）：引擎初始化 → 服务创建 → 音频引擎 → 首帧 | 一次性 | 所有模式 |
| 网络 | Subsonic（`subsonic_service.dart:56-110`）+ Jellyfin（`jellyfin_service.dart:39-78`）Dio 拦截器：耗时/状态码/失败率/超时重试 → `net.request` | 已有 | 所有模式 |
| 内存/缓存 | 原生端 `ProcessInfo.currentRss`（`platform_io.dart`，Web 跳过）+ 图片缓存张数/MB，60s 采样 | 低频采样 | 所有模式 |

### 5.4 DiagnosticsService（`lib/services/diagnostics/diagnostics_service.dart`，新增）✅

- 事件环形缓冲（最近 500 条，实时查看视图）+ 60s 指标快照 JSON；
- **唯一落盘者**：负责事件/快照的落盘（`file_store_io.dart`：1MB×5 轮转、`seq` 持久化、导出）、`seq` 分配；LoggingService 与各埋点只产出事件；
- 会话时间线聚合：按 sessionId 将事件串成时间线，供诊断页回放；
- 崩溃立即 flush；Web 降级（`file_store_stub.dart` 空实现，导出走剪贴板）；
- 导出：`diagnostics/export_<ts>/` 目录（events*.jsonl + metrics.jsonl + export.txt + meta.json）；Web 走浏览器剪贴板；
- 初始化于 `main.dart:131`，全局单例。

### 5.5 诊断页（`lib/services/diagnostics/diagnostics_page.dart`，新增；入口 `settings_screen.dart:99-117`）✅

- 日志查看：级别过滤、搜索、按事件时间线展示；
- 指标卡片：FPS、jank 率、请求数、网络 p90、错误率；
- 导出到文件 / 复制到剪贴板 / 清空（清空前二次确认，仅建议导出后清空）。

## 6. 场景埋点清单

> 约定：所有事件自动携带当前 `sessionId`（播放/网络/App 主会话，见 4.2）、必带 `appSessionId`，网络相关事件另带 `netSessionId`。挂钩点均为现有代码位置。

### 6.1 场景 A：电话/蓝牙中断后"点继续播放无声" ✅

根因假设（探索结论）：`_configureAudioSession()`（`player_provider.dart:1359-1400`）在打断/断开时仅 pause；再次 `play()`（`:1967`）只走 `_ensureAudioFocus()`，**Android 无 `session.setActive(true)`**，且全项目无路由切换处理 → 状态 playing 但音频未激活/未路由。

| 事件 | 挂钩点 | 关键字段 |
|---|---|---|
| `audio.interruption` | `player_provider.dart:1367` | begin/end、type(duck/pause/unknown) |
| `audio.noisy`（设备断开） | `player_provider.dart:1393` | device=becameNoisy |
| `audio.routeChanged` | `player_provider.dart:1409`（`_snapshotAudioRoute`：getDevices 快照对比） | from→to 设备 |
| `audio.playerAction` | `play():1967` / `pause():2002` / `togglePlayPause():2144` | action、sessionActive、route |
| `audio.sessionState` 快照 | play() 入口（player.action payload） | sessionActive、route |
| `audio.silentPlayback`（派生） | `player_provider.dart:1995`（`_checkSilentPlayback`：play 后 1.5s 位置未前进） | posBefore/posAfter、sessionActive、songId |

### 6.2 场景 B：全屏页/车载页切换间歇性卡顿 ✅

重活点：进全屏页调色板提取（`now_playing_screen.dart:1107-1168`，首次需下载图片，`_paletteCache` 上限 20 条 `:1097`）；封面双图加载（`:2046-2068`）；旋转/脉冲双常驻动画（`:1944-1966`）；车载页 initState 拉歌词+收藏（`car_mode_screen.dart:75-101`）；进页动画（`main_screen.dart:310-347` 上滑 400ms、`now_playing_screen.dart:2493-2506` 进车载 Fade 200ms）。

| 事件 | 挂钩点 | 关键字段 |
|---|---|---|
| `nav.transition` | `main_screen.dart:311`、`now_playing_screen.dart:2495` | from、to、transitionMs |
| `screen.buildTime` | `car_mode_screen.dart:59`（首帧耗时） | screen、firstFrameMs |
| `palette.extract` | `now_playing_screen.dart:1107` | url、cacheHit、elapsedMs |
| `palette.evict`（补充 7 号场景） | `now_playing_screen.dart:1160` | evicted、cacheSize |
| `anim.active` | `now_playing_screen.dart:1953`（旋转动画）、`car_mode_screen.dart:63` | anim、pulseMs/rotationSec |
| `car.drag` | `car_mode_screen.dart:480` | dragOffset、velocity、dismiss |
| `frame.jank`（派生） | `metrics_collector.dart`（>50ms 单帧） | 帧耗时、build/raster 拆分、当前路由 |

### 6.3 场景 C：WiFi/移动网切换时的转码问题 ✅

代码路径：转码参数在播放开始时定死（`player_provider.dart:1697-1705` 生成带 maxBitRate 的流 URL）；网络切换只改 `_currentConnectionType`（`transcoding_service.dart:123-136`，仅 smartEnabled 时监听）；`resolveActiveUrl()`（`subsonic_service.dart:86-197`）切 base URL——已开始的流 URL 不变，切换后旧地址可能不可达。

**冷启动优化（2026-08-03）**：`resolveActiveUrl()` 把上次生效的 base URL 持久化到 SharedPreferences（`storage_service.dart:38-46` 的 `last_active_base_url`）。冷启时若上次会话落在远端，则**跳过局域网探测直接走远端**（消除不在局域网时最多 3s 的探测阻塞，`subsonic_service.dart:86-131`），同时后台静默探测局域网（`subsonic_service.dart:149-190`，3s 超时），局域网可达后自动切换并更新持久化值；`_verifyConnection` 逐次尝试时按当前 `activeBaseUrl` 建 URL，后台切换最多延迟一个 ping 重试周期即生效。

| 事件 | 挂钩点 | 关键字段 |
|---|---|---|
| `net.switch` | `transcoding_service.dart:123` + `auth_provider.dart:41` | from→to、smartEnabled、切换前后 bitrate |
| `net.urlResolved` | `subsonic_service.dart:112` | host、probeMs、switched |
| `stream.url` | `_setActiveStream`（`player_provider.dart:996`） | songId、maxBitRate、format、transcoded |
| `audio.bufferStart/End` | `_playerStateSub`（`player_provider.dart:1278-1298`） | event、bufferedMs、songId |
| `audio.stall`（派生） | 由 `audio.buffer` + position 停滞可推导（未单列） | — |
| `audio.error` | `_playerStateSub` onError（`player_provider.dart:1300`） | error、songId、netSessionId |

### 6.4 补充场景（确认纳入）

| # | 场景 | 挂钩点 | 事件与关键字段 | 状态 |
|---|---|---|---|---|
| 1 | 后台杀进程恢复播放 | `_restoreQueueState`（`player_provider.dart:235`） | `restore.start/end/error` {count、index、positionMs、error} | ✅ |
| 2 | 切歌/自动下一首失败 | `_onCurrentIndexChanged`（`player_provider.dart:2890`） | `track.next` {from→to、标题} | ✅ |
| 3 | 缓存清理与播放冲突 | `streaming_cache_cleaner.dart:35-52` prune() | `cache.prune` {deletedCount/Bytes、remainingBytes} | ✅ |
| 4 | BPM 分析卡 UI | `bpm_analyzer_service.dart:28` getBPM | `task.bpm` {songId、elapsedMs、bpm} | ✅ |
| 5 | 远程播放链路 | `cast_service.dart:203`、`upnp_service.dart:240/262` | `remote.connect/disconnect/error` + 设备名 | ✅（最小埋点） |
| 6 | 歌词加载失败 | `lrclib_service.dart:24`、`netease_lyrics_service.dart:26` | `lyrics.load` {source、ok、found、elapsedMs} | ✅ |
| 7 | 调色板缓存逐出 | `_paletteCache`（`now_playing_screen.dart:1160`） | 并入 6.2 `palette.evict` | ✅ |

### 6.5 明确不采集

URL 中的密码/token（打码）、歌词内容、歌单明细、用户身份信息。日志只记"发生了什么、耗时多少、错误码"及排查必需的资源 ID（songId/URL host），**不记标题/歌词等文本内容**。

## 7. 性能损耗说明（三层设计）

| 层 | 机制 | 成本 | 生效模式 |
|---|---|---|---|
| 常开层 | FrameTimings 每帧摘要 + 页面 build 计时 + 事件写入环形缓冲（内存） | 每帧几 µs，无感 | 所有模式 |
| 触发层 | jank 后开启 Timeline 录制 1~2s，录完即关、落盘 | 仅录制窗口，日常为 0 | profile |
| release 替代层 | 慢 build 埋点（代码内 Stopwatch，>16ms 才格式化日志） | 每 build 几 µs | 所有模式 |

## 8. 分阶段实施计划

| 阶段 | 内容 | 产出 | 状态 |
|---|---|---|---|
| P0 | LoggingService + GlobalErrorHandler + main.dart 接线 | 统一日志、全局异常入文件 | ✅ |
| P1 | MetricsCollector（帧/启动/网络/内存）+ DiagnosticsService + 诊断页 | 指标采集、会话时间线、查看导出 | ✅ |
| P2 | 场景 A/B/C 埋点 + 派生检测器（silentPlayback/jank/audio.buffer） | 三个已复现问题可定位 | ✅ |
| P3 | 补充场景 1-7 埋点；逐步替换 465 处 debugPrint（保留 tag 语义） | 补充场景 ✅；debugPrint：核心文件已替换（subsonic/player/auth/lyrics 等），长尾 ⬜（zone print 钩子已兜底 debug 模式全量采集） | 部分 ✅ |

阶段依赖：P1 依赖 P0 的事件流与落盘；P2 依赖 P0 事件流 + P1 的帧聚合数据；P3 依赖 P0 的 LoggingService。每阶段完成后回写本表状态并补文件:行号。

里程碑验收标准：

- P0：release 包下未捕获异常能在日志文件找到（含堆栈）；
- P1：诊断页可查 FPS/网络耗时/启动时长，可导出日志；
- P2：复现三个场景后，诊断页按 sessionId 回放能看到根因事件链（如 `sessionActive=false` + `silentPlayback`）。

## 9. 打点系统缺陷修复轮（2026-08-05）

> 背景：2026-08-05 真机导出约 22h 日志分析，发现导出数据被两个并发实例写穿（同 seq 事件、记录被插入打断、双 appSessionId 并存、metrics 同样损坏），另暴露 appVersion 缺失等 4 个问题。本轮全部修复：

| # | 缺陷 | 现象（导出日志证据） | 修复 | 位置 |
|---|---|---|---|---|
| 1 | **双写损坏（P0）** | 首行出现两条 `seq=1` engineInitialized；记录被另一条插入中间（`{"s{...`）；100 个重复 seq；`as-313f6185`/`as-915146f0` 重叠共活 ~1h；metrics.jsonl 同样写穿 | 写锁 `.writer.lock`（create-exclusive + 10s 心跳 + 30s 陈旧接管）；busy 实例降级仅内存 ring 并周期重试接管；接管后轮转隔离僵尸句柄；`init()` 从文件尾对账 `seq`（取 max）与 `appSessionId`（prefs 缺失时沿用文件尾），不再只信 prefs | `file_store_io.dart:7-13,183-267,340-345`、`diagnostics_service.dart:74-104,290-301` |
| 2 | **meta appVersion 恒 unknown** | `meta.json` 全为 `unknown`，日志无法归属版本 | 新增 `kAppVersion` 常量并 `setAppVersion` | `main.dart:34,142` |
| 3 | **as-init 泄漏** | 19 条事件 appSessionId 为默认 `as-init`（含最近一次启动） | pending 补录时改写为真实 appSessionId | `diagnostics_service.dart:117-122,185-194` |
| 4 | **print 钩子无节流** | `AlbumArtwork error` 1624 条、`Queue state saved` 1 条/秒，log.print 占事件 62% | 同消息前缀 64 字符、10s 时间窗去重（控制台输出保留） | `global_error_handler.dart:23-32,111-115` |
| 5 | **jank 误报** | 892 条 jank 全部 buildMs 5-8ms/rasterMs 1-2ms 却报 50-63ms（totalSpan 含帧管线等待） | 判定改用 build+raster（UI 线程实际工作量），jankRate 同源 | `metrics_collector.dart:126-140,158-163` |

附带修复：`app.probe` 事件登记进 `event_types.dart` 常量表（原为裸字符串）；新增单测覆盖写锁互斥/陈旧接管/轮转隔离/尾部对账（`test/services/diagnostics_store_test.dart`）。

> 追加（2026-08-05 二次反馈）：release 下 adb 无法访问 app 私有目录，诊断页导出改为移动端（Android/iOS）弹系统保存对话框**手选路径**（SAF），导出单文件自包含（meta 概要 + 可读日志，复用 `file_picker` 的 `saveFile` bytes 模式）；桌面端仍导出完整目录、Web 仍走剪贴板。见 `diagnostics_service.dart:exportReadableText`、`diagnostics_page.dart:56-95`。

**验证方式**：真机连续使用并复现一次「杀后台/引擎重建」后导出，应满足：单 appSessionId、seq 全局唯一递增、无写穿行、meta 带版本号、log.print 占比显著下降。
