# Luobo 设置页重构技术方案

> 状态：一期已实施 P0-P2（2026-08-05）；**二期（§9，对标飞牛音乐）设计已定稿待开工（2026-09-23）**，二期吸收原遗留的 P3 视觉统一
> 范围：设置页**导航结构**重构（8 Tab → 单列表 + 二级页）+ 信息架构归位（支持/关于去重、诊断收敛）+ 视觉统一 + 版本号硬编码修复；**二期追加**：设置职责分离（App 本体设置收进右上角）、服务器管理流程重排（对标箭头音乐）、公共范式统一
> 设计约束：不新增设置项、不改业务逻辑、不改存储结构；现有 8 个 tab 的「身体」尽量 1:1 复用为二级页；导航骨架新建文件替换，旧 `settings_screen.dart` 保留不删（可一行回退）；文档先行，用户确认后开工
> 实施进度：✅ P0 骨架 → ✅ P1 版本修复 → ✅ P2 归位收敛 → ⬜ ~~P3 视觉统一~~（并入二期 §9）→ ⬜ **二期 §9**（周期 C2，前置 C1）

---

## 1. 背景与目标

### 1.1 用户反馈的痛点

设置界面用起来不舒服、设计不合理：入口不可预览、要横滑翻找、不知道某个选项在哪个 tab 里。

### 1.2 代码实证的根因

| 根因 | 证据 |
|---|---|
| **8 个 Tab 平级横滑，信息不可预览**——这是浏览器标签页形态，不是设置页形态；用户必须逐个切 tab 才知道里面有什么 | `settings_screen.dart:30`（`TabController(length: 8)`）、`:52-102`（TabBar）、`:107-116`（TabBarView 8 个 body） |
| **诊断被放成「一等公民」**——开发者工具与「支持/关于」平级、带图标占一个 tab | `settings_screen.dart:99-101, 115`（DiagnosticsPage 作为第 8 个 tab） |
| **支持与关于内容重复**——关于 tab 里已有评分+打赏+Discord，支持 tab 又是同样的三样 | `settings_about_tab.dart:231-257`（支持 section） vs `settings_support_tab.dart`（整页心形/捐款/Discord 落地页，**已随去原项目化删除**） |
| **同一屏四种视觉语言**：彩色渐变图标块（播放/存储）、淡色图标块（服务器）、渐变图标（AI tab）、裸图标（部分行）混用 | `settings_playback_tab.dart:158-171`（渐变）、`settings_server_tab.dart:144-154`（`withValues(alpha:0.15)` 淡底） |
| **开关组件混用**：`CupertinoSwitch`（trailing）与 `SwitchListTile`（Material 开关）并存 | `settings_playback_tab.dart:334` vs `settings_storage_tab.dart:293` |
| **分隔线不统一**：`_buildDivider(left:56)` 与裸 `Divider(indent:16/64)` 并存 | `settings_storage_tab.dart:225-233` vs `settings_about_tab.dart:148` vs `settings_ai_playlist_tab.dart:133` |
| **行尾交互过载**：同屏出现 DropdownButton 挤在行尾、inline Slider、跳二级页、开关四种交互；Dropdown 在 ListTile 尾部易误触 | `settings_playback_tab.dart:177, 278`（DropdownButton）、`:239`（subtitle Slider） |
| **版本号硬编码过期**：pubspec 已到 `1.1.6+1`，关于页仍显示 `1.0.13` | `settings_about_tab.dart:57`、`:91`（changelog URL）；`update_service.dart:48`（`currentVersion = '1.0.13'`，唯一来源也应同步） |

### 1.3 目标

- **导航对标大厂**：Apple Music（iOS 设置）/ 网易云 / QQ 音乐的设置页模式 = 单列表分组滚动 + 入口行 push 二级页，扫一遍可见全部入口
- **信息归位**：账号/服务器置顶、播放/存储/显示分组、支持与关于去重、诊断收敛、版本号作为 footer
- **工程原则**：导航骨架新建文件替换（入口 `library_screen.dart:1136` / `desktop_navigation_sidebar.dart:53`）；tab 身体 1:1 复用不改逻辑；文档先行，用户确认后开工

---

## 2. 现状能力盘点

### 2.1 设置页文件清单（8 tab 身体）

| 文件 | 内容 | 重构角色 |
|---|---|---|
| `settings_screen.dart`（121 行） | TabController + TabBar + TabBarView 骨架 | ❌ 整体废弃，保留作回退 |
| `settings_playback_tab.dart`（899 行） | 自动DJ/无缝/淡入淡出/歌词/音量平衡/转码 | ✅ 拆出转码段（`:609-897`）后作「播放设置」子页 body |
| （新增）`settings_streaming_tab.dart` | 转码开关/智能转码/码率/格式（搬移自 `settings_playback_tab.dart:609-897`） | ✅ 作「音质与流媒体」子页 body |
| `settings_storage_tab.dart`（938 行） | 缓存开关/清理/离线下载/本地音乐/BPM | ✅ 整体作「下载与存储」二级页 body |
| `settings_server_tab.dart`（645 行） | 连接信息/保存的配置/音乐文件夹/Jukebox/退出登录 | ✅ 整体作「服务器设置」二级页 body |
| `settings_display_tab.dart`（1464 行） | 外观/语言/播放器界面/实时搜索/推荐/ Discord RPC | ✅ 整体作「显示与外观」二级页 body |
| `settings_ai_playlist_tab.dart`（815 行） | API Key/URL/模型/知识库 | ✅ 整体作「AI 智能」二级页 body |
| `settings_support_tab.dart`（323 行） | 心形落地页/捐款/Discord | ❌ **已删除**（去原项目化移除原作者捐赠/社区入口，不再作为「支持」二级页） |
| `settings_about_tab.dart`（540 行） | 版本/开发者/链接/隐私统计/评分打赏 | ⚠️ 拆两半：信息部分上根页；「支持」section 移除（并入支持页）；版本号修复 |
| `services/diagnostics/diagnostics_page.dart` | 打点指标卡/事件流/清除 | ✅ 自带 AppBar（`:149`），直接 push 为独立页 |

### 2.2 入口挂载点

- 移动端：`library_screen.dart:1136`（`NavigationHelper.push(context, const SettingsScreen())`）
- 桌面端：`desktop_navigation_sidebar.dart:53`（`MaterialPageRoute` push `SettingsScreen`）

两个入口都指向 `SettingsScreen` —— 新根页只需替换这一处组件名（新建 `settings_root_screen.dart`，入口改为指向它），旧 `settings_screen.dart` 保留。

### 2.3 其他

- l10n：`tabPlayback/tabStorage/.../tabDiagnostics` 已存在（`app_en.arb:924-931`、`app_zh.arb:712-719`），重构后多数不再使用，**保留不删**（其他语言文件同样引用）；新文案（如「下载与存储」组标题）需新增后 `flutter gen-l10n`
- Provider：`AuthProvider`/`PlayerProvider`/`TranscodingService` 等注册于 `main.dart` 顶层，二级页 push 后照常可用，无需改动
- 版本：`pubspec.yaml:14` = `1.1.6+1`；`update_service.dart:48` 有唯一版本常量；**未引入 `package_info_plus`**

---

## 3. 大厂对标分析

| 维度 | Apple Music（iOS 设置→音乐） | 网易云 | QQ 音乐 | Luobo 现状 |
|---|---|---|---|---|
| 导航结构 | 单列表分组滚动，无 tab，有搜索 | 单列表分组滚动 | 单列表分组滚动 | ❌ 8 Tab 横滑 |
| 行组件 | 图标+标题+右侧 值/开关/chevron 三选一 | 同左 | 同左 | ⚠️ 四种交互混排、Dropdown 挤行尾 |
| 层级 | 多选型选项 push 子页单选 | push 子页 | push 子页 | ⚠️ inline 展开（`if` 条件块）+ Dropdown |
| 账号/服务器 | 置顶（头像/账户） | 置顶（账号与安全） | 置顶 | ✅ 已近似（服务器 tab） |
| 破坏性操作 | 底部红色 | 底部红色 | 底部红色 | ✅ 已有（清除缓存/退出登录红字） |
| 版本号 | 列表底部 footer | 底部 footer | 底部 footer | ❌ 占一行正文且硬编码过期 |

**三家共性**（本次重构直接采用）：
1. 根页是一条**完整的分组列表**，扫一眼可见全部入口，无 tab
2. 行组件**解剖结构统一**：左侧图标 → 标题 → 右侧「当前值 / 开关 / chevron」三选一；chevron 表示「进去还有子页」
3. 层级靠 **push 二级页**，不在根页做 inline 展开
4. 破坏性操作红色置底
5. 版本号是**页面 footer**，不占正文行

---

## 4. 新结构设计

### 4.1 目标信息架构（根页 → 二级页映射）

```
设置（根页，单列表分组）
│
├─ 账号与服务器                    ← 置顶，信息卡展示当前连接（类型/版本/URL/用户名）
│   ├─ 服务器设置        → SettingsServerTab（配置档案/音乐文件夹/Jukebox）
│   └─ 退出登录(红色)               ← 从 server tab 上移到根页底部，见 §4.2
│
├─ 播放与音质
│   ├─ 播放设置          → SettingsPlaybackTab（自动DJ/无缝/淡入淡出/歌词/音量平衡，已拆出转码）
│   └─ 音质与流媒体      → SettingsStreamingTab（转码开关/智能转码/码率/格式）
│
├─ 下载与存储
│   └─ 下载与存储        → SettingsStorageTab（缓存/清理/离线下载/本地音乐/BPM）
│
├─ 显示与外观
│   └─ 显示与外观        → SettingsDisplayTab
│
├─ AI 智能
│   └─ AI 歌单与知识库   → SettingsAiPlaylistTab
│
├─ 支持与帮助                    ← 合并：从 about tab 移入评分/反馈，去掉重复
│   ├─ ~~支持这个 App~~      ❌ 已删除（SettingsSupportTab 随去原项目化移除，捐赠/社区入口不再提供）
│   └─ 评分 Luobo（原在 about tab:239）
│
└─ 关于
    ├─ 版本 / 平台 / 开发者 / 开源链接 / 更新日志 / 隐私与匿名统计
    └─ 诊断（收敛到组内底部）     → DiagnosticsPage（自带 AppBar，直接 push）
                                       └─ 底部 footer：版本号（动态读取）
```

要点：
- **根页信息密度低、一眼扫完**；每个入口 chevron 明确「进去还有内容」
- **8 个 tab body 一个不丢**，全部作为二级页复用到（映射表见 §2.1），业务逻辑零改动
- **支持与关于去重**：评分/打赏/反馈只留一份（支持页）；关于页只留信息类
- **诊断收敛**：从「第 8 个 tab」降级为「关于组底部一行」（仍是一级可达，一个 push）

### 4.2 根页账号与服务器组（置顶信息卡）

- 信息卡复用 `settings_server_tab.dart:30-40` 的取值逻辑（`authProvider.config?.serverType/serverVersion/serverUrl/username`），未连接时显示「未连接」
- 卡片点击 → 服务器设置子页
- **退出登录保持红色、置于根页底部**（大厂惯例：破坏性操作在页面底部，而非中间）

### 4.3 子页复用方案（零逻辑改动）

新建薄壳 `SettingsSubPage`（`lib/widgets/settings_sub_page.dart`）：

```dart
class SettingsSubPage extends StatelessWidget {
  final String title;
  final Widget body;   // 现各 Tab 组件直接传入
  ...
  // Scaffold + AppBar(返回键) + body
}
```

- 各 tab 组件（`SettingsPlaybackTab` 等）**保持 `ListView` 身体不变**，由壳提供 AppBar 返回
- **唯一的结构性拆分（已确认）**：把 `settings_playback_tab.dart:609-897` 的转码 section（`_buildTranscodingSection` + 智能转码 help 弹窗）整体搬移到新文件 `settings_streaming_tab.dart`，作为「音质与流媒体」子页；播放 tab 移除该段，其余逻辑不动
- `DiagnosticsPage` 自带 AppBar，不套壳，直接 `NavigationHelper.push`
- 唯一例外的内容调整在 P2：`settings_about_tab.dart` 移除「支持」section（`231-257`）、修复版本号

### 4.4 统一行组件 `SettingsTile`（P3，样式收敛）

新建 `lib/widgets/settings_tile.dart`，统一三处解剖结构：

```dart
class SettingsTile extends StatelessWidget {
  final IconData icon;
  final Color iconColor;          // 方案见下
  final String title;
  final String? subtitle;         // 说明文字（可含 trailing 值）
  final Widget? trailing;         // 三选一：值 / CupertinoSwitch / chevron
  final bool destructive;         // 红色标题（清除缓存/退出登录）
  final VoidCallback? onTap;
}
```

- 行内不再放 DropdownButton/Slider（这些留在二级页内）
- 开关统一 `CupertinoSwitch`（现有主流），废弃 `SwitchListTile`
- 分隔线统一 `left:56` 缩进（对齐现有 `_buildDivider`）

**leading 图标方向（已确认：方案 B，Apple Music / iOS Settings 风格）**：
- 统一「主色 12% alpha 淡底 + 主色单色图标」，破坏性行用红色系
- 说明：Apple Music / iOS「设置」的图标是**单色 glyph + 淡底色**，不是彩色渐变块；用户选择「参考 Apple Music 风格」即对应本方案 B（拍板时曾误以为 A 是 Apple Music 风格，已澄清）

### 4.5 版本号修复（P1 顺手做，零新依赖）

`1.0.13` 三处硬编码收敛为**唯一来源** `update_service.dart:48`：

| 位置 | 现状 | 改法 |
|---|---|---|
| `update_service.dart:48` | `currentVersion = '1.0.13'` | 改为 `'1.1.6'`（版本号唯一真源） |
| `settings_about_tab.dart:57` | `subtitle: '1.0.13'` | 引用 `UpdateService.currentVersion` |
| `settings_about_tab.dart:91` | changelog URL 拼 `v1.0.13` | 引用 `UpdateService.currentVersion` 拼 URL |

不引入 `package_info_plus`（pubspec 目前无此依赖）；如需「永远与 pubspec 同步」留待二期（§8）。

---

## 5. 数据契约（根页行 → 状态来源）

| 根页行 | trailing 值 | 数据来源 | 来源现处 |
|---|---|---|---|
| 当前服务器（信息卡） | 类型 + 版本 | `AuthProvider.config` | `settings_server_tab.dart:30-40` |
| 服务器设置 | chevron | — | push 子页 |
| 播放设置 | 自动DJ当前模式（可选） | `playerProvider.autoDjService.mode` | `settings_playback_tab.dart:59` |
| 音质与流媒体 | 当前码率/格式（可选） | `TranscodingService` | 搬移后 `settings_streaming_tab.dart` |
| 下载与存储 | chevron | — | push 子页 |
| 显示与外观 | chevron | — | push 子页 |
| AI 歌单与知识库 | chevron | — | push 子页 |
| 支持这个 App | chevron | — | push 子页 |
| 版本 | `UpdateService.currentVersion` | 常量 `update_service.dart:48` | 修复后 |
| 语言 | 当前语言 | `LocaleService` | `settings_display_tab.dart` |
| 诊断 | chevron | — | push DiagnosticsPage |

---

## 6. 分阶段实施计划

| 阶段 | 内容 | 交付物 | 状态 |
|---|---|---|---|
| P0 骨架 ✅ | `settings_root_screen.dart:29`（单列表 7 组 + 底部退出登录 + 版本 footer）+ `settings_sub_page.dart`（薄壳）；**拆分转码**：`settings_streaming_tab.dart:12`（搬移转码段），`settings_playback_tab.dart` 已删转码段（净减 ~290 行）；入口 `library_screen.dart:1136`、`desktop_navigation_sidebar.dart:53` 改指新根页；旧 `settings_screen.dart` 保留 | 根页可扫、6 个子页接入（播放/音质/存储/服务器/显示/AI） | ✅ |
| P1 版本修复 ✅ | `update_service.dart:48` = `'1.1.6'`（唯一真源）；根页版本行/更新日志 URL/footer 均引用 `UpdateService.currentVersion`；`settings_about_tab.dart` 已随旧屏幕退休保留，其硬编码 `1.0.13` 不再被引用 | 运行时显示 1.1.6 | ✅ |
| P2 归位收敛 ✅ | 评分弹窗抽出为 `rating_dialog.dart`（`showRatingDialog`）供根页复用；根页「支持与帮助」组 = 支持 Musly（→SupportTab 子页）+ 为 Musly 评分；「关于」组 = 版本/平台/开发者/链接/隐私统计/设备ID/诊断（→DiagnosticsPage 直接 push）；l10n 新增 12 键（`app_en.arb`/`app_zh.arb` 各 +12）+ `flutter gen-l10n` 全量重生成 | 支持/关于去重、诊断收敛至关于组底部 | ✅ |
| P3 视觉统一 | 新建 `settings_tile.dart` 并替换各页行组件；开关统一 CupertinoSwitch、分隔线统一缩进；leading 图标按 §4.4 确认的方向收敛 | 视觉语言一致 | ⬜ |

每阶段独立可验收，P0 不触碰任何业务逻辑。

---

## 7. 决策清单

### 已确认（2026-08-05 全部拍板）

1. **8 Tab → 单列表 + 二级页**（对标三家大厂设置页）
2. **诊断入口**：收敛到「关于」组底部，一级可达、一个 push
3. **leading 图标**：方案 B「主色淡底 + 单色图标」（Apple Music / iOS Settings 风格；已澄清 A/B 对应关系）
4. **播放/音质拆两个子页**：「播放设置」（自动DJ/无缝/淡入淡出/歌词/音量平衡）+「音质与流媒体」（转码），P0 拆分转码段
5. **支持页**：保留现有长文案落地页作为二级页
6. **导航骨架新建文件替换**，旧 `settings_screen.dart` 保留不删（沿用首页重构验证过的模式）
7. **8 个 tab 身体 1:1 复用为二级页**，业务逻辑零改动（唯一结构改动 = 转码段搬移）
8. **版本号**收敛到 `update_service.dart:48` 唯一来源，同步至 `1.1.6`

---

## 8. 二期增强清单（本期上线稳定后）

| # | 二期项 | 说明 | 触发条件 |
|---|---|---|---|
| 1 | 设置搜索 | 根页顶部搜索框，过滤设置项（iOS 16+ Settings 模式） | 根页分组变多、用户找设置费力 |
| 2 | 版本号自动同步 | 引入 `package_info_plus`，运行时读 pubspec 版本，彻底消除硬编码 | 发布频率升高、版本再漂移 |
| 3 | 设置项说明气泡 | 复杂项（转码/音量平衡）行尾加 ⓘ，弹 explainer（现有智能转码 help 模式推广） | 用户对专业项产生疑问 |

---

## 9. 二期改造：对标飞牛音乐（2026-09-23）

> **来源**：用户对现有设置页长期不满意，2026-09-23 提供飞牛音乐（fnos）设置族截图 + 箭头音乐（第三方 Subsonic 客户端）服务器管理流程截图作为参考。
> **优先级**：用户明确「设置页改造」为**第 1 优先**（高于歌曲 `⋮` 面板与美化类）。
> **设计语言与组件清单**见 `docs/视觉与交互对标飞牛音乐技术方案.md`（本文件只写设置页的结构与迁移）；**周期 C2**，前置依赖 C1（`design_tokens` + `GlassCircleButton` + 二级页骨架）。

### 9.1 现结构的三条问题

| # | 问题 | 证据 |
|---|---|---|
| 1 | **「显示与外观」是巨型混杂页** | `settings_display_tab.dart`（1464 行）同时装：外观（主题/强调色/玻璃）＋ 播放器界面行为（音量条/星级/迷你播放器心形·重复·随机）＋ 封面样式 ＋ 实时搜索 ＋ 智能推荐 ＋ Discord |
| 2 | **分组粒度不均** | 根页 6 组中，存储 / 显示 / AI **各只有 1 项**——1 项成组等于白占一层 |
| 3 | **没有「App 本体设置」的位置** | 外观 / 缓存 / 日志 / 退出登录这些与音乐内容无关的项，散在内容设置里 |

**fnos 的解法**：根页只放「账号 + 内容」，「App 本体」全部收到**右上角圆钮**里。

### 9.2 修订后的根页排版

```
┌───────────────────────────────────────────────┐
│  设置                                    (⚙)  │ ← 大标题 + 右上圆钮 → App 设置
├───────────────────────────────────────────────┤
│ ╭───────────────────────────────────────────╮ │
│ │ (头像)  17630317193                       │ │
│ │         ● WiFi · 不转码                   │ │ ← 连接状态（网络 + 转码），见 §9.2.1
│ │  ───────────────────────────────────────  │ │
│ │  ☁   服务器管理                         > │ │ ← 与账号卡同一张卡内
│ ╰───────────────────────────────────────────╯ │
│ ╭───────────────────────────────────────────╮ │
│ │  ▶   播放设置                           > │ │
│ │  ───────────────────────────────────────  │ │
│ │  〜   音质与流媒体                       > │ │
│ │  ───────────────────────────────────────  │ │
│ │  ▥   播放器界面                         > │ │ ← 由原「外观组」并入
│ │  ───────────────────────────────────────  │ │
│ │  ⬇   下载与存储             1.2 GB      > │ │ ← 状态值外显
│ ╰───────────────────────────────────────────╯ │
│ ╭───────────────────────────────────────────╮ │
│ │  ✨   AI 智能                            > │ │
│ │  ───────────────────────────────────────  │ │
│ │  ✧   智能推荐                           > │ │
│ ╰───────────────────────────────────────────╯ │
│ ╭───────────────────────────────────────────╮ │
│ │  ?   帮助与反馈                         > │ │
│ │  ───────────────────────────────────────  │ │
│ │  ⓘ   关于 Luobo              v1.1.17    > │ │ ← 版本号外显
│ ╰───────────────────────────────────────────╯ │
└───────────────────────────────────────────────┘
```

- **4 组**，无「1 项成组」
- **删除**底部独立的 `Luobo v1.x.x` footer —— 版本号移到「关于 Luobo」行右侧
- **不设**「用户管理」（道理鱼无此概念）、**不设**独立「音乐库管理」（见 §9.6）
- **不设**「播放页主题」「封面样式」—— 随自定义主题功能整体删除（2026-09-23 用户拍板，见 fnos 方案 §7 ⑬）
- 原「显示与外观」组因此解散，其中**「播放器界面」并入本组**

### 9.2.2 底栏改造（2026-09-23 用户拍板，修订 4）

**结论：第 3 位不是搜索，是「设置」** —— 对照 fnos 的设置页截图，底栏第 3 个图标是**选中态粉色圆**，说明它进的就是设置。

| 项 | 改前 | 改后 |
|---|---|---|
| 项数与内容 | 3 项：首页 / 音乐库 / **搜索** | 3 项：首页 / 音乐库 / **设置** |
| 文字标签 | 图标 + 文字 | **纯图标、无文字**（fnos 亦然） |
| 分布 | — | **3 项均匀分布**（每项 `flex:1`）+ 分隔线 + 一个**等宽槽位**放当前专辑圆封面 |
| 搜索入口 | 底栏第 3 项 | 改为**大标题页右上角圆钮**（首页 + 音乐库各一个） |

**连带处理**

| # | 事项 | 处置 |
|---|---|---|
| 1 | **彩蛋页**（连点搜索 tab 11 次，`main_screen.dart:632-647`） | ✅ **用户拍板：先不要了** |
| 2 | `search_screen.dart:386-392` 的「有声书 / 电台」分类卡与音乐库内那张**重复** | 随搜索 tab 移除一并清理 |
| 3 | 底栏只剩 2 项太空 | ✅ 已解决 —— 第 3 位放「设置」 |

**有声书**：**已在音乐库内**（`library_screen.dart:536-560`，道理鱼下「电台」位换成「有声书」绿书本图标）→ 保留确认，无需新增入口。

**⚠️ 待确认**：底栏去掉文字标签会牺牲一点可发现性（fnos 就是这样），需用户最终确认。

### 9.2.1 账号卡副标题：连接状态（2026-09-23 用户要求）

原副标题 `道理鱼音乐 · 已连接` → 改为**连接状态**（网络类型 + 转码状态），例：`● WiFi · 不转码`、`● 蜂窝网络 · 转码中 OPUS 192kbps`。

**⚠️ 复用现有逻辑，不要新写第三遍。** 同一套判断现已存在两处：

| 位置 | 用途 |
|---|---|
| `mini_player.dart:466 _showTranscodeToast` | 长按迷你播放器封面 → 居中 toast |
| `song_tile.dart:1105 _QualityInfo` | 歌曲 `⋮` 面板内的音质信息 |

账号卡是**第三处** → 应抽成**公共状态函数**（如 `TranscodeStatus.describe(song, player, transcoding, l10n)` 返回 `{networkLabel, networkColor, statusLabel, statusColor}`），三处共用。

**状态取值**（来自现有实现）

| 维度 | 取值 | 颜色 |
|---|---|---|
| 网络 | `WiFi` / `蜂窝网络` | 绿 `#34C759` / 橙 `#FF9500` |
| 不转码 | `不转码` | 强调色（玫红） |
| 转码中 | `转码中` | 橙（浅 `#E65100` / 深 `#FFB74D`） |
| 已转码 | `已转码` | 同上 |

**最终形态（2026-09-23 用户拍板）**：副标题 = `服务器类型 · 网络 · 短转码状态`

| 场景 | 副标题 |
|---|---|
| 不转码 | `道理鱼音乐 · WiFi · 不转码` |
| 转码中 | `道理鱼音乐 · 蜂窝网络 · 转码中` |
| 已转码 | `道理鱼音乐 · WiFi · 已转码` |

> **不含格式码率**（`OPUS 192kbps`）—— 那截已在播放页时间行与 `⋮` 面板的 `_QualityInfo` 里，账号卡是概览位不重复。
> **长度已实测**：`道理鱼音乐 · 蜂窝网络 · 转码中` ≈ 183px，账号卡可用宽度 203px（330 − 页边距 32 − 卡内边距 32 − 头像 50 − 间距 13）→ **单行放得下**（加 `OPUS 192kbps` 则会溢出换行）。

### 9.3 App 设置（右上角圆钮 → 二级页）

**照 fnos 的三个细节（2026-09-23 对照 `08-设置/App设置-浅色.jpg` 修正）**

1. **标题就叫「设置」**，不叫「App 设置」（fnos 原文如此）
2. **行内不带图标** —— 主设置页是「玫红图标 + 文字」，这页是**纯文字 + chevron**；因此**分隔线缩进到 `16px`（文字起点），不是 `53px`**
3. **二级页无底栏**（fnos 亦然）；若有歌曲在播，此处是**一条悬浮播放条胶囊**（见 §2.6）

```
┌───────────────────────────────────────────────┐
│  (←)                设置                      │ ← 居中标题 + 左上圆返回
├───────────────────────────────────────────────┤
│ ╭───────────────────────────────────────────╮ │
│ │  外观                                   > │ │ ← 无图标，纯文字
│ │  ───────────────────────────────────────  │ │ ← 分隔线缩进 16px
│ │  语言                     简体中文      > │ │
│ ╰───────────────────────────────────────────╯ │
│ ╭───────────────────────────────────────────╮ │
│ │  清除 App 缓存              1.2 GB      > │ │
│ │  ───────────────────────────────────────  │ │
│ │  App 日志上报                           > │ │
│ ╰───────────────────────────────────────────╯ │
│ ╭───────────────────────────────────────────╮ │
│ │              退出登录                     │ │ ← 独立胶囊（灰底 #E5E5E7 / 深 #2C2C2E）
│ ╰───────────────────────────────────────────╯ │   ← 居中文字、无 chevron
└───────────────────────────────────────────────┘
```

> **与 fnos 的差异（有意保留）**：fnos 这页第二行是「FN Connect 连接偏好」（飞牛自家连接协议）；Luobo 无对应物，改为**「语言」**（原本在根页）。

### 9.9 设计稿清单（2026-09-23）

**稿子位置**：`/Users/v_chengsiyuan03/Downloads/fnosmusic/设计稿/`（HTML 源 + 渲染 PNG；HTML 用 Chrome 打开即最新）

| 稿 | 内容 | 状态 |
|---|---|---|
| `01-C1-二级页骨架对比.png` | C1 前后对比（裸箭头 vs 悬浮圆钮 + 居中标题） | ✅ 已确认 |
| `02-设置根页.{html,png}` | 设置根页（浅色 + 深色）+ App 设置页，3 屏 | ✅ 已确认（修订 4） |
| `03-设置二级页全量.{html,png}` | 设置二级页 15 屏（见下表） | ⬜ **待确认** |
| `04-C1-地基组件总览.{html,png}` | **C1 地基 8 类**：Token 全表（含间距/字阶/玻璃/**动效**）· 圆钮 7 形态 · **底栏 `GlassPillNav`** · **行家族 7 变体** · 分组卡+小节标题+行下说明 · 胶囊按钮 3 种 · **Sheet 壳** · **空态** | ⬜ **待确认** |

**全量二级页 15 屏**（`03-设置二级页全量.png`，2026-09-23）

**A. 根页直接进入（7 屏）** —— 删掉杜撰项后，根页真实二级页**只有 7 个**

| # | 页面 | fnos 参考 | 对应现有实现 |
|---|---|---|---|
| A1 | 服务器管理（已连接的服务器） | 「箭头音乐」已连接的平台 | `settings_server_tab.dart` / `saved_profiles_screen.dart` |
| A2 | 播放设置（自动 DJ + 音量平衡 + 淡入淡出） | ❌ 无参考 | `settings_playback_tab.dart` |
| A3 | 音质与流媒体（智能转码 + ⓘ + 3 音质 + 格式） | 「音质偏好」 | `settings_streaming_tab.dart` |
| A4 | 下载与存储（开关独占卡 + 上限卡 + 独立操作卡） | 「自动缓存」 | `settings_storage_tab.dart` |
| A5 | 播放器界面（**含「智能推荐」section**） | ❌ 无参考 | `settings_display_tab.dart` |
| A6 | AI 智能（连接设置 3 项 + **知识库 3 项含动作** + 工作原理） | ❌ 无参考 | `settings_ai_playlist_tab.dart` —— **全量**：API Key（打码 `sk-1a2b…ef90`）/ API 地址 / AI 模型；歌曲知识库（已索引 N/M + 最后更新 + 进度条 → **[生成/增量更新]**）/ **导出知识库 [导出]** / **导入知识库 [导入]**；工作原理 |
| A7 | 关于 Luobo（更新日志 / 机制说明 / 诊断 / 仓库） | 「关于飞牛音乐」 | `settings_about_tab.dart` + `ChangelogScreen` + `SettingsMechanismScreen` + `DiagnosticsPage` |

**B. 服务器管理子流程（4 屏）**

| # | 页面 | fnos/箭头音乐参考 |
|---|---|---|
| B1 | 服务器操作面板（`⋮` 弹层：修改连接/服务器状态/重新扫描/**分享服务器二维码** ＋ 移除连接红字 ＋ 取消） | 箭头音乐「平台操作面板」 |
| B2 | **修改连接**（胶囊输入框 + 音乐库范围 + 在线模式 + 更新）—— **添加/编辑共用此样式** | 箭头音乐「编辑服务器」 |
| B3 | **添加服务器**（**类型网格 4 项等宽等距**：自动识别/Subsonic/Jellyfin/道理鱼 ＋ 右上**扫码** ＋ 其他方式：扫码添加/使用本地文件） | 箭头音乐「新增平台」类型网格 |
| B4 | 服务器详情（统计卡 + 服务器信息卡） | 箭头音乐「服务器详情」 |

**C. App 设置（右上角圆钮）+ 诊断（3 屏）**

| # | 页面 | 参考 |
|---|---|---|
| C1 | 外观（主题模式单选 + 强调色 8 色点） | fnos「连接偏好」单选组范式 |
| C2 | 语言（单选列表，30+ 语言） | 同 C1 范式 |
| C3 | 诊断（指标卡 + 搜索 + 日志列表 + 复制/导出/清空） | 现有 `DiagnosticsPage` |

**D. 展开态示例（1 屏）**

| # | 页面 | 参考 |
|---|---|---|
| D1 | 选项面板（自动 DJ 模式 · 单选 Sheet，6 项） | fnos「音质偏好 → 选项面板」 |

**⚠️ 三轮被否的杜撰项（教训，勿重犯）**

| 轮次 | 我杜撰的东西 | 真相 |
|---|---|---|
| v1 | **「连接偏好」页** | 代码零结果 —— `resolveActiveUrl` 是**全自动、不可配置**的 |
| v2 | **「帮助与反馈」页** | 全仓零结果，**不存在** |
| v2 | **「智能推荐」当独立页** | 它只是「播放器界面」页内的一个 **section**（`settings_display_tab.dart:136-148`） |
| v2 | **「App 日志上报」**（照抄 fnos 文案） | 对 Luobo **不准确** —— 日志只存本地、**明确不做网络上报** → 改用现有「匿名分析」+ 新增「导出日志」 |
| v3 | **「局域网发现」**（抄箭头音乐「新增平台」） | **全仓零结果** —— Luobo 只有「登录网关页」（`server_gateway_screen.dart`：服务器卡片列表 + 扫码 + 本地文件），**没有局域网发现** |
| v3 | **服务端类型 9 宫格网格** | Luobo 是 **4 项单选**（自动/Subsonic/Jellyfin/道理鱼），见 `server_form_screen.dart:328-350` |
| v3 | **AI 页漏项** | 只画了 4 行，实际还有**知识库的 3 个动作按钮**（生成/导出/导入）、Key 打码、索引进度与最后更新、工作原理组 |
| v3 | **过度修改：把类型网格整个删了** | 用户说「平台没有整齐排列」是让**修对齐**，我却删了网格。正解：保留 4 项类型网格，用 **CSS `grid` + `repeat(4,1fr)`** 保证严格等宽等距 |

**v3 已拍板的服务器流程细节（2026-09-23）**

- **添加 / 编辑服务器共用同一表单样式** —— 箭头音乐「编辑服务器」的**灰底胶囊输入框**
- **添加服务器**参考「新增平台」的**类型网格**（4 项：自动识别 / Subsonic / Jellyfin / 道理鱼），右上角是**扫码**
- **B1 的「分享」= 分享服务器二维码**（Luobo 已有 `server_qr_dialog.dart`）

**样式 bug（C1 组件必须一次做对）**

| 问题 | 修法 |
|---|---|
| **chevron 跟文字** | 右侧位一律 `margin-left: auto`（§2.3 已记） |
| **开关跟文字** | 同上 —— 同类错误犯了两次 |
| **单选组未选中项带 `›`** | 单选组**只有选中项打勾，其余项什么都不带** |

**新发现的功能（本轮查证）**

- **自动 DJ 是真实功能**：`AutoDjMode` 6 值（`off/shuffleLibrary/similarSongs/sameGenre/sameArtist/smartMix`）+ `AutoDjService` + `player_provider.dart` 4 处调用
- **日志导出已存在**但埋在「关于 → 诊断」：`DiagnosticsService.export()`（`diagnostics_service.dart:410`）→ 本轮**提到 App 设置**并新增入口

### 9.4 服务器管理流程（参考箭头音乐）

### 9.4 服务器管理流程（参考箭头音乐）

**借流程、不借皮**：箭头音乐是蓝色主色 + 灰底胶囊输入框；Luobo 用玫红 + 白卡范式，只对齐**步骤与信息层级**。

```
设置 → 服务器管理
┌───────────────────────────────────────────────┐
│  (←)     已连接的服务器            (⊞)(＋)   │ ← 右上胶囊双钮：扫码 / 新增
├───────────────────────────────────────────────┤
│  (绿)  绿联                             ⋮    │ ← 每项一行，全宽分隔线
│        道理鱼音乐                              │
│  ─────────────────────────────────────────   │
│  (蓝)  我的 Navidrome                    ⋮    │
│        Navidrome                              │
│                                               │
│         点击切换当前服务器                     │ ← 行下灰字说明
└───────────────────────────────────────────────┘
        ↓ 点 ⋮
┌───────────────────────────────────────────────┐
│  绿联                                     ✕   │
│ ╭───────────────────────────────────────────╮ │
│ │  修改连接                              >  │ │
│ │  ───────────────────────────────────────  │ │
│ │  服务器状态                            >  │ │
│ │  ───────────────────────────────────────  │ │
│ │  重新扫描                              >  │ │
│ │  ───────────────────────────────────────  │ │
│ │  分享                                  >  │ │
│ ╰───────────────────────────────────────────╯ │
│ ╭───────────────────────────────────────────╮ │
│ │  移除连接                              >  │ │ ← 红字，独立卡
│ ╰───────────────────────────────────────────╯ │
│ ╭───────────────────────────────────────────╮ │
│ │              取消                        │ │ ← 主色胶囊
│ ╰───────────────────────────────────────────╯ │
└───────────────────────────────────────────────┘
        ↓ 服务器状态
┌───────────────────────────────────────────────┐
│  (←)         服务器详情                (↻)   │
├───────────────────────────────────────────────┤
│  ██████ 渐变头图（取服务器色 / 默认 mesh）█████ │
│            (logo)   绿联                      │
│ ╭───────────────────────────────────────────╮ │
│ │   ◎ 专辑     ♪ 歌曲     👤 艺术家          │ │
│ │    461        2772         69             │ │ ← 三列统计
│ │  ───────────────────────────────────────  │ │
│ │   ● 服务端状态                       Ok    │ │ ← 绿字
│ ╰───────────────────────────────────────────╯ │
│ ╭───────────────────────────────────────────╮ │
│ │  ⓘ  服务器信息                             │ │
│ │   服务端名称       绿联                     │ │
│ │   广域网           https://…:10010          │ │
│ │   局域网           http://192.168…:4000     │ │
│ │   服务端类型       道理鱼音乐                │ │
│ │   服务端版本号     1.0.0                    │ │
│ ╰───────────────────────────────────────────╯ │
│ ╭───────────────────────────────────────────╮ │
│ │  ▤  媒体统计                               │ │
│ │   歌曲数           2772                    │ │
│ │   专辑数           461                     │ │
│ │   艺术家数         69                      │ │
│ │   有声书数         8                       │ │
│ │   最后扫描时间     2026-09-23 13:44        │ │
│ ╰───────────────────────────────────────────╯ │
└───────────────────────────────────────────────┘
```

**复用现有零件**（不重写）：`saved_profiles_screen.dart`（列表）→ 改为「已连接的服务器」；`server_form_screen.dart` → 「修改连接」；`server_qr_dialog.dart` / `qr_scanner_screen.dart` → 分享 / 扫码；`server_gateway_screen.dart` 保留。

### 9.5 迁移映射（逐项从哪到哪）

| 现在的位置 | 去向 |
|---|---|
| 根页：账号卡 / 服务器管理 | 根页组 1（账号卡 + 服务器管理同卡） |
| 根页：播放设置 / 音质与流媒体 / 下载与存储 | 根页组 2 |
| `settings_display_tab`：**播放页主题 / 封面样式 / 播放器界面** | 根页组 3（内容显示） |
| `settings_display_tab`：**主题模式 / 强调色 / 玻璃** | App 设置 →「外观」 |
| `settings_display_tab`：**语言** | App 设置（状态值外显） |
| `settings_display_tab`：**实时搜索** | 并入「播放器界面」 |
| `settings_display_tab`：**智能推荐** | 根页组 4「智能推荐」 |
| `settings_display_tab`：**Discord RPC**（仅桌面） | 留在「播放器界面」内 |
| 根页：AI 智能 | 根页组 4 |
| `settings_about_tab`：**匿名统计开关** | App 设置 →「App 日志上报」 |
| `settings_storage_tab`：**清除全部缓存** | App 设置 →「清除 App 缓存」（状态值外显） |
| `settings_server_tab`：**退出登录** | App 设置（独立胶囊） |
| `settings_server_tab`：**音乐文件夹** | 并入「服务器管理」（见 §9.6） |
| 根页：关于（版本/平台/GitHub/更新日志/机制说明/诊断） | 根页组 5「关于 Luobo」二级页（版本号提到行右侧） |

> **全部为「移动 / 合并」，不新增设置项**（符合本文档 §1 的既有约束）。唯一新增的是**分组标题**的 l10n key（`settingsGroupInterface` 等）。

### 9.6 「音乐文件夹」的处置（含一处既有 bug）

**这个功能原本做什么**：Subsonic/OpenSubsonic 的 `getMusicFolders` 返回服务端的**音乐库列表**。Navidrome 可配置多个音乐文件夹（多个库根目录）。Luobo 的链路是：

1. `settings_server_tab.dart:195 _buildMusicFoldersButton` → `:223 _showMusicFoldersDialog` 拉 `getMusicFolders()`（`subsonic_service.dart:672`）
2. `_MusicFoldersDialog`（`:365`）用 **`CheckboxListTile` 多选**勾选
3. 结果存 `ServerConfig.selectedMusicFolderIds`（`server_config.dart:8`）
4. **生效点只有两处**：`_buildUrl`（`subsonic_service.dart:536-537`，全部业务请求）与封面 URL 构建（`:723-724`）—— **都只取 `.first`**

**实际作用**：把整个 App 的请求范围**限定在某一个音乐文件夹**（典型场景：服务端有「音乐」和「有声书」两个库，只选一个）。

**⚠️ 既有 bug（UI 与实现不一致）**：UI 是**多选**，但代码只用 `.first` —— **勾 2 个与勾第 1 个效果完全相同**，多选是假的。

**处置决定**：并入「服务器管理」→ 放在**「修改连接」页内作为一行**「音乐库范围 → 全部 / 音乐」（语义上它是服务器属性，不是内容管理）。**顺手修掉多选 bug**：改为**单选**（Subsonic 的 `musicFolderId` 是单值参数，真多选需改请求策略，成本高且无必要）。

### 9.7 本次新增的公共范式（来自新截图）

| 范式 | 特征 | 来源截图 |
|---|---|---|
| **单选 Sheet** | 底部升起 + **居中标题** + **每个选项一张独立白卡**（主标题 + 副标题灰字 + 右侧玫红勾） | fnos 音质偏好 |
| **独立操作卡** | 危险 / 独立操作**单独一张卡**，不与其它项同卡 | fnos「清除已缓存歌曲 1014.57 MB」、箭头音乐「移除连接」 |
| **开关独占单行卡** | 一张卡只放一个开关 | fnos「自动缓存播放中的歌曲」 |
| **右上胶囊双钮** | `⚙` 与 `＋` 合并进**一个胶囊** | fnos 用户管理、箭头音乐已连接平台 |
| **统计卡** | 三列（图标 + 标签 + 数值）+ 卡内状态行（● 状态 → 值） | 箭头音乐服务器详情 |
| **label/value 信息卡** | 多行 `label 左 / value 右` | 箭头音乐服务器信息、媒体统计 |

### 9.8 已拍板决策（2026-09-23）

| # | 决策 | 结论 |
|---|---|---|
| 1 | 用户管理 | ❌ **不做**（道理鱼无此概念） |
| 2 | 独立「音乐库管理」入口 | ❌ **不设**；音乐文件夹**并入「服务器管理」的修改连接页**，并修掉多选 bug（§9.6） |
| 3 | 服务器详情页 | ✅ **做**（新增页面：统计卡 + 服务器信息卡 + 媒体统计卡） |
| 4 | 服务器列表拖动排序 | ❌ **不支持**（箭头音乐有，Luobo 不做） |
| 5 | 音质偏好的交互形式 | ✅ **保留现有形式**，只**统一视觉风格**（不改成单选 Sheet） |
| 6 | 退出登录位置 | ✅ 从「服务器管理」移到 **App 设置**（独立胶囊） |
| 7 | 玻璃开关 | ✅ 改为**强度档**（关 / 轻 / 强），见 fnos 方案 §7 ③ |

