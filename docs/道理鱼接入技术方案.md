# 道理鱼接入技术方案

> 状态：已实施（2026-08-07，analyze 无 error、单测 129 绿 / 1 既有失败与本次无关）
> 范围：Luobo 客户端兼容道理鱼服务平台（纯客户端适配，道理鱼服务端零改动）
> 依据：`/Users/v_chengsiyuan03/Downloads/道理鱼接口/道理鱼接口文档.md`（v3.1，含 61 个实测抓包）+ Luobo 源码逐条对照验证
> 当前 master：`c889513`（v1.1.12）

## 1. 背景与目标

道理鱼是一套**独立、闭源**的音乐服务系统（他人部署），对外提供**双协议**：

```
192.168.31.142:4000（内网） / daoliyu.625122961.xyz:10010（远程 HTTPS）
│
├── /rest/*.view   Subsonic 兼容层（Luobo 现用）
│     认证: 明文密码 p=（token+salt 被拒 code41）
│     能力: 浏览/搜索/播放/封面/歌单/收藏（子集）
│
└── /api/*         自研 JSON 层（JWT Bearer）
      认证: POST /api/auth/login → JWT
      能力: 歌词/随机歌/播放控制/推荐/Admin
```

目标：Luobo 以 `serverFamily='daoliyu'` 独立接入，登录后全部数据走道理鱼。由于道理鱼闭源无法改造，**所有适配均为 Luobo 客户端代码改动**。

## 2. 兼容性验证结论（对照源码逐条核实）

### ✅ 已兼容（零改动）

| 能力 | 验证点 |
|---|---|
| Subsonic 认证 | `useLegacyAuth=true` → `p=` 明文，`_getAuthParams()` 已实现（`subsonic_service.dart:355-376`），登录 UI 已有开关（`login_screen.dart:865`） |
| 双地址 | `localUrl + serverUrl` 模型已实现（`resolveActiveUrl`，`subsonic_service.dart:104-210`），填道理鱼两端地址即可 |
| HTTPS 远程 | Let's Encrypt 可信证书（`*.625122961.xyz`），无需开自签 |
| 浏览/播放/封面/歌单/收藏 | 全部 Subsonic 端点已实现且道理鱼支持（`getStarred2` 实测可用） |
| 无损直出 | LAN 已强制 `format=raw`（`transcoding_service.dart:91` rule 1），道理鱼实测直出 FLAC |
| scrobble | 实测 OK（`scrobble?submission=true` 返回 ok，播放统计正常） |

### ⚠️ 需适配（道理鱼不支持的 Subsonic 端点）

| Luobo 功能 | 调用点 | 道理鱼表现 | 适配方案 |
|---|---|---|---|
| 歌词 | `getLyricsBySongId()`（subsonic_service:989） | `Unknown endpoint` | JWT 通道 + 自研 `/api/tracks/{id}/lyrics`（§4） |
| 随机歌 | `getRandomSongs()`（subsonic_service:874），4 处调用 | `Unknown endpoint` | 自研 `/api/tracks/random` 映射（§5） |
| 流派（Search 入口） | `GenresScreen` → `getGenres()`（subsonic_service:1004） | `Unknown endpoint` | 本地聚合（§6） |
| 电台 | `getInternetRadioStations()`（subsonic_service:1127） | `Unknown endpoint` | 显示"服务器不支持"提示（RadioScreen 已有错误态） |
| Jukebox | — | — | **整体删除**（§7，无使用场景） |

### ✅ 天然不受影响

- **音乐库 Genres Tab**：`library_screen.dart:687` 从 `cachedAllSongs` 的 `s.genre` 字段本地聚合，不依赖服务器 `getGenres()`（道理鱼下完全正常）
- **艺人热门歌**：`getArtistTopSongs()` 已有本地回退（getArtist + getAlbumSongs 拼装，subsonic_service:1233-1246）
- **相似歌曲**：失败回退空列表，已有容错

## 3. 登录接入（serverFamily='daoliyu'）

### 3.1 登录页

`login_screen.dart:1465-1484` 的 `_ServerFamilyToggle` chips 增加一项：

```dart
(
  label: '道理鱼',
  family: 'daoliyu',
  icon: CupertinoIcons.music_note,
  activeColor: const Color(0xFF6366F1),
),
```

选择 daoliyu 时**强制 `_useLegacyAuth = true`**（道理鱼只认明文 p=）。

### 3.2 auth_provider.login

`auth_provider.dart:210-329`：daoliyu 分支与 subsonic 相同（走 Subsonic 层 ping 验证），但：

1. 构造 `ServerConfig(serverFamily: 'daoliyu', useLegacyAuth: true, ...)`
2. `configure()` 后调用新增的 `SubsonicService.loginToApi()`（§4.2）——**失败不阻塞登录**（Subsonic 层仍可用，仅歌词/随机歌降级）
3. JWT token 存 `config.apiToken`、userId 存 `config.userId`（字段已存在，Jellyfin 同款用法，`server_config.dart:19-20`）

### 3.3 建议配置

```
serverUrl = https://daoliyu.625122961.xyz:10010
localUrl  = http://192.168.31.142:4000
username  = chengsitom
password  = cheng12345677   // 注意全小写，大写 → 401
useLegacyAuth = true        // 强制
allowSelfSignedCertificates = false
serverFamily = daoliyu
```

## 4. JWT 通道 + 歌词接入（核心改动）

### 4.1 新增能力（`subsonic_service.dart`）

```dart
String? _apiJwt;              // 内存 JWT
bool _apiLoginFailed = false; // 一次失败即标记，避免每首歌重复尝试登录

/// POST {activeBaseUrl}/api/auth/login
/// body: {"username": ..., "password": ...}
/// → 200 {token, user{...}}
Future<bool> loginToApi() async {
  if (_apiJwt != null) return true;
  if (_apiLoginFailed) return false;
  try {
    final resp = await _dio.post('$activeBaseUrl/api/auth/login',
        data: {'username': _config!.username, 'password': _config!.password});
    final data = resp.data as Map<String, dynamic>;
    final token = data['token'] as String?;
    if (token == null || token.isEmpty) return false;
    _apiJwt = token;
    _apiLoginFailed = false;
    return true;
  } catch (e) {
    _apiLoginFailed = true;
    Log.w('Daoliyu', 'API login failed: $e');
    return false;
  }
}

/// 带 Bearer 的 GET：401 → 重新 login 一次 → 重试
Future<Map<String, dynamic>?> _apiGet(String path) async {
  if (!await loginToApi()) return null;
  try {
    final resp = await _dio.get('$activeBaseUrl$path',
        options: Options(headers: {'Authorization': 'Bearer $_apiJwt'}));
    return resp.data as Map<String, dynamic>;
  } on DioException catch (e) {
    if (e.response?.statusCode == 401) {
      _apiJwt = null; // 过期，重登
      if (await loginToApi()) {
        final retry = await _dio.get('$activeBaseUrl$path',
            options: Options(headers: {'Authorization': 'Bearer $_apiJwt'}));
        return retry.data as Map<String, dynamic>;
      }
    }
    return null;
  }
}
```

> `_dio` 复用现有实例（`_configureCertificateValidation` 已重建），登录/歌词请求无需独立 Dio。
> 注意：自研层请求跟随 `activeBaseUrl`（LAN↔远程切换时用对应地址）。
>
> **状态重置**（易漏）：
> - `configure()` 时重置 `_apiJwt = null; _apiLoginFailed = false;`——换服务器后旧失败标记/旧 token 不得残留
> - `_setActiveBaseUrl()`（`subsonic_service.dart:87-91`）时重置 `_apiJwt = null`——LAN↔远程切换后 token 需按新地址重新登录

### 4.2 歌词挂载点 = `getLyricsBySongId()` 内部

**这是核心挂载点，一处改动 4 个 UI 入口全部生效**（全屏歌词页 `synced_lyrics_view.dart:303`、迷你歌词 `compact_lyrics_view.dart:109`、锁屏/通知 `player_provider.dart:2088`、车载 `car_mode_screen.dart`）。

```dart
Future<Map<String, dynamic>?> getLyricsBySongId(String songId) async {
  if (_jellyfin != null) { ... 原逻辑 ... }

  // ── 道理鱼优先：自研层歌词 ──────────────────────────────
  if (_config?.serverFamily == 'daoliyu') {
    final apiLyrics = await _getDaoliyuLyrics(songId);
    if (apiLyrics != null) return apiLyrics;
  }

  try {
    final response = await _request('getLyricsBySongId', {'id': songId});
    return response['lyricsList'] as Map<String, dynamic>?;
  } catch (e) {
    return null;
  }
}
```

### 4.3 自研歌词响应转换

`GET /api/tracks/{id}/lyrics` 返回：

```json
{
  "trackId": "trk_...",
  "candidates": [ { "id": "MR", "syncType": "SYNCED",
                    "lyrics": "[00:00.00] 作曲 : ...\n[00:08.62]Let's start...\n[00:08.62]魔法开始显现\n...",
                    "lyricsPlain": "...", "available": true, "default": true } ],
  "preference": { "offsetMs": 0 },
  "effectiveCandidate": { ...同上... },
  "fallback": false
}
```

转换函数：

```dart
/// 取 effectiveCandidate.lyrics（LRC 格式，含中英交错），
/// 转成 Luobo 现有 structuredLyrics 结构（start 毫秒 + value 文本）
Future<Map<String, dynamic>?> _getDaoliyuLyrics(String songId) async {
  final data = await _apiGet('/api/tracks/$songId/lyrics');
  if (data == null) return null;
  final candidate = data['effectiveCandidate'] as Map<String, dynamic>? ??
      ((data['candidates'] as List?)?.isNotEmpty == true
          ? (data['candidates'] as List).first as Map<String, dynamic>
          : null);
  if (candidate == null) return null;
  final lrc = candidate['lyrics'] as String?;
  if (lrc == null || lrc.isEmpty) return null;

  // 解析 LRC → structuredLyrics 行（复用 lrclib_service._buildStructuredLyrics 的解析逻辑）
  final lines = <Map<String, dynamic>>[];
  for (final raw in LineSplitter.split(lrc)) {
    final line = raw.trim();
    final match = RegExp(r'\[(\d+):(\d{2})\.(\d{2,3})\](.*)').firstMatch(line);
    if (match == null) continue;
    final minutes = int.parse(match.group(1)!);
    final seconds = int.parse(match.group(2)!);
    final fracStr = match.group(3)!;
    final text = match.group(4)!.trim();
    if (text.isEmpty) continue;
    final fracMs = fracStr.length == 2 ? int.parse(fracStr) * 10 : int.parse(fracStr);
    final startMs = (minutes * 60 + seconds) * 1000 + fracMs.clamp(0, 999);
    lines.add({'start': startMs, 'value': text});
  }
  if (lines.isEmpty) {
    // 无时间轴 → 纯文本
    return {'lyrics': lrc};
  }
  return {
    'structuredLyrics': [
      {'synced': true, 'line': lines},
    ],
  };
}
```

- 道理鱼 LRC 是**原文/译文交错行**（同时间戳两行），`SyncedLyrics.fromLrc` / UI 解析器均按行渲染，天然支持双语显示
- `preference.offsetMs` 歌词偏移：**本迭代不接**（设置入口暂无对应 UI），保留扩展点

### 4.4 歌词回退链（道理鱼下）

```
离线缓存 → 道理鱼自研歌词（effectiveCandidate.lyrics）→ LRCLIB → 网易云
```

道理鱼无歌词（返回 candidates 空 / 无有效行）时，`getLyricsBySongId` 返回 null → 现有 UI 层回退链自动接管（LRCLIB/网易，`synced_lyrics_view.dart:368-470`）。

## 5. 随机歌适配（决策 A）

### 5.1 调用点（4 处，改 `getRandomSongs()` 一处全部生效）

- `made_for_you_screen.dart:33`（搜索页"为你推荐"）
- `auto_dj_service.dart:195, 295`（AutoDJ 自动续播）
- `library_provider.dart:1110`（音乐库随机）

### 5.2 自研层接口

```
GET /api/tracks/random?limit=N
Authorization: Bearer <JWT>
→ 200 {"count":N,"items":[<track 对象>]}
```

`api_random.body` 实测返回结构（字段与 Song 模型对齐）：

```json
{
  "id": "trk_a25a...", "title": "再见英雄",
  "artist": {"id": "art_...", "name": "王力宏"}, "artistName": "王力宏",
  "album": {"id": "alb_...", "title": "再见英雄"},
  "durationSeconds": 301, "trackNumber": 1, "bitrate": 861,
  "coverArt": "/api/cover?path=...", "genres": ["pop"]
}
```

### 5.3 客户端适配（`subsonic_service.dart:874`）

```dart
Future<List<Song>> getRandomSongs({int size = 20, String? genre}) async {
  if (_jellyfin != null) return _jellyfin!.getRandomSongs(...);
  if (_youtube != null) return _youtube!.getRandomSongs(...);

  // ── 道理鱼：自研层 random ─────────────────────────────
  if (_config?.serverFamily == 'daoliyu') {
    final data = await _apiGet('/api/tracks/random?limit=$size');
    final items = data?['items'] as List<dynamic>? ?? [];
    return items
        .map((e) => _daoliyuTrackToSong(e as Map<String, dynamic>))
        .whereType<Song>()
        .toList();
  }

  try {
    final response = await _request('getRandomSongs', {
      'size': size.toString(),
      if (genre != null) 'genre': genre,
    });
    final songsData = response['randomSongs']?['song'];
    ...
  }
}
```

### 5.4 字段映射 `_daoliyuTrackToSong`

| Song 字段 | 自研层来源 | 备注 |
|---|---|---|
| id | `id` | 直接透传 |
| title | `title` | |
| album / albumId | `album.title` / `album.id` | 嵌套对象 |
| artist / artistId | `artist.name` / `artist.id` | 或 `artistName/artistId` |
| track | `trackNumber` | |
| genre | `genres?.first` | 数组取首个（或 join） |
| coverArt | `coverArt` | ⚠️ 是 `/api/cover?path=...` 路径式，与 Subsonic `album:alb_...` 不同（见 §8） |
| duration | `durationSeconds` | |
| bitRate | `bitrate` | |
| suffix | `detectedContainer` / `fileFormat` | flac 等 |

> 封面的坑见 §8：自研层 coverArt 是 `/api/cover?path=...` 形式，**不能直接喂 `getCoverArtUrl()`**（那会拼成 `/rest/getCoverArt?id=/api/cover?...`）。随机歌列表如需显示封面，需额外处理（可选）。

## 6. 流派页本地聚合（决策 B）

道理鱼下 `getGenres()` 返回空列表（`subsonic_service.dart:1004` 对 daoliyu 提前 return []），GenresScreen 改为**本地聚合**：

`genres_screen.dart:28-40` 的 `_loadGenres()` 增加道理鱼分支：

```dart
Future<void> _loadGenres() async {
  try {
    final libraryProvider = Provider.of<LibraryProvider>(context, listen: false);
    await libraryProvider.loadGenres();
    var genres = libraryProvider.richGenres;

    // 道理鱼：getGenres 不支持 → 从已缓存歌曲本地聚合（复用 Library Tab 逻辑）
    if (genres.isEmpty && libraryProvider.cachedAllSongs.isNotEmpty) {
      final genreMap = <String, int>{};
      for (final s in libraryProvider.cachedAllSongs) {
        final g = (s.genre ?? 'Unknown').trim();
        if (g.isEmpty) continue;
        genreMap[g] = (genreMap[g] ?? 0) + 1;
      }
      // Genre 构造器三参必填（genre.dart:6-10）；本地聚合无专辑维度，albumCount 置 0
      genres = genreMap.entries
          .map((e) => Genre(
                value: e.key,
                songCount: e.value,
                albumCount: 0,
              ))
          .toList()
        ..sort((a, b) => a.value.compareTo(b.value));
    }
    ...
  }
}
```

> 依赖前提：进入 GenresScreen 前 `cachedAllSongs` 已加载（音乐库浏览过歌曲后即有）。若缓存为空则显示空态，可接受。

## 7. Jukebox（点唱机）整体删除

**决策（2026-08-07 用户确认）**：Jukebox 功能整体删除，对 navidrome 和道理鱼用户都一样（无使用场景），跟随本期需求一起做。**不再做"不支持检测补丁"**，而是移除全部代码与 UI 入口。

### 7.1 删除范围

| 类型 | 文件 | 内容 |
|---|---|---|
| 删除文件 | `lib/services/jukebox_service.dart` | 整个文件（JukeboxService/JukeboxStatus） |
| 删除文件 | `lib/screens/jukebox_screen.dart` | 整个文件（控制器页面） |
| 服务方法 | `lib/services/subsonic_service.dart:1070-1125` | `jukeboxControl` + 全部 `jukebox*()` 便捷方法 |
| export | `lib/services/services.dart:23` | `export 'jukebox_service.dart'` |
| Provider | `lib/main.dart:195, 234-235, 302, 310` | `JukeboxService` 实例化/initialize/Provider 注册 |
| UI 入口 | `lib/screens/settings_server_tab.dart:9, 14, 79-80, 234-300` | import + `_buildJukeboxSection()` + section 挂载 |
| 歌曲菜单 | `lib/widgets/song_tile.dart:10, 584-628` | "播放在点唱机 / 添加到点唱机队列"两个菜单项 |
| 机制说明 | `lib/screens/settings_mechanism_screen.dart:318-320` | 点唱机机制说明文案 |
| l10n | `lib/l10n/app_*.arb`（22 个语言） | `sectionJukebox/jukeboxMode/.../playOnJukebox/addToJukeboxQueue` 等全部 jukebox key |
| 测试 | `test/memory/memory_leak_test.dart:29`、`test/widgets/song_tile_test.dart:11,32`、`test/test_helpers.dart:62` | JukeboxService 依赖移除 |
| 文档 | `docs/客户端接口与文档总览.md:13,76` | Subsonic 端点表 `jukeboxControl` 行、协议描述 Jukebox 字样 |

> l10n：删 `.arb` 源文件 key 后需重新生成 `app_localizations_*.dart`（`flutter gen-l10n`），或手动同步删除生成的 getter。
> Jellyfin 无 jukebox 引用（已验证），无需处理。

### 7.2 删除后效果

- 设置页不再有"点唱机模式"开关与控制器入口
- 歌曲长按菜单不再有"播放在点唱机 / 添加到点唱机队列"
- 代码库无 `JukeboxService`/`jukeboxControl` 残留，`subsonic_service.dart` 端点面干净
- 道理鱼无需任何适配（该端点直接随代码移除）；Navidrome 同样移除（用户无此场景）

### 7.3 注意

- `settings_mechanism_screen.dart` 的机制说明同时提及"网络电台"，只删点唱机部分，保留电台
- 删除 `_buildJukeboxSection` 后，`settings_server_tab.dart` 的 `sectionJukebox` l10n 引用一并清除，避免编译错误
- song_tile.dart 删除 Builder 块后，`JukeboxService`/`SubsonicService` 在该块的 import 若不再使用需一并清理

## 8. 封面注意事项（坑）

道理鱼封面有**两种取法**，语义不同：

| 方式 | URL | 缓存 | 用途 |
|---|---|---|---|
| Subsonic `getCoverArt` | `/rest/getCoverArt?id=album:alb_xxx&size=` | max-age 86400 | 现有 `getCoverArtUrl()` 已用 ✅ 列表/播放页正常 |
| 自研层 coverArt | `/api/cover?path=...` | max-age=0 | 歌词/random 等自研响应里带的是这个 |

**结论**：正常浏览走现有 Subsonic 封面（已兼容）。只有自研层数据（random 歌单）里拿到的是路径式 coverArt——本迭代**不强求**其封面显示（随机歌列表可先不显示封面或用占位图），避免引入 ticket/路径式封面的额外逻辑。若体验需要再二期处理。

## 9. 改动清单汇总

> 实施状态：✅ 全部完成（2026-08-07）。编码中发现并补充：PlayerProvider 深度耦合 Jukebox（构造参数/轮询/播放控制分支/dispose），一并移除；测试文件实际有 4 个（含 player_provider_test.dart）。

| # | 文件 | 改动 | 状态 |
|---|---|---|---|
| 1 | `lib/screens/login_screen.dart` | `_ServerFamilyToggle` 加"道理鱼"chips；选 daoliyu 强制 `_useLegacyAuth=true`（开关禁用） | ✅ |
| 2 | `lib/providers/auth_provider.dart` | daoliyu 分支：ping 成功后 `loginToApi()`（失败不阻塞）→ token/userId 存 config | ✅ |
| 3 | `lib/services/subsonic_service.dart` | `_apiJwt/_apiLoginFailed/_apiUserId` 字段；`isDaoliyu`/`apiJwtOrNull`/`apiUserId` getter；`loginToApi()`；`_apiGet()`（Bearer + 401 重登重试）；`getLyricsBySongId()` 道理鱼分支；`_getDaoliyuLyrics()`（LRC→structuredLyrics）；`getRandomSongs()` 道理鱼分支；`_daoliyuTrackToSong()` 映射；configure/_setActiveBaseUrl 重置 JWT | ✅ |
| 4 | `lib/screens/genres_screen.dart` | `_loadGenres()` 道理鱼本地聚合分支 | ✅ |
| 5 | `lib/services/jukebox_service.dart`、`lib/screens/jukebox_screen.dart` | **整文件删除**（§7） | ✅ |
| 6 | `lib/services/subsonic_service.dart` | 删除 `jukeboxControl` 及 `jukebox*()` 方法（§7） | ✅ |
| 7 | `lib/services/services.dart` | 删除 `export 'jukebox_service.dart'`（§7） | ✅ |
| 8 | `lib/main.dart` | 删除 JukeboxService 实例化/initialize/Provider（§7） | ✅ |
| 9 | `lib/screens/settings_server_tab.dart` | 删除 `_buildJukeboxSection()` 及 section 挂载/import（§7） | ✅ |
| 10 | `lib/widgets/song_tile.dart` | 删除"播放在点唱机/添加到点唱机队列"菜单项及 import（§7） | ✅ |
| 11 | `lib/screens/settings_mechanism_screen.dart` | 删除点唱机机制说明（保留电台）（§7） | ✅ |
| 12 | `lib/providers/player_provider.dart` | 删除 JukeboxService 依赖：import/字段/构造参数/listener/轮询区块/playSong/play/pause/seek/skipNext/skipPrevious/_prepareCurrentSong/dispose 分支（§7 补充） | ✅ |
| 13 | `lib/l10n/app_*.arb` + `app_localizations_*.dart` | 删除全部 jukebox key（22 语言）→ `flutter gen-l10n` 重生成 | ✅ |
| 14 | `test/`（memory_leak / song_tile / test_helpers / **player_provider**） | 移除 JukeboxService 依赖（§7） | ✅ |

**不改**：synced_lyrics_view / compact_lyrics_view / car_mode_screen（歌词挂载点在 service 层自动生效）；transcoding_service / resolveActiveUrl（已兼容）。

## 10. 测试计划

1. **登录**：本地内网 + 远程 HTTPS 各登录一次，验证 ping OK + JWT 获取
2. **歌词**：播放一首有歌词的歌 → 全屏歌词页显示中英双语；播一首无歌词的 → 回退 LRCLIB/网易
3. **随机歌**：搜索页"为你推荐"、音乐库随机、AutoDJ 各自验证出歌
4. **流派**：搜索页流派页显示本地聚合结果；音乐库 Genres Tab 正常
5. **Jukebox 删除**：设置页无点唱机开关/入口；歌曲长按菜单无点唱机项；`flutter analyze` 无 jukebox 残留引用
6. **电台**：电台页显示不支持提示
7. **回归**：Navidrome 登录验证不受影响（改动均为 daoliyu 分支，非 daoliyu 走原逻辑）
8. **scrobble**：真机播完一首，确认道理鱼播放计数增长

## 11. 风险与待验证

| 项 | 风险 | 备注 |
|---|---|---|
| JWT 有效期 | 服务端控制，过期需重登 | `_apiGet` 已有 401 重登重试 |
| 密码大小写 | 大写 → 401 INVALID_CREDENTIALS | 登录页提示用户注意 |
| 自研层 random 返回的 coverArt 是路径式 | 随机歌列表封面可能不显示 | 本迭代不强求，二期再处理 |
| 歌词 offsetMs | 用户设置的歌词偏移未接入 | 客户端无对应 UI，保留扩展点 |
| 远程/内网切换 | 自研层请求跟随 activeBaseUrl | 切换后 loginToApi 需重取 JWT（`_apiJwt` 按需重置） |
| 道理鱼版本升级 | 自研 API 可能变动（当前 v0.3.4/API 1.0.0） | 文档素材已冻结，升级后再核 |

---

*文档基于道理鱼接口文档 v3.1（2026-08-07 实测抓包）+ Luobo master `c889513` 源码对照撰写。所有适配均为 Luobo 客户端改动，道理鱼服务端零改动。*
