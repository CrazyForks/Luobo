# Luobo 客户端接口与文档总览

> 状态：整理完成（2026-08-07）
> 范围：Luobo（原 Musly）Flutter 客户端对外发起的所有网络接口清单，逐条对照 Navidrome 服务端实现；并索引 `docs/` 目录全部技术文档。
> 依据：`lib/services/` 各服务源码（当前 master `c889513` v1.1.12）+ `docs/navidrome-服务端技术文档.md` + `docs/ultrasonic客户端技术文档.md`

## 1. 接口全景

Luobo 客户端是一个多协议音乐播放器，对外接口按"服务器族 + 外部依赖"划分：

| # | 协议 / 对端 | 客户端实现 | 用途 | 说明 |
|---|---|---|---|---|
| 1 | **Subsonic / OpenSubsonic** | `lib/services/subsonic_service.dart` | 主服务器（Navidrome 等） | 全部库浏览/播放/收藏/歌单/歌词/电台 |
| 2 | **道理鱼 /api 层（JWT）** | `lib/services/subsonic_service.dart` | 道理鱼服务器族补充 | `serverFamily='daoliyu'`：歌词/随机歌走自研层（见 `docs/道理鱼接入技术方案.md`） |
| 2 | **Jellyfin / Emby** | `lib/services/jellyfin_service.dart` | 服务器族 `jellyfin` | 同一套 UI 抽象之上的 Jellyfin REST 适配 |
| 3 | **LRCLIB** | `lib/services/lrclib_service.dart` | 歌词回退源 ① | `https://lrclib.net/api` |
| 4 | **网易云音乐** | `lib/services/netease_lyrics_service.dart` | 歌词回退源 ② | `https://music.163.com/api`（中文歌覆盖补充） |
| 5 | **YouTube Music** | `lib/services/youtube_service.dart` | YouTube 模式 | 经 `youtube_explode_dart` 库（manifest/搜索/歌单/相关视频） |
| 6 | **DeepSeek（OpenAI 兼容）** | `lib/services/ai_playlist_service.dart` | AI 歌单 + 知识库打标 | `POST {base}/chat/completions`，默认 `https://api.deepseek.com` |
| 7 | **GitHub Releases** | `lib/services/update_service.dart` | 更新检查 | `api.github.com/repos/chengsitom/Luobo/releases/latest` |
| 8 | **UPnP / DLNA** | `lib/services/upnp_service.dart` | 局域网投送 | SSDP 发现 + SOAP AVTransport/RenderingControl |
| 9 | **Chromecast** | `lib/services/cast_service.dart` | 局域网投送 | Google Cast SDK（`google_cast` 插件） |
| 10 | 离线下载 | `lib/services/offline_service.dart` | 本地缓存 | 复用 `getStreamUrl`/封面 URL 经 Dio 落盘 |
| 11 | 诊断打点 | `lib/services/diagnostics/network_interceptor.dart` | 网络指标 | 所有 Dio 请求耗时/状态采集 + URL 脱敏 |

> 服务器族切换：`ServerConfig.serverFamily`（`subsonic` 默认 / `jellyfin`），见 `lib/models/server_config.dart:36`。`SubsonicService.configure()` 按族实例化对应的适配服务，对外暴露统一的 `SubsonicService` 方法面（`lib/services/subsonic_service.dart:60-82`）。

---

## 2. Subsonic API（Navidrome 主对接）

### 2.1 认证与公共参数

- 每个请求固定携带：`u=<用户名> & v=1.16.1 & c=Musly & f=json`（`_getAuthParams`，`subsonic_service.dart:355-376`）；
- **token+salt（默认）**：`t = md5(password + s)`，`s` 为随机 8 位（UUID v4 前 8 字符）；与 Navidrome `validateCredentials` token 分支算法一致（`navidrome-服务端技术文档.md` §4.1）；
- **明文（legacy）**：`useLegacyAuth=true` 时改带 `p=<密码>`；
- **封面 URL 专用稳定 salt**：`_stableAuthParams` 固定 `s=musly_stable`，保证同一服务器/账号的封面 URL 长期不变，命中 Navidrome 磁盘缩略图缓存（`subsonic_service.dart:378-401`，对照 §5.3 getCoverArt 缓存键规则）；
- 额外公共参数：`musicFolderId`（配置了多库时取第一个，`subsonic_service.dart:411-413`）。

### 2.2 LAN/远程双地址

- `resolveActiveUrl({forceProbe})`：先探测局域网 URL（3s 超时 `ping`），不可达则回退远程地址；上次会话停在远程时冷启跳过阻塞探测、后台静默切换（`subsonic_service.dart:104-210`）；
- 探测结果持久化到 SharedPreferences（`last_active_base_url`）；
- 生效地址变化通过 `onActiveUrlChanged` 通知转码服务刷新 LAN 覆盖（`subsonic_service.dart:38-41`）。

### 2.3 端点全表

端点均挂 `{activeBaseUrl}/rest/<endpoint>?<公共参数>&<业务参数>`。服务端实现列对照 `docs/navidrome-服务端技术文档.md` §5.2 分组。

| # | 端点 | 客户端方法（行号） | 业务参数 | 服务端实现（navidrome） |
|---|---|---|---|---|
| 1 | `ping` | `ping()` 482 / `pingWithError()` 491 | — | system 组；`type="navidrome"`、`serverVersion` 透传 |
| 2 | `getMusicFolders` | `getMusicFolders()` 506 | — | browsing 组 |
| 3 | `getArtists` | `getArtists()` 590 | — | browsing 组（`artists.index[].artist`） |
| 4 | `getArtist` | `getArtist()` 617 / `getArtistAlbums()` 667 | `id` | browsing 组（`artist.album[]`） |
| 5 | `getAlbum` | `getAlbum()` 647 / `getAlbumSongs()` 654 | `id` | browsing 组（`album.song[]`） |
| 6 | `getAlbumList2` | `getAlbumList()` 622 | `type`(recent/newest/random/alphabeticalByName…)、`size`、`offset` | albumLists 组；type 映射见 §5.3 表 |
| 7 | `getAlbumList2` (byGenre) | `getAlbumsByGenre()` 1042 | `type=byGenre`、`genre`、`size`、`offset` | albumLists 组 `byGenre` |
| 8 | `getPlaylists` | `getPlaylists()` 685 | — | playlists 组 |
| 9 | `getPlaylist` | `getPlaylist()` 698 | `id` | playlists 组 |
| 10 | `createPlaylist` | `createPlaylist()` 705 | `name`、`comment`、`songId`（重复参数拼接） | playlists 组；**songId 必须重复键**（Navidrome 不识别 `songId[i]`，注释 720-726） |
| 11 | `updatePlaylist` | `updatePlaylist()` 748 | `playlistId`、`name`、`comment`、`songIdToAdd`（重复）、`songIndexToRemove`（重复） | playlists 组 |
| 12 | `deletePlaylist` | `deletePlaylist()` 800 | `id` | playlists 组 |
| 13 | `search3` | `search()` 812 | `query`、`artistCount`、`albumCount`、`songCount` | searching 组；FTS5/bm25，CJK 自动 LIKE（§5.3） |
| 14 | `getRandomSongs` | `getRandomSongs()` 874 | `size`、`genre` | albumLists 组 |
| 15 | `star` | `star()` 892 | `id` / `albumId` / `artistId` | mediaAnnotation 组 → `annotation` 表 |
| 16 | `unstar` | `unstar()` 908 | 同上 | mediaAnnotation 组 |
| 17 | `setRating` | `setRating()` 924 | `id`、`rating`(0-5) | mediaAnnotation 组；重算 `average_rating` |
| 18 | `getStarred2` | `getStarred()` 931 | — | mediaAnnotation 组（`starred2.artist/album/song`） |
| 19 | `scrobble` | `scrobble()` 955 | `id`、`submission`(true/false) | mediaAnnotation 组 → PlayTracker 会话 + 外部上报 |
| 20 | `getLyrics` | `getLyrics()` 967 | `artist`、`title` | mediaRetrieval 组；纯文本（兼容旧客户端） |
| 21 | `getLyricsBySongId` | `getLyricsBySongId()` 989 | `id` | mediaRetrieval 组；`lyricsList` structuredLyrics |
| 22 | `getGenres` | `getGenres()` 1004 | — | browsing 组 |
| 23 | `getSongsByGenre` | `getSongsByGenre()` 1019 | `genre`、`count`、`offset` | albumLists 组 |
| 24 | `getSimilarSongs2` | `getSimilarSongs()` 1176 | `id`、`count` | browsing 组；失败回退 `getSimilarSongs`（1193） |
| 25 | `getTopSongs` | `getArtistTopSongs()` 1208 | `artist`(名称)、`count` | browsing 组；失败回退 getArtist+getAlbumSongs 本地拼装（1233-1246） |
| 26 | `getInternetRadioStations` | 1127 | — | radio 组 |
| 27 | `createInternetRadioStation` | 1143 | `name`、`streamUrl`、`homepageUrl` | radio 组 |
| 28 | `updateInternetRadioStation` | 1155 | `id`、`name`、`streamUrl`、`homepageUrl` | radio 组 |
| 29 | `deleteInternetRadioStation` | 1172 | `id` | radio 组 |
| 30 | `stream` | `getStreamUrl()` 571 | `id`、`maxBitRate`、`format`、**`estimateContentLength=true`**（固定） | mediaRetrieval 组；转码决策 DirectPlay/ffmpeg，转码流估算 Content-Length（§6） |
| 31 | `getCoverArt` | `getCoverArtUrl()` 529 | `id`、`size`(默认 600)、稳定 salt | artwork 组；缓存键含 size，10 年强缓存（§5.3） |

> **Jukebox 已删除（2026-08-07）**：`jukeboxControl` 及全部 `jukebox*()` 方法随点唱机功能整体移除（无使用场景，见 `docs/道理鱼接入技术方案.md` §7）。`jukebox_service.dart`、`jukebox_screen.dart` 已删除。

### 2.5 道理鱼适配端点（`serverFamily='daoliyu'`，2026-08-07 新增）

| 客户端方法 | 道理鱼自研层调用 | 说明 |
|---|---|---|
| `getLyricsBySongId()` | `GET /api/tracks/{id}/lyrics` | 取 `effectiveCandidate.lyrics`（LRC 中英），转 structuredLyrics；失败回退 LRCLIB/网易 |
| `getRandomSongs()` | `GET /api/tracks/random?limit=` | items 映射 Song（4 处调用点生效） |
| `getGenres()` | — | 返回空，GenresScreen 本地聚合 |
| JWT 登录 | `POST /api/auth/login` | token 存 `ServerConfig.apiToken`；401 重登重试 |
| `getAudiobooks()` | `GET /api/library/audiobooks?limit=100` | 有声书列表（items/total）；**道理鱼有声书在 /Music2 源，不在 Subsonic 索引**（见 `docs/有声书接入技术方案.md` §3） |
| `getAudiobookChapters()` | `GET /api/library/audiobooks/{abk_id}/episodes?limit=&skip=` | 章节分页（chapters + count）；⚠️ 必须带 `library/` 前缀；播放走 Subsonic `/rest/stream?id=<abe_>&format=raw`（章节 id 直接喂，明文认证） |

### 2.4 错误处理约定（`_request`，425-480）

- 超时 → "Connection timed out. Check your server URL."；
- 连接失败含 SSL 关键字 → 提示开启自签名证书；`401/403` → 用户名或密码错误；`404` → URL 路径错误；`>=500` → 服务端错误；
- `subsonic-response.status != "ok"` → 抛 `error.message`。

---

## 3. 歌词三级回退链

客户端歌词加载顺序（`subsonic_service.dart` + 歌词服务编排）：

1. **离线歌词缓存**（本地）→ 命中即返回；
2. **道理鱼自研歌词**（`serverFamily='daoliyu'` 时优先，`getLyricsBySongId` 内部）：`GET /api/tracks/{id}/lyrics` 取 `effectiveCandidate.lyrics`（LRC 含中英交错）→ 转 structuredLyrics；
3. **Subsonic `getLyricsBySongId`**（Navidrome structuredLyrics，含 enhanced cue）；
4. **LRCLIB**：`GET https://lrclib.net/api/get?artist_name=&track_name=&duration=`（`lrclib_service.dart:32-38`），syncedLyrics 优先，否则 plainLyrics，返回 Subsonic 兼容的 `structuredLyrics` / `{value}` 结构；
5. **网易云音乐**：`POST https://music.163.com/api/search/get`（`s=<artist title>&type=1&limit=5&offset=0`，`netease_lyrics_service.dart:75-79`）→ `GET /api/song/lyric?id=&lv=1&tv=1`（125-131），LRC 解析后同样转 Subsonic structured 结构；Referer/UA 伪装浏览器头。

> LRCLIB 与网易云回退开关：`getLrcLibFallback` / `getNeteaseFallback`（`storage_service.dart:157-175`）。

---

## 4. Jellyfin API（`serverFamily=jellyfin`）

认证：`Authorization: MediaBrowser Client="Musly", Device="Musly", DeviceId=…, Version="1.0.0", Token=…`（`jellyfin_service.dart:20-29`）；登录后 Token/UserId 存回 `ServerConfig.apiToken/userId`。

| # | 方法与路径 | 客户端方法 | 说明 |
|---|---|---|---|
| 1 | `GET /System/Info/Public` | `pingWithError()` 82 | 探测 ProductName→Emby/Jellyfin、Version |
| 2 | `POST /Users/AuthenticateByName` | `authenticate()` 106 | 体 `{Username, Pw}` → AccessToken/User.Id |
| 3 | `GET /Items/{id}/Images/Primary?fillHeight&fillWidth&quality=90&api_key` | `getCoverArtUrl()` 128 | 封面（默认 size=300） |
| 4 | `GET /Audio/{id}/universal?audioCodec=…&container=…&transcodingContainer=mp4&transcodingProtocol=http&enableDirectPlay&enableDirectStream&enableTranscoding&userId&deviceId&maxStreamingBitrate&api_key` | `getStreamUrl()` 135 | 通用音频流（Jellyfin 兼容端点） |
| 5 | `GET /Users/{userId}/Items` | 专辑/歌曲/搜索/收藏/随机/分类 等 | 复用查询：`IncludeItemTypes`(MusicAlbum/Audio/Playlist/MusicArtist)、`ParentId`、`ArtistIds`、`SearchTerm`、`Genres`、`Filters=IsFavorite`、`SortBy`(SortName/DateCreated/PlayCount/DatePlayed/Random/ProductionYear/IndexNumber/PlayCount)、`Limit/StartIndex`、`Fields` |
| 6 | `GET /Artists?userId&Recursive&SortBy&Limit&Fields=AlbumCount` | `getArtists()` 331 | 艺人（上限 1000） |
| 7 | `GET /Genres?userId&IncludeItemTypes=Audio` | `getGenres()` 580 | 流派 |
| 8 | `GET /Users/{userId}/Items/{id}` | `getAlbum()`/`getPlaylist()` 元数据 | 单体详情 |
| 9 | `GET /Playlists/{id}/Items?userId&Fields` | `getPlaylist()` 412 | 歌单曲目 |
| 10 | `POST /Playlists` | `createPlaylist()` 713 | 体 `{Name, UserId, MediaType:Audio, Ids}` |
| 11 | `POST /Playlists/{id}/Items?Ids=…&userId` | `addSongsToPlaylist()` 739 | 追加曲目 |
| 12 | `DELETE /Playlists/{id}/Items?EntryIds=` | `removeSongFromPlaylist()` 752 | 移除曲目 |
| 13 | `DELETE /Items/{id}` | `deletePlaylist()` 731 | 删除歌单 |
| 14 | `POST /Users/{userId}/FavoriteItems/{itemId}` / `DELETE` 同路径 | `star()`/`unstar()` 531/546 | 收藏 |
| 15 | `POST /Sessions/Playing`（start）/ `POST /Sessions/Playing/Stopped`（finish） | `scrobble()` 561 | 播放上报 |
| 16 | `GET /Items/{id}/Similar?userId&Limit&Fields` | `getSimilarSongs()` 664 | 相似歌曲 |
| 17 | `GET /Audio/{id}/Lyrics` | `getLyrics()` 763 | `Lyrics[].Start(ticks)/Text` → 转 Subsonic structured |

> Jellyfin `getAlbumList` type 映射：alphabeticalByName→SortName、newest→DateCreated、frequent→PlayCount、recent→DatePlayed、starred→IsFavorite 过滤、其他→Random（`jellyfin_service.dart:220-242`）。

---

## 5. YouTube Music 模式

`SubsonicService` 暴露统一方法面，内部路由到 `YoutubeService`（`youtube_service.dart`）：

- **连通性**：`GET https://music.youtube.com/`（`pingWithError`，117）；
- **封面**：`https://img.youtube.com/vi/{videoId}/mqdefault.jpg`（135）；
- **音频流**：`youtube_explode_dart` `videos.streamsClient.getManifest`（mweb → 默认 android → androidMusic 客户端依次尝试），自实现 `_YoutubeStreamAudioSource` 用 `package:http` 代理 Range 请求规避 ExoPlayer 403（22-92）；流 URL 缓存 5h（`_streamCacheMaxAge`，101）；
- **搜索/随机**：`yt.search.search(query, filter: video)`；
- **歌单**：`yt.playlists.get` / `getVideos`（取前 200）；
- **相关视频**：`yt.videos.getRelatedVideos`；
- **艺人热门**：`yt.channels.get` + search；
- **不支持项**：创建/编辑/删除歌单、收藏、scrobble 均为 no-op 或抛 `UnsupportedError`（匿名模式，237-263、294-299）。

> 注：`configure()` 中 YouTube 分支目前被注释（`subsonic_service.dart:68-70`），YouTube 模式处于保留但未启用状态。

---

## 6. DeepSeek AI（AI 歌单 + 知识库打标）

`lib/services/ai_playlist_service.dart`：

- **端点**：`POST {aiBaseUrl}/chat/completions`，默认 `https://api.deepseek.com`（`storage_service.dart:216`）；`Authorization: Bearer <key>`；
- **模型**：默认 `deepseek-v4-flash`（`storage_service.dart:236`）；**`thinking: {type: disabled}`** 显式关闭思考模式（V4 默认开启，会烧光 max_tokens 导致 content 空，见记忆 `reference_deepseek_docs.md`）；
- **批量打标**（`generateKnowledge`）：50 首/批，`temperature=0.3, max_tokens=4000`，输出 `序号|标签1,标签2,…`；连续 3 批解析失败熔断（`_maxConsecutiveFailures=3`，29）；每批落盘支持中断续跑；
- **AI 歌单**（`generatePlaylist`）：`temperature=0.7, max_tokens=2000, response_format={type: json_object}`，输入含用户画像（常听歌手/风格/完播/跳过/高评分/时段）+ 候选歌曲标签列表，返回歌曲 ID JSON 数组；
- 结果经 `SongKnowledgeCache` 本地持久化（`song_knowledge.json` + `knowledge_progress.json`）。

---

## 7. GitHub Releases（更新检查）

`lib/services/update_service.dart`：

- `GET https://api.github.com/repos/chengsitom/Luobo/releases/latest`（`Accept: application/vnd.github+json`、`X-GitHub-Api-Version: 2022-11-28`）；
- 语义化比较 `tag_name`（去 v 前缀）与 `currentVersion=1.1.12`，新版本返回 ReleaseInfo（版本/正文/资产下载地址）；正文 Markdown 剥离用于展示（`stripMarkdown`，106）。

---

## 8. 局域网投送（UPnP / Chromecast）

### 8.1 UPnP / DLNA（`lib/services/upnp_service.dart`）

- **发现**：SSDP `M-SEARCH * HTTP/1.1` 组播（239.255.255.250:1900），解析响应 `LOCATION`（108）；
- **设备描述**：`GET <location>` 抓 XML，筛选 `AVTransport:1` 与 `RenderingControl:1` 服务（205-228）；
- **控制**：`POST <controlURL>`，`SOAPAction: "urn:schemas-upnp-org:service:AVTransport:1#<action>"`，动作含 Play/Pause/Stop/Seek/SetAVTransportURI 等（550-683）。

### 8.2 Chromecast（`lib/services/cast_service.dart`）

- 基于 `google_cast` 插件；`_mimeTypeFromUrl` 按扩展名映射 audio/flac、ogg/opus、mp4、mpeg 等（297-307）；经 `mediaLoad` 投送 URL（234-266）。

---

## 9. 离线下载

`lib/services/offline_service.dart`：

- 音频：`Dio.download(subsonicService.getStreamUrl(song.id), 本地文件)`（162-167）；
- 封面：`Dio.download(getCoverArtUrl(song.coverArt), 本地文件)`（189-190）；
- 本地播放走 `LocalMusicService`，不依赖服务器。

---

## 10. 网络诊断与日志

- **统一拦截器** `networkMetricsInterceptor`（`diagnostics/network_interceptor.dart:26-87`）：所有 Dio 请求记录 `→/← method url status ms`，聚合进 `MetricsCollector.networkRequest` 并落 `requestTiming` 事件；
- **URL 脱敏** `sanitizeQueryUrl`：Subsonic 认证参数 `p/t/s` 打码为 `***` 后才写日志（8-19）；
- 客户端日志前缀 `[Musly] →/← …`（`subsonic_service.dart:55-58` 的 `logLine`），可与 Navidrome 服务端日志对照排查（见 `navidrome-服务端技术文档.md` §14）。

---

## 11. docs/ 文档索引

| 文档 | 状态（2026-08-07） | 内容定位 |
|---|---|---|
| `navidrome-服务端技术文档.md` | 源码研究完成 | **服务端侧**：Navidrome 架构、Subsonic API 实现、认证/转码/扫描/歌词/播放统计，含客户端对照表（§15） |
| `ultrasonic客户端技术文档.md` | 源码研究完成 | **上游客户端** Ultrasonic 源码解读（Luobo 的参考实现） |
| `局域网转码策略技术方案.md` | 已实施提交（v1.1.10 b1adba3，待真机验证） | 转码规则：LAN 不转码、公网按设置 |
| `封面图片缓存优化技术方案.md` | 已实施（2026-08-06，待真机验证） | 封面缓存 P0-P5（连接复用/语义化 key/内存预热/登出保留/512MB 守护） |
| `图片缓存与播放性能优化技术文档.md` | 部分实施 | 早期图片/播放性能优化记录 |
| `性能监控与日志系统技术文档.md` | 部分实施 | DiagnosticsService/MetricsCollector 设计 |
| `首页重构技术方案.md` | 实施完成上线（HomeV2Screen 挂载，待真机验证）；2026-09-23 追加 §5.6：「为你制作」4 卡封面改用流沙流体背景（**实现细节已独立成文**，见下行）；2026-09-23 追加 §8-13：每日推荐去同质化（**实现细节已独立成文**，见下行） | 首页探索/最近播放/常听 + 流沙流体背景的**首页接入点** |
| `流沙流体背景技术文档.md` | **已抽离为独立 pub 包**（2026-09-23；`packages/fluid_mesh_background/`，MIT 占位，暂不上架；App 以 `path:` 依赖；例测在 `example/test/`，待真机验证观感与帧率）；**已接入两处：首页「为你制作」4 卡（§5.6）+ 二级页 hero（`SongListScreen`，§5.9）** | **独立功能文档**：`FluidBackground` / `FluidBackgroundStyle` / `FluidBackgroundScope` / `FluidClock` / `FluidPalette` / `FluidBackgroundEvents` 公共 API、着色器原理与 uniform 契约、单调时钟等 3 条硬约束、每项独立动画（seed）、调参、性能红线、测试与验证方法、移植清单 |
| `每日推荐探索配额与冷却技术方案.md` | **已实施**（2026-09-23；单测 19+6 绿，analyze 零 error，待真机验证） | **推荐算法独立功能文档**：每日推荐「今日推荐」的去同质化改造——问题根因（一路召回无配额 / 正反馈棘轮 / 分段选歌绕过去重上限）、大厂对标结论、熟悉槽 20 + 探索槽 10 配额、`RecommendedHistoryStore` 跨天冷却、收藏变化即时失效（`feedRevision`）、退化与安全阀、二期清单 |
| `音乐库四Tab平级横滑技术方案.md` | v1.3 实施完成（analyze 无 error、单测 102 绿，待真机验证） | TabBar+TabBarView |
| `音乐库艺术家页改版技术方案.md` | 实施完成 v1.2 | 纯客户端封面/本地统计置顶/方形网格 |
| `音乐库刷新功能改造技术方案.md` | 已实施 v1.3 | 下拉刷新/增量刷新 |
| `设置页重构技术方案.md` | 一期已实施 P0-P2；**二期 §9 设计已定稿待开工（2026-09-23，对标飞牛音乐 + 箭头音乐）** | 设置页结构重构 + 二期：职责分离 / 服务器管理流程重排 / 公共范式统一 |
| `设置页机制说明功能技术方案.md` | v0.2 已确认（待「开始 code」放行） | 设置项机制说明入口 |
| `歌词页视觉对齐苹果设计技术方案.md` | 已实施（94af7b4 v1.1.11） | 歌词页视觉对齐 Apple Music |
| `车机媒体会话与歌词推送技术方案.md` | **P0/P1 已实施（2026-09-23；analyze 零 error、`flutter test` 182 绿/5 跳过、Kotlin 编译通过，待真机/车机验证）** | **车机侧独立功能文档**：蓝牙/CarLife 下三条链路（媒体信息 / 上一曲下一曲 / 歌词副标题）——歌词断链根因（原生链路完整、Dart 侧被上游 `1edeebb` 删除）、按键四跳链路的两处静默丢弃、P0 歌词推送 + P1 命令缓冲 + logcat 定位法、`audio_service` 迁移与 CarLife SDK 两个二期候选 |
| `Apple Music全屏播放页样式与交互参考.md` | 需求澄清完成，待实施方案 | 苹果全屏播放页 UI 参考 |
| `视觉与交互对标飞牛音乐技术方案.md` | **方案待拍板（2026-09-23，26 张截图含深色模式）；流体渐变背景已实现（见 `流沙流体背景技术文档.md`）** | 对标飞牛音乐：玻璃视觉配方 + 彩色渐变流体动态背景 + 3 种页面骨架 / 4 种列表呈现 / 5 种浮层范式 / 表单范式 / 播放条覆盖式布局 + 深色模式规范 + 交互设计原则 10 条 + 歌曲信息只读页 + 性能红线（勿做「处处毛玻璃」） |
| `ai-knowledge-generation-design.md` | 已实施（含共享缓存修复） | AI 知识库生成设计 |
| `README.md` | 活跃维护 | 项目总览、功能、构建说明 |

---

## 12. 客户端 ↔ Navidrome 服务端对照要点（承接 navidrome 文档 §15）

| 客户端行为 | 服务端对应 | 备注 |
|---|---|---|
| `t/s` token+salt（随机 8 位） | `validateCredentials` token 分支 `md5(password+salt)` | salt 不校验格式 |
| 封面稳定 salt `musly_stable` + 统一 `size=600` | 缩略图缓存键含 size，10 年强缓存 | URL 稳定才命中缓存 |
| `stream` + `estimateContentLength=true` | `EstimatedContentLength = duration×bitrate/8×1024` | 转码流可估计时长/拖动（#170） |
| `scrobble` start/finish（失败入离线队列补发） | PlayTracker 会话 + `incPlay` + buffered scrobbler | 429/网络抖动不丢播放记录 |
| `star/unstar/setRating/getStarred2` | `annotation` 表 + `average_rating` 重算 + RefreshResource 广播 | 多端同步 |
| `search3` | FTS5/bm25，CJK 自动 LIKE | — |
| `getLyricsBySongId` | structuredLyrics（含 enhanced cue） | 客户端三级回退 |
| `updatePlaylist` 重复 `songIdToAdd` 键 | `p.Strings("songIdToAdd")` 多值解析 | Navidrome 不识别 `[i]` 下标形式 |
| LAN/远程双地址探测（3s ping） | 无特殊处理 | 客户端 `resolveActiveUrl`，冷启跳过/后台切换 |
| 转码流 429 `Retry-After` | `ErrorTooManyTranscodes`（`TranscodeLimiter`） | 客户端本地转码开关/码率设置规避 |

---

*文档基于 Luobo master `c889513`（v1.1.12）客户端源码整理；服务端侧细节以 `docs/navidrome-服务端技术文档.md` 为准。*
