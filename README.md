# Luobo - 最佳 Navidrome 客户端

**Luobo**（萝卜）是基于 [Musly](https://github.com/dddevid/Musly) 二次开发的 Navidrome / Subsonic 音乐播放客户端，使用 Flutter 构建，支持 Android 和 iOS。**相比原版 Musly 增加了 Apple Music 风格首页、车载模式、听歌报告、AI 歌单等特色功能，并对设置页、音乐库艺术家页做了重构，以及大规模国际化适配。**

> **当前版本：v1.1.10**（在 Musly v1.0.13 基础上独立迭代）

---

## 🎵 Musly 原生功能

### 核心播放与服务器支持

- **多服务器兼容** — 支持 Navidrome、Subsonic、Airsonic、Gonic、Emby、Jellyfin
- **高品质音乐流** — 智能转码（WiFi/移动网络自适应比特率切换）、ReplayGain、无缝播放（Gapless Playback）
- **转码流完整进度控制** — 转码播放（AAC/MP3/Opus）时进度条正常显示，可随意拖动跳转，不再受限于服务器实时转码
- **音频增强** — 10 波段均衡器、淡入淡出、音高控制、播放速度调节、Dolby Atmos 支持

### 播放器与歌词

- **Apple Music 风格界面** — 现代化设计，暗色/亮色主题自动切换
- **同步歌词显示** — 实时歌词同步、桌面全屏歌词、iOS 动态岛歌词、Android 锁屏歌词、Windows 通知栏歌词
- **播放控制** — 随机播放、循环播放（列表/单曲）、播放队列管理、手势快速切歌

### 离线与本地

- **离线下载** — 并行下载、批量下载、离线播放切换自动降级
- **本地音乐库** — 自动扫描本地文件、与服务器库无缝融合

### 推荐与内容发现

- **智能推荐** — 基于听歌历史的个性化推荐、For You 列表
- **网络电台** — 服务器电台流媒体
- **浏览功能** — 按流派、艺术家、专辑、时间线浏览

### 高级特性

- **多服务器档案** — 快速切换不同服务器账户，支持从设置页直接编辑已保存的服务器配置（表单预填，添加新服务器不中断当前连接）
- **主题自定义** — 完整的 Now Playing 主题编辑器（5 个编辑 Tab、背景/圆盘/文字/控制/动画）、主题导入导出
- **Android Auto** — 车载系统原生集成
- **Chromecast / UPnP** — 投屏到电视
- **Discord 状态** — 正在播放的歌曲显示在 Discord
- **Jukebox 模式** — 服务器端远程播放控制
- **自动更新** — GitHub 版本检查和应用更新

### 平台覆盖

- 📱 Android 6.0+
- 🍎 iOS 15.0+
- 🖥️ Windows 10+
- 🐧 Linux（GTK）
- 🍎 macOS 11+

---

## 🚀 Luobo 差异化功能

### 🏠 Apple Music 风格首页

首页整体重构，图片优先、混合布局节奏（大卡 → 横卡网格 → 方块网格 → 横滑）：

- **知识图谱混合推荐** — `KnowledgeRecommendationEngine` + `HomeRecommendationService`：AI 生成的歌曲标签（情绪/场景/速度/能量/风格/歌手）构建倒排索引与偏好向量，**行为分 × 0.7 + 图谱内容分 × 0.3** 融合排序，熟悉/探索分层、跨模块全局去重，无图谱或无行为数据时自动退化到原算法
- **每日推荐大卡** — 全库按融合分取 Top30（熟悉优先、未听过补足），当日固定；封面随机 + 按封面主色生成渐变背景，一键播放全部
- **继续播放** — 最近播放 2 行 × 每行 5 个网格横滑，点击续播；歌单/收藏以 2×2 拼图卡与专辑一同展示
- **为你制作** — 通勤活力 / 专注学习 / 睡前放松 / 你的最爱 4 个 Mix，2×2 封面拼贴避免撞封面
- **探索发现** — 只推未听过的歌：4 行 × 5 首单曲横滑网格（加宽横卡、无播放按钮、歌名两行，与合集方块网格区分），按日固定
- **歌曲列表二级页** — 封面取色 Hero、随机标语、34pt 标题，随机 / 播放 / 加入队列三键
- **全 App iOS 侧滑转场** — 统一页面转场；旧首页保留可随时回退

**适用场景：** 🎧 打开首页即听、🚗 通勤路上听 Mix、💤 睡前放松听单

---

### 📍 车载模式（Car Mode）

专为驾驶安全设计的播放界面：
- **3 行大字歌词** — 当前行 + 前后各 1 行，实时同步，驾驶中清晰易见
- **环形进度条** — 可视化播放进度 + 已播/剩余时间
- **屏幕常亮** — 驾驶过程自动禁用息屏
- **下滑返回** — 手势快速返回播放器

**适用场景：** 🚗 开车听歌、🛵 骑车听歌、🏃 跑步健身

---

### 📊 听歌报告（Listening Report）

个人音乐数据分析面板，了解你的听歌习惯：
- **概览统计** — 总听歌时长、播放次数、歌手数、歌曲数
- **完播率分析** — 你有多认真地听完每首歌，配合个性化评语
- **时段偏好** — 早中晚分别喜欢听什么流派、什么歌手
- **排行榜** — 最常听的歌、最常听的歌手、最常跳过的歌
- **单曲循环** — 你最喜欢反复听的歌曲
- **流派分布** — 你的音乐品味占比（自动中英文归一化）
- **新发现** — 最近新喜欢的歌
- **老朋友** — 陪伴你最久的歌手
- **周末对比** — 周末 vs 工作日的听歌差异
- **能量曲线** — 一天各个时段的音乐节奏能量
- **歌荒预警** — 常听歌曲耗尽时的智能提示
- **连续听歌** — 连续听歌天数记录（Streak）
- **月度趋势** — 每月听歌量变化趋势
- **流派演变** — 数月间音乐品味的变化轨迹
- **报告说明** — 内置使用帮助，解释每个指标的含义

**适用场景：** 🎉 年终回顾听歌数据、📈 发现音乐品味变化、💭 了解个人听歌规律

---

### 🤖 AI 歌单系统

用 AI 自动生成个性化歌单，支持多 AI 平台：

**AI 歌单生成**
- 支持 DeepSeek、OpenAI、Kimi、阿里通义等多种 AI 模型
- 自定义 AI 连接配置（API Key、Base URL、模型选择）
- 三种生成模式：**最近听歌**（基于历史偏好）、**场景模式**（10 种预设场景，含图标）、**自由文本**（随意描述你想听的）
- 可选生成数量：15 / 25 / 40 首
- 歌单预览后可直接保存

**歌曲知识库系统**
- 批量为音乐库生成 AI 特征（加速后续推荐）
- 网络中断自动续接，无需重新开始
- 新增歌曲时自动增量更新，无需重复处理
- **知识库导出/导入** — 以 JSON 文件共享，多设备/多人协作时省去重复生成的 API 费用

**配置入口**
- 在「设置」新增独立「AI 歌单」入口（与播放设置分离）
- API Key、API URL（默认 DeepSeek）、模型名称均可自定义
- 实时进度显示、生成原理说明

**适用场景：** 🎵 快速生成主题歌单、👨‍👩‍👧‍👦 家庭/团队共享知识库、💰 降低 API 调用成本



---

## 🎨 UI 与国际化优化

### 设置页重构

设置入口从 8 个横滑 Tab 改为 **单列表分组 + 二级页**（对标 Apple Music / 网易云 / QQ 音乐）：

- **7 个分组** — 账号与服务器 / 播放与音质 / 下载与存储 / 显示与外观 / AI 智能 / 支持与帮助 / 关于
- **音质与流媒体独立二级页** — 转码设置（智能/WiFi/移动码率、格式）从播放设置拆出
- **信息架构归位** — 支持与关于去重、评分弹窗统一复用、诊断收敛至关于组底部、版本号统一引用唯一真源
- **机制说明** — 关于组新增入口：7 大类 34 条中文文案（首页推荐 9 / 听歌报告 3 / 下载与存储 7 / AI 3 / 音频 4 / 连接与投送 5 / 诊断与隐私 3），三级导航 + 技术细节默认收起可展开
- **更新日志页** — 关于组直达，从 README 版本历史实时解析渲染

### 音乐库艺术家页改版

- **方形封面网格** — 网易云/QQ 风格方形网格 + 保留字母索引（A-Z + #），替换原纯文字 chip 瀑布流
- **常听歌手置顶 shelf** — 按 App 本地播放统计（非服务端 play_count）取 Top 8，带播放次数小字；无播放历史自动隐藏
- **5 级封面解析链路** — `artistImageUrl` 直链 → `coverArt` → 专辑封面单图 → **2×2 专辑封面拼贴** → 渐变首字母占位，保证任何图源下页面都不素
- **拼贴自适应布局** — 按专辑张数 2×2 / 左大右小 / 斜对角 / 单图铺满；无封面专辑占位图运行时 hash 学习自动排除

### 刷新反馈

- 音乐库 AppBar / 首页刷新按钮统一走 **刷新状态机**（按钮 loading 态）
- 完成后居中 SnackBar 提示：**成功（专辑/歌曲数）/ 本地重载 / 失败原因**；已在刷新中的并发请求自动合并不重复弹窗

### 播放条转码状态 toast

- 长按迷你播放器封面显示居中、自动消失的 toast：源文件信息 + 实际/将要/未转码状态
- **网络信息前置成组**（WiFi 绿 / 蜂窝橙图标 + 文字），SnackBar 观感统一（inverseSurface 深色圆角条）

### 中文适配

- **完整中文翻译** — 补全原版缺失的 168 + 15 个条目
- **流派本地化** — 自动转换为中文（Pop → 流行、Rock → 摇滚、Classical → 古典等）
- **术语统一** — 歌手、歌曲、专辑、播放列表等术语统一为符合习惯的表述

### 播放器界面优化

- **歌词入口重新设计** — 从顶部 header 移到歌名旁，与歌名对齐（歌词 / 车载 / + / ♡）
- **音量条移除** — 播放页下方不再显示音量滑动条，简化界面
- **Android 全屏适配** — Edge-to-edge 模式，底部系统手势条透明化处理
- **音乐库四 Tab 平级横滑** — 顶部 TabBar + 页面左右跟手滑动（艺术家/专辑/歌曲/收藏，本地模式自动扩展 6 Tab），惯性落页、滚动位置保留，艺术家 Tab 默认置顶

### 大规模国际化（28 种语言）

- **歌单管理** — 多选移除、重新排序、批量下载等操作界面
- **全部歌曲排序** — 排序菜单完整翻译
- **收藏功能** — 点赞、收藏提示完整翻译
- **电台界面** — 流媒体电台相关文案
- **登录错误** — 分类错误提示（网络错误、认证失败、服务器错误等）
- **主题编辑器** — 5 个 Tab、所有控件的标签和说明
- **自定义组件** — Flutter 代码编辑器的所有提示和帮助文本

---

## 🐛 Bug 修复

- 修复创建歌单弹窗取消/确认后的崩溃问题（Context 覆盖导致的异常）
- 修复专辑卡片在网格布局中底部溢出 1px 的问题
- 修复转码播放（如 AAC 192kbps）时进度条不更新、拖动进度条导致歌曲从 0 秒重新播放的问题
- 修复 Smart 转码关闭时移动网络下仍使用 WiFi 码率的问题
- 听歌报告歌手浓度卡片统计口径与排行榜不一致的问题
- 修复随机播放重复播放同一首歌的问题（改用 Fisher-Yates 洗牌队列，每首歌每轮仅播放一次）
- 修复播放队列面板在亮色主题下顶部露出白色色块的问题
- 修复冷启动首帧整页空态闪屏的问题（loading 条件改 `!isInitialized`）
- 修复诊断系统双实例并存：写锁所有权校验，被接管实例停止落盘（曾见接管后旧实例继续写旧分卷）

---

## 🛠️ 版本历史

**v1.1.10（当前）** — 局域网转码策略（局域网连接强制原始音质）：

**🎚️ 局域网转码策略（规则 1）**
- ✅ **局域网连接强制不转码** — 连接局域网（localUrl）时转码强制关闭：`currentBitRate` 回落原始、`getCurrentFormat` 返回 null，不拼 maxBitRate/format 参数，服务器直出原码流（无损音质零转码延迟）；WiFi/移动码率设置在局域网下不生效
- ✅ **LAN↔远端切换即时生效** — `SubsonicService` 新增 `onActiveUrlChanged` 回调，启动探测 / 后台探测 / 网络变化 forceProbe 切换地址时通知转码层刷新生效码率（`_setActiveBaseUrl` 值变化才通知）
- ✅ **转码/缓存流路由修正** — `PlayerProvider` 新增 `_willTranscode()` 按「实际是否转码」选择 ExoPlayer 直连或 LockCachingAudioSource 缓存流：局域网原码流重新走本地缓存，拖动定位不受影响；三处转码参数拼接统一走 `getCurrentBitrate/getCurrentFormat`
- ✅ **Jellyfin 排除** — Jellyfin 恒走远端 URL 不判局域网，避免公网场景误强推原码流
- ✅ **设置页显式提示** — 局域网连接时流媒体设置页顶部显示绿色 LAN 图标「局域网连接中 · 强制原始音质（不转码）」，避免误以为设置失效
- ✅ **LAN-only 配置双存** — 仅配置局域网地址时 localUrl 与 serverUrl 同址双存，规则 1 在纯局域网场景仍生效
- ✅ **测试与文档** — 新增 `transcoding_service_test`（LAN 覆盖 / 地址切换刷新），`subsonic_service_test` / `player_provider_test` 适配；`docs/局域网转码策略技术方案.md` 归档；机制说明与 28 语言文案同步（`transcodingLanForceOriginal`）

**v1.1.9** — 封面缓存全面优化（连接复用 / 语义化 key / 内存预热 / 登出不清缓存）：

**🖼️ 封面缓存优化（P0-P5 全量落地）**
- ✅ **封面下载复用连接池（P0）** — 封面下载从每次新 TCP+TLS 握手改为共享 `http.Client`（懒加载以继承登录时配置的自签证书），连续加载未缓存封面 TCP 连接数从 ~50 收敛到 1~2
- ✅ **切歌不再冷下载高清图（P1）** — 修复 `_refreshArtworkUrl` 死代码：iOS Now Playing / Control Center 封面查找改用语义化 key 命中已下载变体（1200→200 逐档），同一首歌再次播放零新增 getCoverArt 请求
- ✅ **首屏内存预热（P2）** — 全量预热首批 24 张封面同步解码进内存 ImageCache，冷启动首屏滚动不再逐张"实时解码卡一拍"
- ✅ **语义化缓存 key（P3）** — 磁盘 key 由 URL 改为 `coverArt-<serverId>-<id>-<size>`（serverId = md5(服务器地址 + 用户名)，不含密码）：换密码不再全量失效、可按 id 反查、多服务器/多账号不串图；全 App CachedNetworkImage 调用点（~18 处）统一收口
- ✅ **登出不清封面缓存（P4）** — 登出仅清内存解码结果，磁盘封面保留：同账号登出再登回秒开；换账号/换服务器由语义化 key 天然隔离，设置页手动清空兜底
- ✅ **缓存上限与字节守护（P5）** — 对象上限 1000→10000（大库不再频繁驱逐重下）；新增 `CoverCacheCleaner` 启动时按 512MB 字节阈值清理最旧封面（走 removeFile 同步删索引记录，带诊断打点）
- ✅ **设置页占用统计修正** — 封面缓存目录统计从 appCacheDir 改到 tempDir（flutter_cache_manager 实际存储位置），机制说明文案同步更新

**v1.1.8** — 音乐库四 Tab 平级横滑、登录连接兜底与首页探索副标题：

**🎵 音乐库四 Tab 平级横滑**
- ✅ **顶部 TabBar + 页面跟手横滑** — 艺术家/专辑/歌曲/收藏（本地模式自动扩展 6 Tab）从 FilterChip 原地跳变改为网易新闻/百度 App 首页式：文字 TabBar + 下划线指示器 + TabBarView 左右跟手滑动、惯性落页，点 Tab 与横滑双向联动（删除原 `_swipeDelta` / GestureDetector / AnimatedSwitcher 跳变逻辑）
- ✅ **每页滚动位置保活** — 每 Tab 独立 ScrollController 常驻，切走再切回滚动位置不丢（Albums / Songs / Faves 首次支持）
- ✅ **本地/服务端模式动态适配** — 4⇄6 Tab 切换时 TabController 自动重建，选中 Tab 不跳位
- ✅ **切回艺术家页封面预热** — 预热时机移到 Tab 落定触发，缓解冷内存批量读盘"一张张冒出来"

**🔌 登录/连接稳定性**
- ✅ **认证备选地址兜底（failover）** — 登录 ping / 连接验证首 ping 失败且为连接类错误（Socket / refused / 超时，凭据与 SSL 除外）时，`resolveActiveUrl(forceProbe: true)` 强制同步探测备选地址（LAN↔远端）后重试一次，消除「上次在远端→远端抖动但局域网通」「局域网抖动但远端通」两类偶发登录失败
- ✅ **网络变化强制重探测** — 切换网络时不再依赖上次会话记忆跳过探测，立即同步重估可达地址

**🏠 首页探索发现**
- ✅ **单曲横卡副标题** — 「歌手 · 专辑」填满右侧留白，认歌更快；`ContinuePlayingCard` 支持自定义副标题

**🧪 测试**
- ✅ `resolveActiveUrl(forceProbe)` 单测 — 本地 HTTP 服务模拟可达局域网端点

**v1.1.7** — 首页 / 设置页 / 艺术家页三大重构、诊断系统加固与刷新反馈：

**🏠 首页重构（Apple Music 风格）**
- ✅ **知识图谱混合推荐** — 引入 `KnowledgeRecommendationEngine`（标签倒排、Jaccard 相似度、偏好向量）与 `HomeRecommendationService`，行为/内容 α=0.7 融合，熟悉/探索分层、全局去重、按日缓存，无图谱或无行为自动退化
- ✅ **每日推荐补足** — 全库按融合分取 Top30（熟悉优先、未听过补足），避免行为数据薄时推不满
- ✅ **Apple Music 风格首页** — 每日推荐、继续播放、通勤/专注/睡前/你的最爱 4 个 Mix、探索发现、最近播放混合区；每日推荐与歌曲二级页按封面主色生成渐变，Mix/歌单/收藏用 2×2 封面拼贴
- ✅ **继续播放双行横滑** — 最近播放 2 行 × 每行 5 个网格横滑（原单行 10 个纵向排布）；`PlaybackContextTracker` 持久化手动播放的歌单/收藏与专辑同区展示
- ✅ **探索发现单曲横滑网格** — 4 行 × 5 首全量横卡（无播放按钮、歌名两行，与合集方块区分），探索发现按日固定
- ✅ **歌曲列表二级页** — 封面取色 Hero、随机标语、34pt 标题、随机/播放/加入队列三键
- ✅ **全局页面转场** — 全 App iOS 侧滑转场；首页顶部搜索图标替换为设置图标直达设置页
- ✅ **首页下拉刷新移除** — 同步曲库统一走音乐库刷新（带结果反馈）
- ✅ **播放队列面板修复** — 亮色主题下队列弹层上方露白色色块（`DraggableScrollableSheet` 增加 `expand: false`）
- ✅ **封面取色不重复下载** — 每日推荐/二级页取色复用磁盘封面缓存

**🎛️ 设置页重构**
- ✅ **8 Tab → 单列表分组 + 二级页** — 账号与服务器 / 播放与音质 / 下载与存储 / 显示与外观 / AI 智能 / 支持与帮助 / 关于 7 组；旧 Tab 身体 1:1 复用为二级页（`settings_root_screen.dart` + `settings_sub_page.dart` 薄壳）
- ✅ **音质与流媒体独立二级页** — 转码设置段自播放 Tab 整体搬移（播放 Tab 净减 ~290 行）
- ✅ **信息架构归位** — 评分弹窗抽出 `rating_dialog.dart` 复用、诊断收敛至关于组底部、支持/关于去重、版本号统一引用 `UpdateService.currentVersion`
- ✅ **机制说明** — 关于组新增「机制说明」入口：7 大类 34 条（首页推荐 / 听歌报告 / 下载与存储 / AI 智能 / 音频 / 连接与投送 / 诊断与隐私），三级导航 + 技术细节默认收起
- ✅ **更新日志页** — 从 README.md 版本历史实时解析渲染，无需新增依赖

**🎵 音乐库艺术家页改版**
- ✅ **方形封面网格** — 网易云/QQ 风，替换原纯文字 chip 瀑布流，保留字母索引
- ✅ **常听歌手置顶** — 本地播放统计 TopN（最多 8 位，行为画像变化自动失效重算）
- ✅ **5 级封面解析链路** — `resolveArtistCover`：artistImageUrl 直链 → coverArt → 专辑单图 → 2×2 专辑封面拼贴 → 渐变首字母占位（按名字 hash 稳定取色）
- ✅ **拼贴自适应布局** — 按专辑张数 2×2 / 左大右小 / 斜对角 / 单图；Navidrome 占位图运行时 hash 学习全局排除
- ✅ **全量同步后补封面预热** — 冷启磁盘封面不空，艺术家拼图/列表加载提速

**🔁 刷新反馈**
- ✅ 刷新状态机（`RefreshStatus` 驱动按钮 loading）+ 居中 SnackBar 结果提示（成功专辑/歌曲数、本地重载、失败原因）；并发刷新自动合并不重复启动全量同步

**📡 诊断系统加固**
- ✅ **跨实例写锁** — `.writer.lock` 防 Flutter 引擎重建后双实例并发追加（曾致 JSONL 写穿、seq 重复）；**写锁所有权校验**：锁文件升级 `ts token`，心跳校验 token，被接管即 `lockLost` 停止落盘转 secondary 周期重试接管
- ✅ **跨启动 seq/会话对账** — 冷启动从事件文件尾恢复 `{seq, appSessionId}`，修复实例重建后 seq 回退、会话分叉
- ✅ **慢帧事件** — `frame.slow`（UI 线程 build+raster 25~50ms，转场动画轻微掉帧，info 级秒级聚合）
- ✅ **jank 判定改 UI 线程实际耗时** — `buildDuration + rasterDuration` 替代 `totalSpan`，避免低帧率上下文（Android Auto 投射/后台）误报
- ✅ **转场埋点增强** — 进全屏页实测转场耗时（掉帧拖长动画即量化"进全屏是否卡"）、下拉隐藏手势带 dragOffset/velocity
- ✅ **debug print 节流** — 按消息前缀 10s 时间窗去重，避免循环打印淹没事件流
- ✅ **导出单文件** — meta 概要 + 可读日志 + 原始 JSONL 尾部 + fps/jankRate/网络 60s 指标快照，移动端走系统保存对话框（SAF）
- ✅ **诊断导出携带真实 appVersion** — `main()` 启动早期写入 `kAppVersion`
- ✅ **冷启动跳过 LAN 探测** — 远程会话结束跳过本地探测（原最多阻塞 3s），后台静默重探并在可达时切换

**🌐 其他**
- ✅ 播放条转码 toast 网络信息前置成组（图标 + 文字），SnackBar 观感统一
- ✅ 后台刷新延迟 5s → 2s，刷新防重入与结果返回
- ✅ 本地化补充（刷新反馈 / 设置分组 / 机制说明等文案）+ `flutter gen-l10n` 全量重生成
- ✅ 推荐与播放上下文测试覆盖（图谱推荐、首页融合推荐、最近播放上下文、写锁互斥/接管/对账、艺术家 TopN/封面解析）

**v1.1.6** — 播放统计修复与封面缓存全局统一：
- ✅ **播放完成事件竞态修复** — 捕获 `_completedSong` 快照，异步 `_onSongComplete` 不再把新歌误计为完成；手动切歌后旧事件直接丢弃
- ✅ **听歌报告周末/工作日统计修复** — 改用 `dailyPlays` 逐日统计，不再把歌曲所有历史播放误归到最后一次播放的星期几
- ✅ **时段偏好只统计有效播放** — `hourlyPlays` 移入 `completed` 分支，未听完的播放不再计入时段偏好
- ✅ **未知时长保护** — `_recordSongEnd` 在 duration 未知时要求至少听满 30 秒才计为完整播放
- ✅ **单曲循环重复记录修复** — 循环至下一轮时重置 `_trackedSongId`，同一首歌每轮独立计次
- ✅ **封面请求尺寸全局统一为 300px** — 引入 `kCoverArtRequestSize = 300` 常量，覆盖 `album_artwork`、`home_screen`、`library_screen`、`now_playing_screen`、`car_mode_screen`、`synced_lyrics_view`、`library_search_delegate`、`offline_service`，消除多个硬编码 size 值（80/120/200/300/600）
- ✅ **Release 构建去除调试日志** — cover prefetch 系列 `debugPrint` 加 `kDebugMode` 保护

**v1.1.5** — 音质信息、艺术家索引、缓存与性能优化：
- ✅ **歌曲音质信息** — 歌曲封面底部音质角标（`FLAC·964`），点 ⋯ 查看歌曲信息（格式/码率/采样率/位深/大小/转码状态），转码状态与实际播放流一致（非设置推断）
- ✅ **艺术家索引重做** — 中文歌手按拼音首字母分组（A-Z + #），右侧字母擦条快速跳转，chip 瀑布流布局 + 每艺术家歌曲数
- ✅ **音乐库搜索升级** — 顶部搜索支持歌单/艺术家/专辑/歌曲四类本地匹配
- ✅ **播放页歌名自适应** — 歌名显示在封面上方，短名 1 行、长名 2 行，宽度与封面一致
- ✅ **迷你播放器优化** — 歌名字号自适应（16→12px 完整显示），封面描边显示局域网（绿）/外网（橙），长按封面查看连接详情
- ✅ **缓存管理** — 设置 → 存储 显示封面缓存占用量，清空缓存增加二次确认
- ✅ **封面缓存按专辑归一** — 同专辑歌曲共享同一封面缓存键（不再每首歌重复下载），尺寸档位统一为 120/300/600，播放页大图 1200→300（解码量降 16 倍）
- ✅ **性能优化** — 艺术家详情缓存优先 + 后台刷新、全量同步 8 并发、自适应字号测量缓存、毛玻璃降耗
- ✅ **服务器配置可编辑** — 设置 → 服务器列表点铅笔（或点行）直接编辑已保存的配置（表单预填服务器/账号/证书等），添加新服务器改为压栈打开登录页，不再断开当前连接
- ✅ **智能转码设置重构** — 关闭智能转码时仅显示单一「转码码率」固定档，开启时显示 WiFi/移动网络两档并自动切换，修复手动模式下移动网络仍用 WiFi 码率的问题；转码信息行码率完整显示
- ✅ **封面缓存按档位取图** — 封面请求按显示档位传 size（不再一律 600px），列表/迷你条/歌曲弹窗共享同一缓存键，切歌时旧封面保持显示不闪占位图
- ✅ **车载模式副标题** — 显示「歌手 · 专辑」，驾驶中信息更完整

**v1.1.4** — UI 打磨 + AI 歌单升级 + 网络与缓存优化：
- ✅ **按压缩放动画**（PressableScale）— 全 UI 统一，所有可点击卡片/列表项均有按下时 0.96x 缩放反馈，覆盖首页、音乐库、歌单卡片、艺术家/专辑卡片等
- ✅ **下拉刷新**（首页）— 使用 EasyRefresh ClassicHeader，iOS/Android 双端一致
- ✅ **首页说明入口**（ⓘ 按钮）— 解释「为你推荐」「快速选择」「最近播放」等各分区的来源逻辑
- ✅ **局域网 / 外网状态指示器** — 迷你播放器右侧实时显示当前网络模式（绿色"局域网" / 橙色"外网"）
- ✅ **登录支持纯局域网地址** — 外网地址非必填，只填内网地址也能登录连接
- ✅ **网易云歌词回退** — LRCLIB 无歌词时自动查询网易云音乐，提高中文歌词命中率
- ✅ **AI 歌单独立设置 Tab** — 从播放设置中分离，独立「AI 歌单」Tab，含 API Key / URL / 模型配置及知识库管理
- ✅ **AI 知识库生成状态提升为全局单例** — 离开页面进度不丢、冷启动从磁盘恢复已索引数、进程被杀可增量续传
- ✅ **AI 歌单生成界面升级** — 支持「最近听歌」「场景模式（10 种预设）」「自由文本」三种模式，可选生成数量（15/25/40）
- ✅ **封面缓存统一优化** — 所有封面请求统一使用 600px 尺寸，自定义 CacheManager（最多缓存 1000 张，60 天保留期）
- ✅ **流式缓存上限**（2GB）— 播放完毕后自动清理旧 `.tmp` 临时文件，防止无限占用磁盘
- ✅ **随机播放重构** — Fisher-Yates 洗牌，保证每首歌在一轮内仅播放一次，修复重复播同一首的 bug
- ✅ **底部弹窗去毛玻璃** — 所有 BottomSheet 改为纯色背景（深色 #1C1C1E / 浅色白色），避免视觉模糊
- ✅ **TextTheme 行高优化** — display 1.1 / title 1.3 / body 1.4，中文排版更舒适

**v1.1.3** — Luobo 独立版本，基于 Musly v1.0.13：
- ✅ 首页歌曲列表隐藏时长，歌名最多两行完整显示
- ✅ 播放页封面上方新增歌曲标题显示
- ✅ App 名称正式更名为 Luobo（萝卜），Dart 包名同步为 luobo
- ✅ 转码流完整进度控制（进度条正常 + 可拖动跳转）
- ✅ 音乐库四 Tab 滑动切换（艺术家/专辑/歌曲/收藏）
- ✅ Smart 转码码率修复、听歌报告统计口径修正
- ✅ 车载模式、听歌报告、AI 歌单（差异化功能详见上文）

**v1.0.13（Musly 上游基线）** 包含：
- ✅ Now Playing 自定义主题系统（5 个编辑 Tab）
- ✅ 无缝播放（Gapless Playback）
- ✅ LRCLIB 歌词自动查询
- ✅ 音频淡入淡出、音高控制
- ✅ Dolby Atmos 支持检测
- ✅ 所有平台 Bug 修复与优化

详见 [CHANGELOG.md](CHANGELOG.md)

## 运行

```bash
flutter pub get
flutter run
```

构建 release APK（arm64）：

```bash
flutter build apk --release --target-platform android-arm64
```

## 连接服务器

1. 启动应用
2. 输入服务器地址（如 `https://your-server.com`）
3. 输入用户名和密码
4. 如果使用旧版服务器，开启"旧版认证"
5. 点击"连接"

## 上游同步

本项目 fork 自 [dddevid/Musly](https://github.com/dddevid/Musly)，可随时同步上游更新：

```bash
git remote add upstream https://github.com/dddevid/Musly.git
git fetch upstream
git merge upstream/master
```

## 许可证

本项目遵循 **CC BY-NC-SA 4.0** 许可证，不可用于商业分发。详见 [LICENSE](LICENSE)。
